# 英雄艾卓-尼鲁布 / 阿努巴拉克（29120，map 601）

## 接手摘要

- 更新：2026-09-12 深夜第三轮（run449–run456）。累计 **22 场 4 击杀（约 18%）**，
  击杀耗时稳定在 **249–264 秒**。
- **本轮最重要的产出不是击杀率，是定位了一个共享层缺陷**：
  [bot 移动时放弃一切读条法术](../../DEFECT-CAST-WHILE-MOVING.md)。
  牧师有读条的治疗 **92% 被移动打掉**，2.5 秒的强效治疗 110 次尝试 0 次成功，
  一半以上 GCD 在甩魔杖。这解释了「坦克被爆发打死 → 全队崩」这条主线。
- 唯一下一步：**去修那个共享层缺陷**（单开一轮，见该文档的修复方向与回归口径）。
  本副本这边不要再改目标选择/仇恨，本轮又新增了一条反证。
- 阻塞：无。

## ⚠️ 本轮我判断错两次，先写在前面

1. **把践踏当成瓶颈**。做了闪避才发现它两场只触发一次——`ANPOUND` 逐 tick 轨迹显示
   非坦克与锥轴的最小夹角是 25° / 89° / 108°，**根本不在 ±12° 的锥里**。
   践踏不是瓶颈。
2. **把潜地空转的收益估成 90 秒**。核对召唤时刻表后是错的：50% 段最后一只毒疗者
   **29 秒**才进场，按实测每只 12–13 秒，理论最快 42 秒清完、47 秒出土，
   对比上限 60 秒，**每段天花板只有 13 秒**，两段合计约 25 秒。

教训写在最后一节。

## ⚠️ 先纠正上一轮记错的结论

上一轮（run430）我把死因记成「戒律牧师开场把 GCD 花在 1 小时团队 buff 上」。**这是错的**，
那只是次要现象，且只出现在团灭之后的场次（团灭抹掉 buff、框架开怪前不补，bot 只能在战斗中补）。

真实死因是**场景坐标错误**，下面有完整证据链。

## 根因：远程被关在 boss 房门外，治疗因此选不到坦克

阿努巴拉克房间有三扇门 192396/192397/192398（`gameobject_template.type = 0` 实体门、
`Data0 = 1` 初始开启、尺寸 3.4–3.75），位于 **y ≈ 252.2–256.2**；实例脚本把它们登记为
`DOOR_TYPE_ROOM`，`boss_anubarak.cpp` 的 `EVENT_CLOSE_DOORS` 在开怪 **5 秒后**关闭。

boss 在 y = 248.3，即门的**南侧**。而我把准备点 (551, 280) 与开怪点 (551, 260) 都放在了
门的**北侧**。于是每场都是：坦克和盗贼进门内打 boss，治疗/法师/萨满留在门外 —— 门一关，
**三个远程与坦克之间没有视线**。

治疗侧的连锁反应（`PartyMemberToHeal.cpp:135` 的 `IsWithinLOSInMap`）：
坦克被过滤掉 → 队伍里唯一「可治疗」的只剩牧师自己 → 所有治疗动作退化成对自己施放 →
自己满血 → 全部 `IMPOSSIBLE`。

**逐 tick 轨迹实证**（run443，`LogInGroupOnly = 0`）：

```
target is immuned to spell - target name: Raidtebnfivc, spellid: 48066, bot name: Raidtebnfivc
Raidtebnfivc A:power word: shield - IMPOSSIBLE
Raidtebnfivc A:power word: shield on party - IMPOSSIBLE
```

（48066 是真言术：盾，撞的是牧师自己身上的虚弱灵魂。冷却类失败同样全是自己：
强效自愈 10060 ×35、愈合祈祷 48113 ×12、苦修 53007 ×7。）

采样证据（run443 a1）：牧师 `in_combat=true`、`hp=16500/16500`，
**法力一路从 15786 回到 16283**（= 完全没施法），从 10.7 秒到 102.5 秒空转 **92 秒**；
同期坦克从满血被磨到 0、**55.2 秒阵亡，全程一次治疗都没吃到**。
按角色分：坦克与盗贼（近战）全程活跃，治疗/法师/萨满三个远程在 20–80 秒几乎零施法。
萨满图腾落点（带 `dst=`）显示它整场停在 y ≈ 278–280，牧师位置采样固定在 (551.00, 280.00, 224.33)。

## ⚠️ 产生这个坐标的方法错误：`raidtest los` 的 z 用法

`raidtest los <map> <x1> <y1> <z1> <x2> <y2> <z2>` 里：

- `vmap_floor_from/to` 是从 **z+2 向下搜 50 码**找地面 —— 传偏高的 z 只是搜索起点，没问题；
- 但 **`los` 是从 `(x1,y1,z1+2)` 到 `(x2,y2,z2+2)` 量的**。

我勘测时传了 z1 = 240 而地面在 ≈ 223，等于**在离地 18 码处量视线**，墙沿、坑沿全越过去了，
整批 `los=true` 都是假的。按它选的第二版坐标 (551, 224) 用实测地面 221.65 复量是 `los=false`，
5/5 场 `pull_rejected`（日志：`los=false distance=12.88 leader_pos=(551,224,221.65)`）。

**正确做法是两步**：第一遍随便传个高 z 拿 `vmap_floor_from`，
第二遍**用拿到的地面高度当 z1** 重新量 los，只信第二遍。

另外，`raidtest los` 是**静态 vmap 探针，看不到战斗中才关上的门**。
boss 带 `DOOR_TYPE_ROOM` 门时，准备点与开怪点必须都在 boss 那一侧。

## 可复现基线

| 项 | 值 |
|---|---|
| 场景 | `heroic-an-anubarak-n5`（完整遭遇战，无前置怪） |
| map / boss | 601 / 29120（英雄 entry 31610） |
| 难度 / 人数 | heroic / 5，normal5-v1，`BotCheats` 空 |
| 盗贼专精 | 刺杀 |
| **准备点** | **(527, 248, 224.35)** — 距 boss 24.0 码（> 22 码仇恨半径），门内侧，以 224.35 为 z1 实测 `los=true` |
| **开怪点** | **(543, 248, 223.44)** — 距 boss 16.0 码，地面高度实测 `los=true`，同在门内侧 |
| TimeoutSeconds | 480 |
| run | **445** |

内圈地面 y 240–251 约为 x 527–575，向南收窄；x ≤ 523 没有地面。
**门内侧距 boss 最远就是 24 码**（东西两翼 y = 248/251），没有更远的合法点。

## 尝试记录（run 445）

| 场 | 结果 | 用时 | 死亡 | boss 最低 | 盾 | 苦修 | 愈合祈祷 | 牧师总施法 |
|---|---|---|---|---|---|---|---|---|
| 1 | 作废 | 157.7 秒 | 0 | 42% | 19 | 4 | 35 | 249 |
| **2** | **击杀** | **249.9 秒** | **0** | **0%** | 35 | 6 | 46 | 399 |
| 3 | 团灭 | 229.4 秒 | 5 | 20% | 33 | 10 | 41 | 400 |
| 4 | 团灭 | 127.6 秒 | 5 | 54% | 10 | 2 | 6 | 124 |
| 5 | 超时 | 480.0 秒 | 4 | 17% | 25 | 7 | 40 | 323 |

对照修正前（run443 a1，治疗空转那场）：牧师**全场只有 29 次施法**。

a1 的 `boss lost combat state (stuck/reset)` 值得注意：`boss_anubarak.cpp` 的
`SummonedCreatureEvade` 会在**任何一只召唤怪 evade 时直接 `EnterEvadeMode()`**，整场重置。
建场景时就记过这个易碎点，a1 应当就是它。

## 第三轮（run449–run456）：践踏机制被查清，两条改动被证伪并回退

### 基线表

| run | 改动 | 击杀 | 各场 boss 最低血量 | 均时长 |
|---|---|---|---|---|
| 449 | 穿刺闪避（上一轮结论） | 1/5 | 0 / 46 / 21 / 73 / 18% | 337s |
| 450 | 践踏「沿半径后撤 25 码」 | 0/3 | 72 / 87 / 37% | 313s |
| 451 | 践踏「横向侧移出锥」 | 0/5 | 6 / 73 / 24 / 10 / 21% | 295s |
| 452 | 同 451（诊断构建） | 2/2 | 0 / 0 | 258s |
| 453 | 同 451（复跑） | 1/5 | 46 / 19 / 19 / 14 / **0** | 211s |
| 454 | + DPS 集火大召唤物（无坦克接怪） | **0/5** | 65 / 48 / 45 / 43 / 71% | 213s |
| 455 | + 两段（坦克先接 + DPS 集火已接住的） | **0/5** | 13 / 21 / 14 / 24 / 20% | 244s |
| 456 | 回退 454/455，单场诊断 | 1/1 | **0** | 258s |

451 / 452 / 453 是同一份代码，击杀率 0/5、2/2、1/5 —— **这个副本的单次 5 场样本噪声极大，
不要用一个 5 场 run 判断一条改动的好坏。**

### ✅ 践踏的真实机制（上一轮和本轮前半段都搞错了）

`DoCast(target, SPELL_POUND)`，英雄版 **59433**：

- `EffectImplicitTargetA = 24` → `SpellInfo.cpp:237` 是 **`TARGET_UNIT_CONE_ENEMY_24`**
  （CONE / DIR_FRONT），**不是圆形区域**
- `Spell.cpp:1251` 对该目标类型写死 `cone_degrees = 24`，`Position::HasInArc` 取半角
  → **±12°**；`EffectRadiusIndex` 给出锥长 **15 码**
- boss 在 `DoCast` **之前**先 `SELF_ROOT` + `DisableRotate(3300ms)`，读条期间锥向锁死
- `spell_azjol_nerub_pound` 对每个命中者补 59432，**满额不分摊**（BasePoints 47,124）
- `IsWithinBoundaryRadius` 不是漏洞：玩家 `UNIT_FIELD_BOUNDINGRADIUS` ≈ 0.4，
  取 `max(…, MIN_MELEE_REACH = 2.0)`，只有站在 boss 原点 2 码内才无视角度

实测单次伤害：坦克 15,551–19,973（扛得住），布甲 27,502 / 32,603 / 35,953 / 37,154 /
40,664（约 2.5 倍血量上限，必死）。

### ❌ 践踏「沿半径后撤」——方向错误，已改（run450）

原实现 `Move(bot->GetAngle(boss), distance + 2)` 是朝 boss 跑、穿到背后 2 码。
我先按「圆形 AoE」把它改成朝反方向撤到 25 码 —— **正好是沿锥轴直线逃跑，全程待在锥里**，
要跑满 15 码才出得去。三场 7 次重击、单次最高 39,254，比不动还差；第 1 场 22.3 秒
一发同时打中三人（23,663 / 37,540 / 39,254），81 秒团灭。

### ⚠️ 践踏「横向侧移」——几何正确，但实战几乎不触发（保留）

改成垂直于 boss→自己连线横移：夹角直接加 `atan(L/d)`，在 d 码处只要 `d·tan(12°) ≈ 0.21d`
就出锥，取 `max(6, 0.45d)`，且只有真在锥内（`HasInArc` + 15 码内）的非坦克才动。

**但逐 tick 轨迹证明它是个近乎空操作**：run452 两场（都击杀）133 个读条 tick 里，
`ANPOUND exec` 只有 **1 次**（`moved=1`，成功）。

| bot | 坦克 | ticks | 离 boss 距离 | 与锥轴最小夹角 | 在锥内的 tick |
|---|---|---|---|---|---|
| Raidteanfivc | 是 | 30 | 8.2–12.0 | **0.0°** | 30 |
| Raidtebnfivc | 否 | 32 | 12.2–20.7 | 25.0° | 0 |
| Raidtecnfivc | 否 | 31 | 4.9–12.3 | 108.0° | 0 |
| Raidtednfivc | 否 | 22 | 12.2–20.6 | 89.2° | 0 |
| Raidteenfivc | 否 | 17 | 5.8–17.9 | 0.0° | 1 |

boss 永远面向坦克，±12° 太窄，其余人天然站在锥外。**践踏不是这个 boss 的瓶颈。**
这条改动保留，因为它是对真实机制的几何修正（旧实现 52 次零成功），且无害；
但**不要指望它提高击杀率**。

### ❌ 潜地期集火大召唤物——两种形状都失败，已全部回退（**别重走**）

机制本身是真的：`boss_anubarak.cpp:267` `SummonedCreatureDies` 只统计
守卫 **29216** / 毒疗者 **29217**，`_remainingLargeSummonsBeforeEmerge` 归零就把
`EVENT_EMERGE` 从 60 秒改成 5 秒（开头 `if (!me->HasUnitFlag(UNIT_FLAG_NOT_SELECTABLE)) return;`
—— 出土后再杀**不计数**）。英雄三段分别要杀 2 / 4 / 4 只。

22 场潜地停滞实测：75% 段 20–45 秒（**杀掉了**），50% 段 51–66 秒、25% 段 38–63 秒
（**都吃满 60 秒**）。一场约 145 秒 boss 不可攻击。

**run454（只让 DPS 集火）**：0/5，五场全部活不过第二段潜地（最好才 43%），
首段停滞中位数从 ~31 秒涨到 ~49 秒。承伤归属（每场均值）：

| 来源 | run453（无集火） | run454（DPS 集火） |
|---|---|---|
| Anub'arak | 123,640 | 100,988 |
| **守卫 29216** | 92,497 | **111,070（跃居第一）** |
| 毒疗者 29217 | 78,415 | 48,339 |

**run455（两段：坦克先接 + DPS 只打已被坦克接住的）**：0/5，但危害消除
（boss 最低血量均值从 54.4% 回到 18.4%，是所有非击杀 run 里最深的）。
**危害没了，收益也没兑现** —— 见上面第 2 条判断错误，天花板本来就只有 25 秒。

两条都已 `git checkout` 回退，只留践踏侧移。

**教训（与上一轮的转火反证叠加）**：这个 boss 的小怪目标选择已经被证伪**三次**
（run446 全队转火、run447 坦克主导、run454 潜地 DPS 集火），第四次（run455 两段）
虽然不再有害但也无收益。**不要再动这个副本的目标选择和仇恨。**

### 📌 召唤物不会在出土时消失

`EVENT_EMERGE` 只做 `RemoveAurasDueToSpell` + 去掉不可选中标记，**不清召唤物**；
`summons.DespawnAll()` 只在 `EnterEvadeMode` 里。实测每场生成 6 只守卫 + 2–4 只毒疗者，
180 秒后仍有约 4 只在持续输出。这是后期承伤越滚越大的结构性原因。

### 🔴 真正的瓶颈：共享层治疗缺陷

完整证据链见 [DEFECT-CAST-WHILE-MOVING.md](../../DEFECT-CAST-WHILE-MOVING.md)。摘要：

- `PlayerbotAI.cpp:3946`：bot 移动时，有读条的法术被直接取消返回失败，
  `bot->StopMoving()` 被注释掉了
- 单场 258 秒里，有读条的治疗 **11 次成功 / 101 次 FAILED**；强效治疗 **110 次尝试 0 次成功**
- 只有 37% 的 GCD 用在治疗上，魔杖甩了 23 次
- 4/5 场团灭是**坦克先倒**，治疗死在坦克之后 —— 治疗阵亡是结果不是起因

### 一并记下但未修的脚本缺陷

`boss_anubarak.cpp:297` 的 `if (!UpdateVictim()) return;` 排在 `events.Update(diff)`
之前：仇恨持有者死光后 `EVENT_EMERGE` 永不触发，boss 永久卡在水下直到脱战重置。
run451 seq5 就是这么废掉的（boss 21% 血、只死 1 人，被判
`boss lost combat state (stuck/reset)`）。不是击杀阻塞项，但会污染样本。

> **evidence 目录说明**：`*.log.gz`（各 run 的 `Playerbots.log` 整份归档）只留在运行机本地，不入管理库（`.gitignore`）；
> 入库的是地形探针表 `an-arena-floor-scan-2yd.txt`、验收脚本 `an_round_report.sh`、结果码统计 `prepare_fail_stats.py` 与 `castmove_stats.py`。
> 所有数字都能用文中 SQL 从本地 `acore_characters.raidtest_events` 复算。

## 第四轮（2026-09-13）：共享层「为施法让出走位型移动」修复上线，归因被证伪

改动与设计见 [DEFECT-CAST-WHILE-MOVING-PLAN.md](../../DEFECT-CAST-WHILE-MOVING-PLAN.md)。同一场景、同一装档、
`LogInGroupOnly = 0`，两个二进制各跑 5 场：

| run | 二进制 | 结果 | 场次时长 |
|---|---|---|---|
| **457** | 只改 `CastSpell`（`CanCastSpell` 里的移动闸仍在，让路路径实际不可达） | 1 击杀 / 3 团灭 / 1 超时 | 480 / 236 / 243 / 262 / 215 秒 |
| **459** | `CanCastSpell` 也改为「走位型移动不判 IMPOSSIBLE」 | 0 击杀 / 4 团灭 / 1 超时 | 225 / 232 / 146 / 480 / 263 秒 |

（run 458 全部 5 场在传送阶段被 `CANNOT_ENTER_TOO_MANY_INSTANCES` 拒绝，清 `account_instance_times` 后重跑即 459。）

### 逐动作统计（戒律牧师 `Raidtebnfivc`，5 场合计）

| 动作 | run 457 OK / FAILED / IMPOSSIBLE | run 459 OK / FAILED / IMPOSSIBLE |
|---|---|---|
| greater heal on party（2.5s） | 2 / 11 / 124 | 1 / 54 / 121 |
| flash heal on party（1.5s） | 1 / 9 / 134 | 7 / 24 / 163 |
| penance on party（引导） | 38 / 21 / 90 | 36 / 69 / 106 |
| power word: shield on party | 104 / 21 / 271 | 94 / 117 / 262 |
| renew on party | 65 / 16 / 170 | 71 / 71 / 170 |
| shoot | 123 / 0 / 272 | 102 / 0 / 277 |

**读条治疗被判 IMPOSSIBLE 的原因（run 457，强效+快速+苦修共 348 次 IMPOSSIBLE 中可归因的）：**
GCD 未转好（`SPELL_FAILED_NOT_READY`）173 次、苦修正在引导 66 次、苦修冷却 30 次、**「移动中」19 次**。
全队"读条时在移动"拦截 run 457 共 113 次、run 459 共 70 次，其中法师占 58 / 39 次；
逐条回溯上下文，run 459 的 70 次里 45 次发生在穿刺触发期间（被击飞或正在躲刺），其余是逃跑等战术移动。

**新让路路径（`cast-vs-move ... result=yield`）在 run 459 全程 1346 秒里 0 次触发**；`result=refuse` 2 次，
都是苦修在 `flee` 期间被正确拒绝。run 457 的 4 次 refuse 是苦修在被穿刺击飞（生成器 EFFECT）和逃跑时。

### 结论

1. **缺陷文档「92% 读条治疗被移动打掉」的归因不成立。** 读条治疗放不出来的主因是瞬发（盾/恢复/愈合祷言）
   先占 GCD、读条治疗被评估时撞上 GCD 或苦修引导——这是策略优先级与 GCD 争用，不是移动。
   "移动中"只占可归因拒绝的 ~7%，且几乎全是穿刺击飞/躲刺这类**不该让路**的场景。
2. 修复本身行为正确（拒绝只发生在保命/被击飞时，无一次误让路），但在这个 boss 上**没有可观测收益**；
   击杀率 1/5 → 0/5 在本场景的历史波动（0/5、2/2、1/5）之内，不构成回归证据。
3. **FAILED 的原因仍是黑箱**：run 459 牧师所有治疗（含瞬发）的 FAILED 都比 457 高 3–5 倍，而瞬发不经过任何移动闸，
   说明 FAILED 来自 `Spell::prepare` 的其它拒绝码（视线/距离/目标状态……），两轮之间的差异是环境波动而非改动。
   缺陷文档当初就是把 FAILED 全算成移动。**下一步先给 `CastSpell` 的 prepare 失败补上结果码日志**（现有注释掉的
   `LOG_DEBUG` 直接放开即可），再判断牧师真正的瓶颈。
4. 五场死亡顺序仍是 DPS 先倒（萨满/法师/盗贼），坦克、牧师最后；run 459 每场团灭集中在 10–20 秒内全灭，
   像是某个爆发窗口（潜地阶段小怪堆积 + 出土践踏）而不是慢性治疗不足。

证据文件：`Playerbots.log` 两轮整份副本各 8 MB，已从会话暂存目录归档到本目录 `evidence/`（见下）。

### run 460：放开 prepare 结果码日志后，FAILED 有了名字——**牧师没蓝**

只改一处 `.cpp`（`CastSpell` 两处 `prepare()` 失败时记录 `SpellCastResult`），5 场：0 击杀 / 2 团灭 / 3 超时
（480 / 227 / 239 / 480 / 480 秒）。全队 756 次 prepare 失败中 **578 次 `SPELL_FAILED_NO_POWER`**；
牧师 365 次 **全部** 是 `NO_POWER`（盾 122、恢复 47、苦修 35、强效治疗 31、快速治疗 13、治疗祷言 30……）。
法师 241 次里大部分也是没蓝。

牧师蓝量曲线（`raidtest_events` 的 `resource:` 行，每 30 秒取最小值；满蓝 16,708）：

| 场次 | 0s | 30s | 60s | 90s | 120s | 150s | 180s | 210s | 结局 |
|---|---|---|---|---|---|---|---|---|---|
| 457/2 团灭 | 12786 | 8133 | 6545 | 11612 | 6799 | 2143 | 794 | 20 | 236 秒全灭 |
| 457/4 **击杀** | 11308 | 8655 | 8707 | 7815 | 5412 | 11393 | 8123 | 4867 | 240 秒剩 1539，262 秒 boss 倒 |
| 459/1 团灭 | 15448 | 9006 | 6229 | 10569 | 6473 | 1865 | 125 | 0 | 225 秒全灭 |
| 459/4 超时 | 13338 | 8612 | 7263 | 4951 | 2334 | 1032 | 0 | 0 | 之后 300 秒一直 0 |
| 460/2 团灭 | 15554 | 8408 | 5805 | 7069 | 1952 | 2548 | 32 | 0 | 227 秒全灭 |
| 460/3 团灭 | 12983 | 6047 | 9356 | 5684 | 1925 | 788 | 21 | 0 | 239 秒全灭 |
| 460/4 超时 | 10368 | 3796 | 5393 | 6402 | **0** | 0 | 0 | 0 | 130–152 秒 4 人倒 |

三轮 15 场里所有 200 秒以上的团灭，牧师都在 150–210 秒见底，随后 20–40 秒全队倒；唯一的击杀是蓝刚好撑到 boss 死。
另一类输法（457/1、460/1、460/4、460/5）是 74–130 秒 DPS 先被小怪打死、boss 潜地后停滞到超时，
与蓝量无关，属于小怪爆发窗口。

**耗蓝结构（`spell` 事件，460/2 一场 227 秒）**：真魂盾 26 次（约 23% 基础法力/次 ≈ 890，合计约 23k）、
恢复 14、愈合祷言 14、苦修 10、渐隐 5；估算总耗蓝约 47k，对 16.7k 蓝池 + 战斗回蓝 + 一次暗影魔 + 一次希望圣歌，
在 180 秒左右耗尽是必然的。策略表里 `party member almost full health` 就推真魂盾（`HealPriestStrategy.cpp:87-89`），
盾几乎每 8 秒一个，是最大的蓝坑。

**回蓝手段的真实状态**（推翻缺陷文档"不是蓝量"的排除项——那条把 IMPOSSIBLE 读成了"没触发"）：
暗影魔每场用了 1 次（其余 IMPOSSIBLE 是冷却）、希望圣歌每场 1 次、`mana potion` 187 次 IMPOSSIBLE 是因为
**包里没有法力药水**（牧师槽 `Supplies = 44615,33443,33445`：蜡烛、奶酪、蜂蜜薄荷茶），`drink` 战斗中不可用。
`save mana` 策略在 mod-playerbots 里的两个乘子（`ConserveManaStrategy.cpp`）**整段被注释掉**，等于没有。

证据：`evidence/playerbots-run460.log.gz`、`evidence/prepare_fail_stats.py`（结果码→名字聚合）。

## 第五轮（2026-09-13 上午）：加法力药水 + 盾退出 almost full 档 → **2 击杀（零死亡）/ 2 团灭 / 1 超时**（run 461）

两处改动（用户拍板）：

1. 夹具 `mod-raidtest-roster-normal5-v1.conf` 牧师槽 `Supplies` 加 **33448 符文法力药水**（基线改动，已记入
   [夹具 README](../../fixtures/normal5-v1/README.md)）。`mana potion` 动作认任何 `SPELL_EFFECT_ENERGIZE` 药水，一场一瓶。
2. `HealPriestStrategy.cpp` 的 `party member almost full health` 节点去掉 `power word: shield on party`
   （盾留在 medium / low / critical 三档）。单文件编译。

| seq | 结果 | 时长 | 牧师蓝 150s / 180s / 210s / 240s | 药水 | 盾 / 恢复 / 祷言 / 苦修 |
|---|---|---|---|---|---|
| 1 | 团灭 | 263 s | 6087 / 7709 / 2855 / **0** | 69 s | 20 / 20 / 16 / 7 |
| 2 | 团灭 | 252 s | 15607 / 8936 / 3176 / **0** | 1 | 20 / 20 / 18 / 11 |
| 3 | **击杀 0 死** | 253 s | 13666 / 9423 / 2905 / 1804 | 1 | 26 / 23 / 16 / 8 |
| 4 | 超时 | 480 s | 6574 / 9274 / 2180 / **0** | 1 | 26 / 20 / 17 / 11 |
| 5 | **击杀 0 死** | 255 s | 13792 / 13068 / 6623 / 3633 | 1 | 23 / 17 / 17 / 10 |

- 牧师蓝见底从 150–210 秒推迟到 **约 240 秒**；两场击杀都在 253–255 秒、240 秒时还剩 1.8k–3.6k。边际很薄：
  boss 能不能在牧师见底前倒下，就是击杀与团灭的分水岭。
- 牧师本轮 prepare 失败 **0 次**（上一轮 365 次 `NO_POWER`）。
- 盾没有明显减少（20–26 次，含给自己 5–7 次），省下的 GCD 被恢复（14→17–23）和愈合祷言（14→16–18）接管，
  这两个还在 almost full 档；估算总耗蓝只从约 47k 降到约 44k。真正多出来的续航主要来自药水的 4.3k。
- 对照：run 445–460 共 20 场 2 击杀（均有死亡），本轮 5 场 2 击杀均零死亡。仍在单轮波动范围内，**需要再跑一轮确认**。

### 下一个杠杆：法师 90–120 秒就没蓝

run 461 全队 1251 次 prepare 失败，法师 861 次，活体炸弹一项 292 次 `NO_POWER`。法师蓝量曲线每场都在
90–120 秒归零，之后整场基本靠魔杖：

| seq | 30s | 60s | 90s | 120s | 150s | 180s |
|---|---|---|---|---|---|---|
| 1 | 5797 | 3365 | 1513 | 1821 | 270 | 177 |
| 2 | 6090 | 2882 | 21 | 0 | 0 | 0 |
| 5 | 8551 | 2778 | 2005 | 3940 | 3747 | 13 |

每场火球只放 7–14 个、活体炸弹 9–22 个（`living bomb on attacker` 往每只小怪上铺，每个约 22% 基础法力）、
唤醒 1 次、法力宝石 1–2 次；`mana potion` 555 次 IMPOSSIBLE 同样是**包里没药水**（法师槽 `Supplies = 17020,17031,33443,33445`）。
DPS 掉线 → 战斗拖到 250 秒 → 牧师见底，两件事是同一条因果链。候选：法师槽也加 33448；`living bomb on attacker`
限制铺的目标数或在低蓝时停铺。

证据：`evidence/playerbots-run461.log.gz`。

## 第六轮（2026-09-13 上午）：法师止血 → run 462 两场 1 击杀零死亡；顺带揪出两个真正的团灭引爆点

改动（用户拍板）：法师槽 `Supplies` 加 33448；AN 层新增 `AnubarakMageManaMultiplier`（阿努巴拉克在场时把法师的
`blizzard` / `flamestrike` / `living bomb on attackers` 相关性归零，其余副本不受影响）。两场：

| seq | 结果 | 时长 | 法师蓝 90s/150s/240s | 法师暴风雪/烈焰风暴/活体炸弹 | 法师 DPS | 牧师蓝最低 |
|---|---|---|---|---|---|---|
| 1 | 超时（**牧师 133 秒掉出平台**） | 480 s | 5133 / 4484 / — | 0 / 0 / 22 | — | 7405（没花完，人不在场上） |
| 2 | **击杀 0 死** | 254 s | 5410 / 4064 / 2787 | 0 / 0 / 34 | **1243**（此前 1040–1190） | 4169 |

法师不再 90–120 秒归零，但活体炸弹仍有 22–34 次：那是 `FireMageStrategy` 的单体 `living bomb` 触发（当前目标换到哪只小怪就
给哪只上），不是被归零的 `living bomb on attackers`。要再省蓝得把单体炸弹限定在 boss 上（Multiplier 里按目标 entry 判）。

### 新发现 A：**活着掉出平台**（27 场里 6 人次，五次超时里四次是它引起的）

`state` 事件 `pos:` 行按"z<200 早于死亡时刻"筛：

| 场次 | 谁 | 掉落时刻 | 掉落前最后落点 (x, y) |
|---|---|---|---|
| 457/1 | 牧师 | 119 s | (526.2, 248.1) |
| 460/1 | 牧师 | 98 s | (526.4, 247.8) |
| 460/4 | 法师 | 90 s | (532.6, 238.5) |
| 460/5 | 牧师 | 58 s | (525.2, 253.1) |
| 462/1 | 牧师 | 138 s | (526.0, 259.3) |
| 462/1 | 法师 | 139 s | (526.4, 261.3) |

六次全部在 **x ≈ 525–533 的西沿**（开怪点 x=543，场地地面 z≈223.4），掉下去的人满血、脱战、最后停在 z=0，
之后整场不再参与。462/1 的牧师从 133 秒起坐标 x/y 不变、z 匀加速下坠——是被击飞后落在平台外，不是走下去的。
远程站位被阵型/逃跑推到西沿，再被穿刺击飞（或践踏击退）一下就出界。**这是超时场次的主因**，
此前一直被记成"DPS 先倒 → 潜地停滞"。

### 新发现 B：**践踏一击秒杀非坦克**（27 场里 7 人次，全部当场死亡）

boss 对玩家单击 ≥15k 的记录：坦克每 65–85 秒吃一次 15–17k（22 s、88 s、172 s……）都扛得住；
落在萨满/盗贼身上就是 21k–33k 直接死：457/2（89 s 萨满）、457/3（92 s 萨满+盗贼）、457/4（242 s 萨满）、
459/1（174 s 萨满+盗贼）、459/5（92 s 萨满）、462/1（172 s 萨满）。七次里六次受害者在前 1–3 秒**正在读条或打技能**
（闪电链、闪电箭、连击），没有躲。原因有两层：`kPoundConeRadius = 15` 而萨满站在 15 码外的锥内，躲踏动作根本不判 useful；
即便判了，读条期间引擎不评估触发器（计划文档 §5 的二阶效应）。这就是很多团灭"十几秒内全灭"的第一张多米诺。

证据：`evidence/playerbots-run462.log.gz`；掉落与践踏两组数字均可用文中 SQL 从 `raidtest_events` 复算。

## 第七轮（2026-09-13 中午）：践踏锥角/半径修正 + 走廊怪移除 + 三项站位实验（全部回退）→ run 469 **2 击杀 / 2 团灭 / 1 复位**

### 保留下来的改动（AN 层 + 场景）

1. **践踏锥角 24° → 120°**：`spell_cone` 表把 53472/59433 覆盖成 120°（Spell.cpp 默认 24° 只在无覆盖时生效）。
   旧 ±12° 让躲踏对站在锥里的人一律判 USELESS。
2. **践踏距离按 core 口径**：`WorldObjectSpellAreaTargetCheck` 用 `IsWithinDist3d(pos, 15)` = 三维距离 < 15 + 目标 `GetObjectSize()`；
   旧判定二维精确 ≤15，站在 15–17 码的人不躲却挨打（run 467/1 牧师 22 秒被 27k 秒杀时正是如此）。
3. **躲踏几何按 120° 重算**：远程径向退到 18 码（候选角度每 15° 交替旋转，逐个用 vmap 地面校验），近战绕到 boss 背后 5 码；
   直线 spline 不经 mmap（见下文 mmap 教训）。run 469 躲踏终于执行了 5 次（此前所有轮次为 0）。
4. **场景夹具 `FixtureDespawnSpawns = 132274,132275`**：走廊尽头两只 Anub'ar Prime Guard（29128，(558,341)/(542,341) z 241）。
   boss 潜地时坦克没目标就去追它们，全队跟进北面坡道被打死（464/2、465/1 死于此；457–462 也有 13 人次活着跑到 y>280）。
5. 法师单体活体炸弹限定 boss（Multiplier 按目标 entry）。

### 三项站位实验，六轮（run 463–468）每一轮都比 461/462 差，已全部回退（代码保留、触发器未接入）

| 实验 | 意图 | 实际后果 |
|---|---|---|
| 西沿护栏（x<533 拉回 536） | 防掉出平台 | 开怪时把全队从准备点 527 拉到 536，牧师离 boss 10 码成为践踏随机目标，22 秒被 27k 秒杀（467/1） |
| 远程保距（<16 码退到 18） | 远程站到践踏射程外 | 开怪前就把牧师挪走，它没放过治疗、不进战斗状态，整场留在非战斗引擎，坦克 42 秒倒（466/1）；目标点经 mmap 寻路被吸到平台下层/上层（463：三远程开场 12–22 秒沿 spline 匀速走进深渊；464：为到 6 码外的点绕行 80 码跑上北坡道） |
| 准备点南移 (543,230) | 远程开场就在 20 码外 | 离南沿只有 7 码，牧师从东南沿掉出、法师站到南沿外 z=219，两场 54/80 秒全灭（468） |

**三条教训**（已写进记忆）：
- `state` 事件里 bot"站着"的 (x,y,z) 不能证明脚下有地面：服务端 bot 没有重力，spline 终点的 z 是导航网格给的。
  x≈525–533 那 4530 个"站位"样本全是悬空的。看地面要用 `raidtest los <map> x y z x y z`（两点同坐标）读 `vmap_floor_from`。
- 场地内短距离移动别走 `MoveTo`/`CheckCollisionAndGetValidCoords`：这一带 mmap 会把终点吸附到别的层。
  `MotionMaster::MovePoint(..., generatePath=false)` + vmap 地面校验（|Δz|≤3）才安全。
- 平台是半径约 27 码的近圆（中心约 (553,250)，西南角被削），boss 出生在 (551,248) 偏西，仇恨半径约 24 码：
  **不存在既在仇恨半径外、又离边沿 ≥8 码的准备点**。远程要么开场站边沿（现状），要么开怪后再进场——这是下一轮站位设计的约束。
  地形图：`evidence/an-arena-floor-scan-2yd.txt`。

### run 469 数据

| seq | 结果 | 时长 | 死亡 | 备注 |
|---|---|---|---|---|
| 1 | 团灭 | 233 s | 5（220–240 s 全部死于守卫/毒疗者） | 牧师 180 s 还有 13.2k 蓝：**不是蓝，是潜地期小怪堆积** |
| 2 | **击杀** | 261 s | 1（法师 235 s） | |
| 3 | **击杀** | 297 s | 2（萨满 181 s 被践踏 18k、坦克 291 s 被践踏 29.5k） | 法师 52 秒**活着掉出西沿**，整场 DPS 233 |
| 4 | 作废 | 99 s | 0 | `boss lost combat state`（boss 脚本 `UpdateVictim` 缺陷，见上文） |
| 5 | 团灭 | 211 s | 5（206–218 s 全部死于守卫/毒疗者） | |

与 461+462（7 场 3 击杀零死亡）同一水平：有效 4 场 2 击杀。践踏只在 seq3 打到两次非坦克（此前 27 场 7 人次）。

### 剩下的三个问题，按影响

1. **潜地期小怪堆积**：两场团灭都是 206–240 秒全队被守卫/毒疗者打死，牧师蓝还够。法师群攻归零后小怪死得慢了
   （run 469 每场击杀小怪数待统计），需要"boss 潜地时集火小怪"的目标选择——但本副本的目标选择/仇恨已被证伪四次，要先量再动。
2. **西沿掉落**仍在（469/3 法师 52 秒）：站位设计要在上面三条约束下做，先把阵型移动（`combat formation move`）在本副本禁掉
   （卡拉赞/祖阿曼有先例 Multiplier），再考虑开怪后进场。
3. **践踏**：躲踏已能执行，但坦克 291 秒被 29.5k 一击秒杀（坦克 21k 血）——那是叠了 debuff 还是暴击，待查。

证据：`evidence/playerbots-run464.log.gz`（mmap 绕路）、`evidence/playerbots-run469.log.gz`、`evidence/an_round_report.sh`（本轮验收脚本）。

### 小怪击杀曲线：法师群攻归零**没有**拖慢清小怪（2026-09-13 下午量化）

事件表不记小怪死亡，用代理：每 30 秒桶内出现过伤害事件的小怪 guid 数（"活跃小怪"），以及每场小怪总数与
"最后一次事件早于场次结束 20 秒以上"的数量（≈被清掉）。对比 run 461（法师有暴风雪/烈焰风暴/铺炸弹）与 run 469（全部归零）：

| 场次 | 结果 | 时长 | 活跃小怪 @60s / 150s / 210s | 小怪总数 | 清掉 |
|---|---|---|---|---|---|
| 461/1 | 团灭 | 263 s | 7 / 5 / 11 | 70 | 50 |
| 461/3 | 击杀 | 253 s | 12 / 11 / 9 | 69 | 56 |
| 462/2 | 击杀 | 254 s | 8 / 8 / 9 | 63 | 54 |
| 469/1 | 团灭 | 233 s | 8 / 12 / 13 | 57 | 44 |
| 469/2 | 击杀 | 261 s | 10 / 9 / 10 | 67 | 57 |
| 469/3 | 击杀 | 297 s | 14 / 13 / 6 | 74 | 63 |
| 469/5 | 团灭 | 211 s | 12 / 10 / 8 | 57 | 44 |

两轮曲线同形、清掉比例同为 75–85%。全队对小怪的输出：461 共 3.65M（法师 738k），469 共 2.97M（法师 608k，其中一场法师 52 秒掉出平台）；
对 boss：461 共 2.23M，469 共 1.96M。**全队一半以上的输出打在小怪上**，每场约 60 只（飞镖手 ~15、刺客 ~9、守卫 ~4、毒疗者 ~4）。

结论：不恢复法师群攻。团灭窗口是 boss 约 20% 时的第三次潜地：击杀场 boss 在 254–302 秒倒下，团灭场 206–240 秒全队被守卫/毒疗者打死，
分水岭是 boss 能否在第三波小怪堆满前死掉——这是 DPS 检查。法师在 150–180 秒仍只剩 1.6k–2k 蓝（469/1、469/5），
火球停摆，是当前最弱的一环（击杀场法师 DPS 917 vs 萨满 1870）。

## 第八轮（2026-09-13 下午）：法师蓝坑真相 + 共享层同层守卫 → run 470 / 471

### 法师唤醒/法力宝石/药水的时机都合理，真蓝坑是活体炸弹铺小怪从没被压住

run 469 数据：法力宝石在 64% 蓝、冷却好时用（约 3.4k）；药水在 40–45% 用（4.3k）；唤醒在 15%（`LowMana`）用；时机没有问题，
总预算（蓝池 13.9k + 宝石 ×2 + 药水 + 唤醒 ≈ 33k / 230 秒）是结构性上限。**但 run 469/2 法师 32 次活体炸弹里 24 次打在小怪上
（约 17k 蓝，占全部消耗四成）**：第六轮的 `AnubarakMageManaMultiplier` 匹配的是策略节点名 `living bomb on attackers`（复数），
而动作实例名由 `CastDebuffSpellOnAttackerAction` 拼成 `living bomb on attacker`（单数），一次都没压住。
修正后 run 470：Multiplier 命中 29 次，炸弹只在 boss 上放 4 次；击杀场（470/2）法师全程蓝 ≥5.5k、**DPS 1341**（此前最高 1243）。

### 掉出平台的真机制：`flee` 后退走进深渊，与击退无关

八次活着掉落的轨迹完全一致：x/y 几乎不动、z 以 **4.5 码/秒匀速**下降（= 后退跑速度），掉落前无任何承伤，两例前 1–5 秒有 `flee - OK`。
远程在西沿被小怪贴身 → `FleeAction` 后退 5 码 → `CheckCollisionAndGetValidCoords` 的 Detour 射线把落点 z 吸到平台下方地形（z≈0）
→ 直线 spline 倒着走下去。这也解释了第七轮"站位实验"里的匀速下坠（同一机制，另一条触发路径）。

**共享层改动**：`MovementAction::IsSameFloorDestination`（`MoveTo` 两个分支都调用）：二维距离 <30 码而 |Δz| > max(6, 0.8×dist2d) 的目标
视为另一层地面，拒绝并记 `floor-guard` 日志。详见 [修改计划 §12](../../DEFECT-CAST-WHILE-MOVING-PLAN.md)。

| run | 二进制 | 结果 | 活着掉落 | 守卫拦截 |
|---|---|---|---|---|
| 470 | 炸弹名字修正 | 1 击杀零死亡 / 2 团灭 / 1 超时（法师 109 秒掉落）/ 1 场背包满夹具失败 | 1 | — |
| 471 | + 同层守卫 | 1 击杀零死亡 / 2 团灭 / 2 超时（其一 boss 打到 **4%**） | **0** | **22 次**，全是牧师/远程的 `flee` 落点 (x≈520–524, z=0) |

### 顺带记下

- **坦克被践踏一击**：run 457–471 坦克共吃 93 次践踏，14–18k 为主，≥21k 只有 3 次（2 次致死：469/3 29.5k、471/2 22k）。
  坦克满血 20,974。频率 3%，暂不处理；防骑的 `divine protection` 在日志里 12 次 IMPOSSIBLE，若要做就是"boss 读条践踏时开减伤"。
- **盗贼被践踏秒杀 4 次（457/3、459/1、466/1、471/1）时躲踏均判 USELESS**：盗贼在 boss "背后"，`HasInArc` 用服务端朝向，
  而 boss 读条时会朝随机目标转（FocusTarget），朝向读数与真实锥不一致。已改为非坦克只看距离（≤15 码+体型半径就退到 18 码），
  代价是每约 75 秒损失 3 秒近战输出。run 472 验证。
- **夹具已知限制**：bot 没有背包、只有 16 格主包，拾取的装备/布料会在约 20 场后塞满，`Supplies` 补给 `AddItem` 失败 →
  `fixture_invalid: missing supply item`（470/5）。修法：roster 给背包，或 `RestoreRoster` 清掉非补给物品。

证据：`evidence/playerbots-run470.log.gz`、`evidence/playerbots-run471.log.gz`。

## 第九轮（2026-09-13 下午）：躲踏按角色分档 + 远程保距重新接入 → run 472 / 473

| run | 改动 | 结果 | 活着掉落 | 非坦克被践踏秒杀 |
|---|---|---|---|---|
| 472 | 所有非坦克只看距离（≤15 码+半径）就退 18 码 | **0 击杀** / 4 团灭 / 1 超时 | 0 | 萨满 24k（472/4，21 秒，在准备点没保距） |
| 473 | 治疗只在 10 码内躲；DPS 只看距离；远程 DPS 保距重新接入（战斗中、直线、TACTICAL） | **2 击杀** / 3 团灭 | 0 | 牧师 32.6k（s1 260 秒，boss 6 秒后倒）、牧师 32.2k（s5 187 秒） |

run 472 的教训：治疗在坦克吃 16–20k 践踏的同一刻跑位（472/2、472/4 那两记践踏前后 0 治疗），坦克倒下 → 全灭。
run 473：远程保距生效（萨满/法师 16–28 码占大部分采样，`keep range` 25 次 OK、5 次无候选点），盗贼每次践踏都退（46 次），
掉落 0；法师击杀场 DPS 1380。三场团灭：473/3 坦克 188 秒被 **31.6k** 践踏一击（此前坦克 93 次践踏只 3 次 ≥21k，现在第三个践踏周期
频繁出现 30k+，待查是否有 debuff 叠加）；473/5 牧师 187 秒被 32.2k 一击（站在 10–15 码之间，"仅 10 码内躲"留的缝）；
473/4 法师 149 秒被小怪打死后第三次潜地全灭。

**当前基线 = run 473 二进制（15:00）+ 场景/夹具**：与 461/462、469、470、471 同一水平（每 5 场 1–2 击杀）。

证据：`evidence/playerbots-run472.log.gz`、`evidence/playerbots-run473.log.gz`。

### 30k+ 践踏的成因：守卫的破甲叠层，不是暴击（2026-09-13 傍晚，纯查库）

践踏伤害法术 59432 原始值 **47,124** 物理。坦克 109 次正常命中均值 15,977（12.2k–19.5k）= 约 66% 护甲减免；
三次大额（31.6k / 29.5k / 22k）分别对应**同一窗口 boss 普攻也翻 2–4 倍**（2.9k–5.1k vs 全场均值 1.1k–1.5k），
且坦克身上有 Anub'ar Guardian（29216）的 **59350 Sunder Armor ×5 / ×5 / ×2**。DBC：59350 = `SPELL_AURA_MOD_RESISTANCE`
**−2000 护甲/层，最多 20 层，持续 30 秒**。5 层 = −10k，把 ilvl 187 防骑约 21k 的护甲砍半，减免从 66% 掉到约 45%。
暴击假说不成立：坦克防御 ≈ 532 + 附魔宝石 ≈ 543（夹具 `MinDefenseSkill = 535`），暴击率 <0.5%。
牧师那两次 30k+ 是布甲对 47k 原始值的正常结果（布甲减免约 30%）。

推论：第三次潜地后守卫多、坦克破甲层数高 → boss 普攻与践踏同时翻倍 → 这才是 187 秒后坦克"莫名"暴毙的机制，
也是小怪压力在后期变致命的物理原因。

候选（都是 bot 行为层）：
1. **守卫优先**：DPS 在守卫在场时先打守卫（它同时是缩短潜地的"大型召唤物"）。本副本目标选择已被证伪四次，
   但那四次是"全队转火所有小怪"；这次是"只把守卫排在毒疗者/刺客/飞镖手之前"，要单独设计和量。
2. **坦克开减伤**：boss 读条践踏且破甲 ≥3 层时开圣佑术（498，−50%，已在技能表；日志里 24 次 IMPOSSIBLE 是被控中、6 次是冷却）。
3. **治疗预盾**：`anub'arak pound` 触发时给坦克上真魂盾并接大治疗（践踏读条 3.2 秒足够）。
4. 30 秒内没被守卫打就掉层：坦克把守卫交给盗贼/萨满控制不现实，暂不考虑。

## 第十轮（2026-09-13 晚）：践踏读条期的坦克圣佑 + 治疗预盾 → run 477 机制成立、击杀 0/5

改动（AN 层）：`anub'arak pound tank` 触发器——坦克、boss 读条践踏、身上 Sunder Armor ≥3 层 → `divine protection`（−50%，ACTION_EMERGENCY）；
`anub'arak pound healer` 触发器——治疗、boss 读条践踏、主坦在 40 码内 → 先 `anub'arak pound shield tank`（真魂盾→主坦）再
`anub'arak pound heal tank`（苦修→主坦），相关性 39/38 压过普通治疗。两个动作以 `main tank` 值为目标。

| 指标 | run 473 | run 477 |
|---|---|---|
| 坦克吃到的践踏均值 / 最大 | 16,906 / **31,602** | **9,171 / 11,386** |
| 全队 ≥15k 命中 | 5 次（坦克 2、牧师 2、萨满 1） | **1 次**（牧师 94 秒 30k，站在 10–15 码） |
| 预盾 / 接治疗 / 圣佑 执行 | — | 12 / 13 / 4 次，每次践踏都在落地前 2–3 秒完成 |
| 结果 | 2 击杀 / 3 团灭 | **0 击杀** / 3 团灭 / 2 超时（全灭后 boss 卡潜地） |

践踏对坦克的威胁已经解除，但击杀率反而是 0/5——五场的第一个死者：牧师 254 秒（小怪）、法师 207 秒（小怪）、牧师 94 秒（践踏，10–15 码缝）、
牧师 128 秒（飞镖手，蓝还有 10k）、萨满 223 秒（小怪）。**治疗被小怪盯死**成了新的头号引爆点；击杀数 0–2/5 的轮间波动也说明
本 boss 现在处在"每项机制都勉强顶住、随机哪一项先崩"的状态。

证据：`evidence/playerbots-run477.log.gz`（本地）。

## 第十一轮（2026-09-13 晚，纯查库）：治疗不是被小怪"盯死"，是被 `reach spell` 引到小怪刷新坡道上单挑

先量仇恨接手（run 469/470/471/473/477 有效 23 场，`raidtest_events`）：

| 打过牧师的小怪 | 只数 | 对牧师总伤 | 之后转打坦克 | 平均多久转走 |
|---|---|---|---|---|
| 腐尸甲虫 29209 | 104 | 16k | 30%（大多先死） | 3.8 秒 |
| 刺客 29214 | 71 | 13k | 65% | 4.1 秒 |
| 飞镖手 29213 | 30 | 50k | 37% | 6.0 秒 |
| 毒疗者 29217 | 67 | **368k** | **96%** | 4.8 秒 |
| 守卫 29216 | 23 | **195k** | 78% | 7.5 秒 |

坦克接手是正常的：真正打疼牧师的毒疗者/守卫打上牧师后平均 5–8 秒就转到坦克身上，run 477 里 `hand of reckoning` 59 次、`righteous defense` 43 次 OK。
守卫在 **6 场击杀里对牧师 0 次命中**，在团灭/超时里 196k——差别不在仇恨，在**位置**。

真机制（`pos` 状态行 + `heal_actions`）：每一场团灭里牧师死前约 30 秒都有一次**以跑速直线北上**，从 y≈235 跑到 y≈300–329、
z 224→236–240（12 秒 80 码），然后站定、死亡。六场击杀里牧师死前北上 **0 次**，团灭/超时 17 场里 **7 场**有（469/1、469/5、470/1、470/3、471/3、477/2、477/4；死后的北上是尸体/灵魂移动，不算）。
7 次北上前后的 OK 动作全是 `drop target`/`dps assist`/`reach spell`/`set facing` 这一组，触发器带 `enemy out of spell range`、`invalid target`：

- 小怪刷新点：守卫/毒疗者从 `creature_summon_groups` 组 1 的世界触发器 **(550,352,241)** 召唤，`smart_scripts` 事件 54 让它们走到 (551,274,224)
  ——即从北坡道顶端走 80 码进平台。飞镖手/刺客从组 2 的东西两侧高台 (478/621, 225–298, 251) 跳下。
- 牧师 `dps assist` 的 `dps target` 来自 `AttackersValue`：全队成员仇恨列表里、距任一成员 `SightDistance`=100 码内的都算，
  刚刷出的毒疗者（已对全队产生仇恨）100 码外也入选；`CombatStrategy` 里 `enemy out of spell` → `reach spell`（ACTION_HIGH=20）没有距离上限，
  牧师就朝刷新点跑，到距目标 30 码（y≈320–329，与刷新点 352 差 23–32 码）停下——正是死亡位置。
- 然后守卫/毒疗者走下来，坦克在 40–78 码外（嘲讽 30 码够不到），牧师独自挨 4k 一刀的守卫（2–9 刀）和 1.7k 一发的毒箭齐射。
  死亡前 15 秒伤害：469/1 毒疗者 24k；469/5 守卫 12k+毒疗者 10k；470/3 守卫 30k；471/3 毒疗者 32k；477/2 毒疗者 20k；477/4 守卫 43k。
- 死前另外两类：践踏 30–32k 秒杀满血牧师（473/1、473/5、477/3，站在 10–15 码缝）；死时蓝 <100（471/1、473/4、477/1）。

坦克/近战也有北上，但除 470/3 盗贼 44 秒外全在团灭之后（boss 无伤害行）。`IsMovingAllowed(target)` 里原有的 `ReactDistance` 距离守卫被上游注释掉了，
`ReachTargetAction::isUseful` 只判"不在范围内"。结论：**改治疗自保（不要离队去追目标），不用改仇恨接管。**

候选改法（都没编译）：
1. 共享层、按角色：`ReachTargetAction` 加"追敌"变体（`reach spell`/`reach melee`）的离队上限——治疗在组队战斗中，目标点若离主坦超过治疗范围（40 码）
   则 `isUseful=false`；治疗/复活的 reach 变体不受影响。推荐：修的是"治疗离开治疗范围"这一通用错误，与 `MovementIntent` 同一层设计。
2. 值层：`DpsTargetValue` 对治疗排除超出自身法术范围的目标，`dps assist` 就不会选它。副作用是治疗切目标更频繁，覆盖不了 rti/tagged 来源。
3. AN 层：Multiplier 对治疗归零 `reach spell`。最便宜，但只治本 boss。

脚本：`evidence/healer_lure_stats.py 469 470 471 473 477`。

## 第十二轮（2026-09-13 深夜）：共享层"追敌不离主坦"→ run 478（只拦治疗，1/5）/ run 479（拦所有非坦克，1/5），北上归零

改动（共享层，mod-playerbots `4b02c3ee`）：`ReachTargetAction` 新增 `ChasesEnemy()`（`reach spell`/`reach melee` 为真，去治疗/复活的 reach 为假）；
`IsChaseLeashed()`——组队战斗中的非坦克，若追击目标离 `main tank` 超过 `HealDistance`（38.5 码），追敌 reach 判无用，`LOG_DEBUG reach-leash` 记一行。
坦克豁免。第一版（run 478 二进制）只拦治疗、按落点算距离；478/1 证明不够：法师 61 秒先被 `reach spell` 引上坡道，牧师 168 秒用
`reach party member to heal`（按设计不拦）追去治他，到 (555,301) 后被毒疗者 17k 打死——所以第二版改为拦所有非坦克、按目标离主坦的距离算。

| 指标 | run 473/477（改前） | run 478（拦治疗） | run 479（拦非坦克） |
|---|---|---|---|
| 牧师/法师死前北上 | 团灭/超时 17 场 7 场牧师 + 法师 2 | 牧师 1、法师 1、盗贼 1（同一场） | **0 / 0**（盗贼 1 次在死后） |
| `reach-leash` 拒绝 | — | 0（落点都在 38.5 内） | 12 次（盗贼 10、牧师 2），离主坦 **76–90 码**，全是刷新点方向，平台内 0 误拦 |
| 守卫 / 毒疗者 对牧师总伤 | 49k/56k、59k/97k | 8k/43k | **4k**/125k |
| 结果 | 2/5、0/5 | 1 击杀 / 4 团灭 | 1 击杀 / 4 团灭 |

引出平台这条死路已封（`reach spell` OK 34、USELESS 12；`reach melee` OK 218、USELESS 41；同层守卫 0、让路 3）。击杀率没动，因为团灭换了原因：

- **践踏秒杀非坦克**：478/2 牧师 16.3 码 35k（盗贼同刀 36k）、478/3 牧师 12.9 码 30.6k、478/5 牧师 13.2 码 28k、479/1 盗贼 11 码 29k、479/5 牧师 14.5 码 30.7k。
  两轮 8 场团灭里 5 场首死是这个；≥15k 命中非坦克 478 七次、479 三次。牧师躲踏只在 10 码内（run 472 教训），10–15 码缝就是死缝。
- **末段小怪压坦克**：479/3、479/4 坦克 245/256 秒首死，15 秒内守卫 30k/9.6k + 毒疗者 20k/8.9k（+boss 6k），boss 分别剩 21%/18%。
- 毒疗者对牧师伤害反升到 125k：没人再追它们上坡道，它们走到 (551,274) 后站着放毒箭齐射，寿命变长；是否要"守卫/毒疗者优先"仍是第 8 项。

附带：`LogInGroupOnly=1` 会同时关掉 `an-move`/`cast-vs-move` 和入库的 `heal_actions`/`tank_actions`（477 场 2035 行 → 478 场 2 行），已改回 0；
`restart_world.sh` 的 `set -u` 未定义变量 bug（杀完旧进程不启新进程）已修。证据：`evidence/playerbots-run478.log.gz`、`playerbots-run479.log.gz`（本地）。

## 第十三轮（2026-09-14 上午）：治疗接入保距（17→20 码）→ run 483 **4 击杀 / 1 团灭**

改动（AN 层，mod-playerbots `f54c55f5`）：`anub'arak ranged too close` 按角色取阈值——远程 DPS 14 码不变，治疗 17 码（都加体型）；
`anub'arak keep range` 退到 18 / 20 码。其余不变：战斗中、boss 可选中、TACTICAL、直线 no-path、地面校验、已在退不重发。

| 指标 | run 477 | run 478 | run 479 | **run 483** |
|---|---|---|---|---|
| ≥15k 践踏命中非坦克（其中牧师） | 1（1） | 4（3） | 2（1） | **2（0）**，两次都是盗贼 10.7 码 |
| 牧师吃到的最大践踏 | 30k | 35k | 30.7k | **<8k（一次都没有）** |
| 牧师治疗次数 / 场 | 127 | 136 | 158 | **177** |
| 牧师读条被打断 / 场 | 18.6 | 20.2 | 24.6 | 23.0（保距移动没有多打断） |
| 治疗保距执行 | — | — | — | 88 次：ok 41、no_candidate 47（见下） |
| 结果 | 0/5 | 1/5 | 1/5 | **4/5**（276/1 死、274、249、271 零死亡；团灭 253 s boss 20%） |

牧师 10–16 码的践踏缝封住了：整轮牧师没有被践踏打到过。击杀率第一次跳出 0–2/5 的区间，但按约定单轮不下结论，要再跑一轮确认。

团灭 483/5 与 483/1 的唯一死亡同形：**盗贼在 10.7 码被 31–32k 践踏秒杀**（187 秒首死 → 236 秒牧师 → 全灭，boss 剩 20%）。
近战躲踏是"绕到 boss 背后 5 码"，boss 读条期锁方向，理论安全；两次都没躲成，是下一个要量的缝（`an-move dodge` 本轮 50 次 ok）。

顺带三条日志：
- `floor-guard` 6 次，全是牧师 `flee` 想往东北沿 (574,272) 退、落点 z=0——共享层同层守卫又拦下 6 次掉落。
- `reach-leash` 1 次：牧师追 41 码外的刺客被拦，正常。
- `keep range no_candidate` 47 次全在开怪前 20 秒：牧师在准备点 (527,248)，boss 被坦克拉到 (539,248) 西沿，20 码候选点全落在 `kArenaGuardX=536` 外或
  `kArenaSafeRadius=22` 外（候选角只扫 ±90°）。此时牧师离 boss 12 码，若首记践踏来早会中。候选：候选角扫到 ±180°，或允许候选半径到 24。

证据：`evidence/playerbots-run483.log.gz`（本地）。

## 第十四轮（2026-09-14 中午）：确认轮 run 484（1/5）+ 近战躲踏路径修正 run 485（2/5）；发现 AN 移动动作被 `set facing` 抢占

**run 484**（与 483 同二进制，确认轮）：1 击杀 / 4 团灭，boss 剩 22/20/18/20%。牧师仍没被践踏打到（保距成立），但首死换成：
坦克 222 s（守卫 7 刀 11.7k + 毒疗者）、盗贼 176 s（35k 践踏）、盗贼 224 s（被磨到 2.1k 后穿刺 8k 收掉）、法师 153 s（毒疗者 9.5k + 穿刺 7.9k）。
483+484 合计 **5/10**，此前两轮 2/10；483 的 4/5 有运气成分。

**盗贼躲踏为什么没躲成**（run 478–485 六次秒杀逐一重建，用命中时间减 3.2 秒取读条起点——`spell` 表的践踏 `cast:` 行是完成时落库的）：
core 里 `Spell::prepare → Creature::FocusTarget → SetInFront(目标)`，读条期服务端朝向就是锥轴（`DisableRotate` 只关客户端转向），
"背后 5 码"没算错；错在**路径**：483/1、483/5 盗贼站在正面 10.7 / 15 码，去背后要穿过整个 120° 锥，20 码 2.9 秒，落地前 0.2 秒还在正面。
修法（mod-playerbots `32a59e29`）：读条期算偏角，正面（≤75°）且 ≥8 码 → 径向退到 18 码（15→18 只要 3 码），贴近或侧后方才绕背后。

**run 485**（含路径修正）：2 击杀 / 3 团灭（264/1 死、281 团灭 boss 5%、264/1 死、239 团灭 23%、287 团灭 2%）。盗贼躲踏 13 次全部落地；
唯一一次盗贼被秒（485/2，正面 12.3 码 32.8k）**躲踏根本没执行**——读条 3.2 秒的三个 tick 一步没动，`an-move` 没有对应行。

**tick 级统计（本轮日志，每 tick 只执行一个动作）**：

| bot / 触发器 → 动作（相关性） | 触发 tick | 动作没执行 | 被谁吃掉 |
|---|---|---|---|
| 盗贼 `anub'arak pound` → 躲踏（35） | 46 | **23** | `set facing`（37）15、`drop target`（99）4、`dps assist` 3 |
| 牧师 `ranged too close` → 保距（33） | 43 | **31** | `set facing` 22、drop target / dps assist 各 3 |
| 法师 `ranged too close` → 保距（33） | 27 | **21** | `set facing` 7、`dps assist` 5、`blink` 3 |
| 牧师 `pound` → 躲踏（35） | 52 | 22 | 自己的践踏预盾 / 接治疗（瞬发，可接受） |

`CombatStrategy` 的 `not facing target → set facing` 是 ACTION_MOVE+7=37，压在所有 AN 移动动作之上。修法（未编译）：躲穿刺/躲践踏 35→40、保距 33→38，
治疗的践踏预盾 39 仍先于保距。这也解释了 483 里治疗保距"88 次里只 41 次 ok"的另一半。

**同时浮出的下一层**：毒疗者。run 479–485 每轮对全队 545–700k（全队承伤第一），485/4 坦克 15 秒内吃 10 发毒箭 14.3k；
牧师蓝 <500 的采样：483 只 3 次，484/485 是 112/159 次，与团灭强相关。毒箭齐射（53616）和毒箭（53617）都是读条，盗贼脚踢/萨满风剪/法师法术反制
都能打断——"毒疗者优先 + 打断"是下一项候选。

证据：`evidence/playerbots-run484.log.gz`、`playerbots-run485.log.gz`（本地）。

## 第十五轮（2026-09-14 下午）：AN 移动动作相关性压过 `set facing` → run 486 实为 2/5；发现框架漏判击杀、穿刺躲避 3/4 失败

改动（mod-playerbots `c409c7e8`）：躲穿刺/躲践踏 35→40、保距 33→38。

| 指标 | run 485 | **run 486** |
|---|---|---|
| 盗贼践踏 tick 被抢占 | 23/46 | **21/82**，全是 `dps assist`/`drop target`（99） |
| 牧师保距 tick 被抢占 | 31/43 | **12/78** |
| ≥15k 践踏命中非坦克 | 2 | **0**（全轮第一次） |
| 结果 | 2/5 | 1 击杀 + **1 记成 timeout 的击杀** + 3 团灭（boss 19/18/21%） |

**框架漏判**：486/2 坦克 237 s 阵亡释放灵魂，法师 278 s 打死 boss（有 `death 104` 行、其余四人满血脱战），但 `AttemptObserver::ResolveBoss` 只从 `bots[0]`
的地图找 boss——坦克在墓地地图，找不到 → 血量采样冻结在 17% → 不判 Kill。run 451/4 同签名。修法（mod-raidtest，未编译）：用任何一个还在场景地图上的 bot 找。

**穿刺躲避 3/4 失败**：`anub'arak dodge impale` 四个非坦克 FAILED:OK = 61:10 / 90:52 / 74:21 / 78:14，穿刺每场每人中 1–8 次、7–8k 一记；
486 三场首死是法师被毒箭齐射打到半血后穿刺收掉（146/169 s）。动作走的是共享层 `Move()`（`CheckCollisionAndGetValidCoords` + 寻路 `MoveTo`），
正是第七轮记过的坑。修法（未编译）：改为直线 spline + 同层地面 + 护栏校验，首选背离尖刺方向，落点出界换 ±45°/±90°。

**毒疗者量化**（run 483–485，纯查库）：每场刷 4 只（两次潜地各一对），平均活 24 s、完成 2 次毒箭齐射（59359，2 s 读条，每人 2–3.5k，一发约 13k 全队），
每轮对全队 545–700k、承伤第一；毒的 DoT 只有 ~1k 且萨满 Cure Toxins 每场 11 次已在清。盗贼脚踢 22 / 萨满风剪 58 次已经打在毒疗者身上，仍有 118 发齐射完成。
bot 打在毒疗者身上的伤害只占 14%（守卫 33%、boss 38%）。牧师蓝 <500 采样：483 只 3 次，484/485/486 是 112/159/89 次，与团灭强相关。
共享层 `prioritized targets` 值没有任何副本策略在填；团队标记（骷髅 icon 7）会让所有 DPS 的 `DpsTargetValue` 优先打它，是 AN 层最自然的钩子。

证据：`evidence/playerbots-run486.log.gz`（本地）。

## 第十六轮（2026-09-14 下午）：框架 ResolveBoss 修复（mod-raidtest `51826f1`）+ 穿刺躲避改直线 spline → run 487 2/5；穿刺命中 17/21 落在准备点

run 487：2 击杀零死亡（272 / 272 s）+ 3 团灭（boss 20 / 24 / 16%）。首死：盗贼 179 s 贴脸正面 26.7k 践踏（读条 3 个 tick 被"目标死→drop target(99)→dps assist(50)"吃光，
躲踏 40 仍没轮到）；萨满 203 s 守卫+boss；坦克 234 s 毒疗者 17k + 守卫 12.5k、牧师蓝 116。框架修复没被触发（本轮没有坦克先死后击杀的场）。

**穿刺躲避改写后命中没降**（4.2 次/场 vs 483–486 的 2.6–4.5）：逐一看 21 次命中——**17 次落在准备点 (527,248)**，都是法师/萨满站在原地 0 移动，
常常一根尖刺同时穿两人。`an-move dodge impale` 133 次 ok、**67 次 no_candidate**，全在 x≈527：西沿护栏要求候选点 x ≥ 536，站在准备点的人 8 码内没有任何合法落点。
run 483 保距的 47 次 no_candidate 也是同一条护栏（boss 被拉到 x=539 时）。修法（未编译）：护栏改为"不比现在更靠西即可"（`x >= kArenaGuardX || x >= bot.x`），
躲穿刺、`PickPointAwayFromBoss` 两处同改。

| 指标 | 483 | 484 | 485 | 486 | 487 |
|---|---|---|---|---|---|
| 结果 | 4/5 | 1/5 | 2/5 | 2/5（1 漏判） | 2/5 |
| ≥15k 践踏命中非坦克 | 2 | 1 | 2 | 0 | 1 |
| 穿刺命中 / 场 | 2.6 | 4.5 | 4.4 | 3.4 | 4.2 |
| 毒疗者对全队（k） | 545 | 629 | 700 | 489 | 624 |
| 牧师蓝 <500 采样 | 3 | 112 | 159 | 89 | 70 |

已写好未编译的一批：护栏放宽（两处）；躲踏/躲穿刺 40→60、保距 38→58（压过 dps assist 50）；毒疗者优先——`anub'arak venomancer focus` 触发器 +
`anub'arak focus venomancer` 动作（相关性 55）：非坦克非治疗在有活着且已进入主坦 40 码内的毒疗者时，把最近一只写进 `prioritized targets`
（`DpsTargetValue` 的 `IsHighPriority` 先选它），没有时清掉；用主坦距离做门限是避免 DPS 盯着坡道上追敌上限够不到的目标发呆。

证据：`evidence/playerbots-run487.log.gz`（本地）。

## 第十七轮（2026-09-14 下午）：护栏放宽 + 相关性 60/58 + 毒疗者优先 → run 488 2 击杀 / 1 团灭 / 2 超时（boss 重置）；毒疗者优先证伪、躲踏再挖出两个 bug

| 指标 | run 487 | **run 488** |
|---|---|---|
| 穿刺命中 / 场（其中在准备点） | 4.2（17/21） | **1.5（0）**；`dodge impale` ok 155、no_candidate 52（不再是准备点） |
| 盗贼践踏 tick 被抢占 | 6/58 | **1/72**；牧师保距 3/88 |
| ≥15k 践踏命中非坦克 | 1 | 2——都在开怪 22–24 秒、西沿（见下） |
| DPS 打在守卫 / 毒疗者身上（k） | 1713 / 648 | **1226** / 586 |
| 守卫对全队（k） | — | 729 |
| 结果 | 2/5 | 2 击杀（281/1 死、252 零死亡）+ 1 团灭 + 2 超时（早期死人后 boss 脱战重置到 100%，不是漏判） |

**毒疗者优先证伪**：488/1 第二次潜地期 DPS 全在打毒疗者（盗贼 23k vs 其他 1k，法师 17k vs 守卫 4k），两只守卫 15 秒 28k（单刀 8.5k）打死坦克，
牧师/萨满跟着死，boss 重置。守卫才是坦克杀手（破甲 + 5–8.5k 一刀，也是 30k 践踏的来源），bot 原生目标选择本来就把约三分之一伤害给守卫；
盯毒疗者让守卫多活。节点已停用（代码保留），"没有守卫时才盯毒疗者"另议。

**躲踏两个新 bug（都在西沿、开怪头 25 秒）**：
- 488/3 盗贼 22 s 33.8k：boss 被拉到 x=533.7，盗贼在准备点 (527,248) 本来就在背后；背后点 (528.7,250) 被 `x = kArenaSafeMinX` **夹成 (533,250)——boss 脚下**，
  盗贼跑上去挨打。修法：夹紧改为"不比现在更靠西"，不合法退到径向。
- 488/4 萨满 24 s 23.5k：萨满在准备点、离 boss 15.8 码、**锥外 83°**，本来安全；径向候选被护栏挡掉后选了侧向 18 码点，直线路过 13.9 码、偏轴 10° 处被打。
  修法：(1) `isUseful` 重新看朝向——读条期 `FocusTarget → SetInFront` 朝向可信，已在 ±75° 锥外的人不动（第七轮"不再看朝向"是锥角还按 24° 算时的结论）；
  (2) `PickPointAwayFromBoss` 加"直线路径离 boss 的最近距离不得比现在更近"（`PathMinDistToBoss`），躲踏用、保距不用。

保距 `no_candidate` 仍 76 次：准备点上的人朝外的候选点更靠西、侧向的又出 `kArenaSafeRadius=22`；候选半径放到 24 是下一步候选（平台半径 27，有同层守卫兜底）。

证据：`evidence/playerbots-run488.log.gz`（本地）。

## 第十八轮（2026-09-14 下午）：躲踏西沿修正 + 毒疗者优先停用 → run 489 2/5、run 490 1/5；挖出两个共享层根因：坦克追到刷新点、治疗掉进非战斗引擎

AN 层批次提交 mod-playerbots `5142cc13`（护栏放宽、相关性 60/58、穿刺直线 spline、躲踏两处修正、毒疗者优先停用）。

| 指标 | run 489 | run 490 |
|---|---|---|
| 结果 | 2 击杀 / 3 团灭（boss 20 / 16 / **72**%） | 1 击杀 / 4 团灭（**66** / 17 / 21 / 13%） |
| ≥15k 践踏命中非坦克 | 1 | **0** |
| 穿刺命中 / 场 | 1.3 | 2.0 |
| 牧师蓝 <500 采样 | 45 | 63 |

践踏对非坦克的威胁基本解除（两轮 1 次），穿刺降到 1–2 次/场。团灭换成两种潜地期死法，都追到了共享层根因：

**1. 坦克追到刷新点（489/5，78 s 团灭 boss 72%）**：37 s 坦克 `reach melee` 去追 100 码外刚刷新的守卫，自己跑到坡道 (550,327)，两只守卫走下来 30 秒把留在
平台上的四人全杀（牧师 5 刀 24k、法师/盗贼单刀 11–12k）。第十二轮的追敌上限把坦克豁免了（"接小怪是它的活"）——错。
修法（未提交，已编译进 run 491 二进制）：`ReachTargetAction::ChaseAnchor()`——非坦克锚主坦，坦克锚治疗（没有活着的治疗就取最近队友），上限同 38.5 码。

**2. 治疗掉进非战斗引擎（490/1 124 s 团灭 boss 66%、490/3）**：490/1 坦克 44–62 s 从 14k 掉到 0，牧师满蓝、**零治疗**，动作日志只有
`add gathering loot / apply oil / check mount state / follow / circle of healing IMPOSSIBLE`——非战斗引擎的动作；策略快照 12 s 起是非战斗集合，直到 68 s。
机制：12.2 s `dps assist` 之后目标失效 → `DropTargetAction` **无条件** `ChangeEngine(NON_COMBAT)`；`PlayerbotAI` 自己从不切回战斗引擎（1523 行只清目标），
只有 `AttackAction::Attack` / 拉怪动作会切；戒律牧师的非战斗策略集里没有可用治疗。490/3 死前 20 秒同样零治疗、快照非战斗集合。
量化（run 483–490，按策略快照）：治疗每场 30–40 秒处于非战斗集合，切成 5–7 秒一段，正好落在潜地转换点（守卫刷新时）；团灭场的长间隙 56 / 55 / 51 / 21 / 17 秒
（490/1、487/4、486/4、487/3、484/1）。37 场合计约 1660 秒治疗空窗。
修法（未提交，已编译进 run 491 二进制）：`UpdateAIInternal` 里"在战斗中却处于非战斗引擎 → 切回战斗引擎"（`engine-sync` 调试行）；
`DropTargetAction` 只在脱战时切非战斗引擎。

其余：490/4 萨满 209 s 被守卫 7.5k 刀打死、牧师 171 蓝（治疗 OOM）；490/5 法师 121 s 穿刺 7.3k + 守卫 4.5k。守卫仇恨量化（run 486–489，110 只）：
52 只先打非坦克（多为牧师），2–10 秒后全部转到坦克，坦克嘲讽 49 只；嘲讽只挂在"当前目标失去仇恨"上，守卫打萨满时要等坦克切目标才会嘲讽。

证据：`evidence/playerbots-run489.log.gz`、`playerbots-run490.log.gz`（本地）。

## 第十九轮（2026-09-14 下午）：共享层三处（战斗中必在战斗引擎、drop target 无目标判无用、追敌上限扩到坦克）→ run 491 中止、run 492 2/5；AOE 量化

mod-playerbots `4511b131`。第一版只做了"战斗中切回战斗引擎"+ 坦克锚点，run 491 前三场 45 / 37 / 33 秒全灭（boss 87–97%）：`InvalidTargetValue` 在**无目标**时也返回真，
以前 drop target 把 bot 切进非战斗引擎所以这个触发器不再被评估；现在留在战斗引擎，目标为空 → 每 tick `invalid target` → `drop target`（99）吃掉所有 tick，
牧师 34 秒 `drop target - OK` 38 次、0 次治疗。`raidtest stop` 中止，补 `DropTargetAction::isUseful`（有目标可丢或已脱战才有用），run 492 重跑。

| 指标 | run 489/490（改前） | **run 492** |
|---|---|---|
| 治疗处于非战斗集合的时长 | ~175 秒/轮（每场 30–40 秒，潜地转换点） | **0 秒** |
| 坦克死前北上 | 489/5 一次（全灭根因） | **0** |
| `engine-sync` 触发 | — | 8 次 |
| 牧师前 60 秒治疗 | 24–50 | 35（正常） |
| ≥15k 践踏命中非坦克 / 穿刺命中每场 | 1 / 1.3–2.0 | 1 / 1.0 |
| 结果 | 2/5、1/5 | 2 击杀（265 / 257 s 零死亡）/ 2 团灭（boss 25 / 11%）/ 1 超时（五人 244 s 齐倒，萨满自复活独活） |

三场失败都在末段：492/1 坦克 188 s 满血 20.5k 被 **25.2k 践踏**秒杀（破甲叠层，圣佑 88 s 用过冷却未转好）；492/2、492/3 牧师 225–262 s 蓝 252–355 见底，
坦克/盗贼被四种小怪磨死。回蓝手段查了 27 场：药水（40–130 s）、暗影魔、心灵专注、希望圣歌（~200 s）每场都用，蓝仍在 215–245 s 见底，而击杀要 250–280 s。

**AOE 量化**（run 489–492 引擎 tick）：盗贼 41% 的 tick 有 ≥3 小怪在目标 8 码内，刀扇推入 538 次只执行 35 次（相关性 15 输给绞喉 27/伏击 26/饥渴 26/切割 25/
剑刃乱舞 24/set facing 37）；元素萨闪电链 38 次、火焰新星 7 次（火图腾在自己脚下、离目标 >8 码判无用 116 次）；法师烈焰风暴/暴风雪 0 次——全被第六轮的
`AnubarakMageManaMultiplier` 归零（引擎把归零记成 IMPOSSIBLE），旧 boss 里同一法师烈焰风暴 10 次、炸弹铺小怪 164 次。法师蓝均值 8.8k、最低 5。

候选：法师 Multiplier 改条件放行（潜地期 + ≥3 小怪聚集 + 蓝 >50%）；盗贼刀扇 AOE 相关性 15→28（共享层职业策略，要回归）；
牧师痛苦压制接入践踏（破甲≥3 且圣佑冷却中）；共享层三处先回归三个旧 boss。

证据：`evidence/playerbots-run492.log.gz`（本地）。

## 第二十轮（2026-09-14 傍晚）：法师 AOE 条件放行 + 盗贼刀扇相关性 28 → run 493 1/5（零死亡击杀 276 s）；英勇时机量化

改动（mod-playerbots `e3c31f6e`，含回归后对引擎同步的修正——只在有可见 attackers 时强制进战斗引擎）：`AnubarakMageManaMultiplier` 对烈焰风暴/暴风雪条件放行（boss 潜地 + 目标 8 码内 ≥3 活小怪 + 法师蓝 >50%，铺炸弹仍归零）；
刺杀/通用盗贼策略 `medium aoe → fan of knives` 相关性 15→28（共享层）。

| 指标 | run 492 | **run 493** |
|---|---|---|
| 法师烈焰风暴/暴风雪 | 0 | **25**（引擎 OK 21 + 4） |
| 盗贼刀扇 / 场 | 10.2 | 11.2（引擎 OK 56 / 推入 644，8.7%——tick 仍被 dps assist / reach melee / set facing 占） |
| 守卫 / 毒疗者平均存活 | 27 / 26 s | **23 / 21 s** |
| 小怪对全队伤害 | 1384k | 1254k（−9%） |
| 牧师蓝 <500 采样 | 62 | 41 |
| 法师最低蓝 | 5 | 36 |
| 结果 | 2/5 | 1 击杀（零死亡）/ 3 团灭（boss 15 / 20 / 17%）/ 1 超时（17%） |

法师 AOE 放出来了、小怪活得短了一成，但四场失败仍全部倒在 boss 15–20%、牧师蓝 159–2900 的同一道墙上；击杀率差异在噪声内。
刀扇的瓶颈不在相关性而在 tick 被目标切换/移动占掉（AOE tick 里执行的前三名是 dps assist、reach melee、set facing）。

**英勇时机**（run 483–493 共 39 场）：开怪 20 秒内 1 次；40–95 s 9 次；123–177 s（boss 41–48%，第二次潜地附近）**17 次**；200 s 后 9 次；
**13 场整场没放**（含 3 场击杀）。8 次落在 boss 潜地窗口（之后 40 秒对 boss 伤害行 0–3）。机制：共享层 `BoostTrigger` 按"balance"≤50 触发，
不看阶段——打得顺就不放。候选：AN 层开怪 5–15 秒、boss 可选中且 >90% 时开英勇（全队满蓝、无小怪的唯一窗口）。

证据：`evidence/playerbots-run493.log.gz`（本地）。

## 第二十一轮（2026-09-14 夜）：先量化再动手——潜地窗口分解 + 坦克接小怪（共享层圣骑士）

### 一、改前口径（run 483–493 共 50 场，脚本 `evidence/taunt_baseline.py`，可复跑）

| 项 | 值 |
|---|---|
| 结果 | 18 击杀 / 27 团灭 / 5 超时（36%） |
| 每场承伤 | **443.5k**，落在非坦克身上 **236.2k（53%）** |
| boss / 守卫 / 毒疗者 / 掠夺者 | 166.3k / 124.6k / 109.5k / 16.2k；其中打非坦克 94.6k / **39.1k** / **69.3k** / **12.6k** |
| 毒疗者 | 刷出 186 只，186 只都打过非坦克，**83 只（45%）一辈子没打过坦克** |
| 守卫 | 刷出 290 只，40 只从不打坦克；在非坦克身上停留中位 **4.0s**、p90 10.1s |
| 坦克嘲讽用量 | 制裁之手 14.5 + 正义防御 11.2 = 25.7 次/场（两个 8 秒冷却，理论上限约 68 次/场） |
| 段落覆盖 | 小怪开在非坦克身上 23.8 段/场，5 秒内有任意嘲讽：守卫 80% / 毒疗者 57% / 掠夺者 66% |
| 牧师首次蓝 <500 | 43/50 场，中位 **231s**（击杀要 250–280s） |

**可寻址的池子不是 121k。** 毒疗者是远程施法者，毒箭齐射对全队落伤害，嘲讽只按住 3 秒、之后没仇恨照样转回去，
所以那 69.3k 接不走；真正能靠嘲讽改善的是贴身近战的守卫 39.1k + 掠夺者 12.6k ≈ **52k/场（约 12% 全队承伤）**。
覆盖率数据也说明问题是**延迟不是缺失**：守卫的段落 80% 在 5 秒内已经有嘲讽。这一改动的合理预期是把中位停留 4.0s 压到 1–2s、
省 20–30k/场（5–7% 承伤），折成牧师蓝大约多撑 15 秒左右——**不足以单独推倒末段 OOM 那道墙**。

### 二、顺带排除了另外两个候选（脚本 `evidence/submerge_windows.py`）

boss 在 75/50/25 三个血线潜地，core `boss_anubarak.cpp` 的 `EVENT_EMERGE` 是 60 秒，但杀光「大型召唤物」
（守卫 / 毒疗者，英雄第一次 2 只、第二三次各 4 只）会把出土改成 5 秒。实测 49 场：

| | 第一次潜地 | 第二次 | 第三次 |
|---|---|---|---|
| 时长中位 | 28s | 48s | 49s |
| 潜地期输出落在大型召唤物身上 | 90% | 91% | 84% |

- **「守卫优先」没有头寸**：输出已经 90% 压在大型召唤物身上，提前出土的机制一直在生效；第二三次的下限被毒疗者
  24/29 秒才刷出卡死，理论上最多再省 5 秒。
- **英勇开怪即放（§5-2）的天花板很低**：三次潜地合计 ~127 秒，占全场（250–280s）一半，这段时间 boss 不可被攻击；
  40 秒 30% 急速摊到全场约等于省 10 秒（~4%）。

### 三、改动（mod-playerbots，共享层，只动圣骑士坦克）

根因在 `src/Ai/Class/Paladin/Actions/PaladinActions.cpp:581`：`CastRighteousDefenseAction::GetTarget()` 只取
`current target->GetVictim()`，触发器是 `LoseAggroTrigger`（`GenericTriggers.cpp:108`，只看「我当前目标掉没掉仇恨」）。
也就是说**坦克只盯自己当前的那一只**，其他正在啃法师/牧师的怪根本不进视野。

新增：
- `PartyMemberMeleeAggroTrigger`（"party member melee aggro"）：`attackers` 里存在「victim 是队友、不是坦克当前目标、
  且**贴在 victim 近战距离内**、victim 在 30 码内且有视线」的怪。近战判据用 `IsWithinMeleeRange`，天然把毒疗者一类
  远程施法者排除在外——不靠 entry 硬编码。
- `CastRighteousDefenseOnPartyAction`（"righteous defense on party"）：对身上小怪最多的队友放正义防御（一次最多接 3 只），
  同数取血量低的。**只用正义防御，不用制裁之手**——制裁之手留给「当前目标掉仇恨」那条路，避免两个嘲讽同时进冷却。

6 个文件、17 个 TU。

### 四、run 499 作废：相关性 26.5 被抢占，一次都没执行（又一次同样的坑）

第一版挂在 `ACTION_HIGH + 6.5`（=26.5）。tick 级统计：触发器亮起的 15 个 tick 里，新动作被**尝试 0 次**——
队列里 `pull action`(105)、`tank assist`(50)、`tank face`(30)、`hand of reckoning`(27)、`consecration`(27) 全部排在前面，
其中之一 OK 后 tick 就结束了。run 499 因此等于「没改动的对照」，跑完第一场（团灭，boss 40%）后 `raidtest stop` 作废。

改为 `ACTION_HIGH + 35.0f`（=55）：压过 `tank assist`(50)，仍低于 AN 层躲踏/保距（58/60，躲 25k 践踏优先）。
重编后实测 5 次推入 → **5 次 OK**。注意 USELESS 不吃 tick（正义防御在冷却时直接跳过），所以高相关性不会挤占其他动作。

### 五、两轮结果（run 500 / 501，各 5 场）：机制成立，收益在噪声内

| | 基线 483–493（50 场） | run 500 | run 501 | **合并 10 场** |
|---|---|---|---|---|
| 击杀 | 18 = **36%** | 4/5 | 2/5 | **6 = 60%（Fisher 双尾 p=0.178，不显著）** |
| 全队承伤/场 | 443.5k | 429.6k | — | 444.7k（没变） |
| 非坦承伤/场 | 236.2k（53%） | 200.3k | — | 219.7k（49%，−7%） |
| 守卫打非坦 | 39.1k | 30.5k | — | 35.5k（−9%） |
| 守卫开在非坦身上的段数/场 | 6.0 | 3.8 | — | 5.0（−17%） |
| **守卫停留中位（验收口径）** | 4.0s | 4.8s | — | **4.6s——目标 ≤2s，未达成，反而变长** |
| 毒疗者打非坦 | 69.3k | 68.6k | — | 66.4k（不变，符合预期） |
| 非坦克死亡/场 | 3.00 | — | — | **2.30（−23%）** |
| **牧师首次蓝<500（验收口径）** | 中位 231s | 220s | — | **中位 220s——没后移** |
| 嘲讽用量 | RD 11.2 + HoR 14.5 | — | — | RD 13.6 + HoR 13.0（总量不变，其中 **9 次/场**走新路径） |

**判定：不能算有效。** 收益方向对（守卫段数 −17%、非坦承伤 −7%、非坦死亡 −23%），但两项验收口径都没达成，
击杀率 36%→60% 在噪声内。收益机制也和设计预期不同：不是「把停留从 4 秒压到 1–2 秒」，而是**段数变少**——
真正溜掉的那几段反而更久（4.6s），多半是正义防御在冷却、或队友在 30 码/视线外。

两个异常场与本改动无关：
- run 501/4（88 秒团灭）：牧师开场 18 秒被 boss 两记共 **35.7k** 打死——穿刺/践踏开在治疗身上的老缝。
- run 501/1（团灭但 `boss_hp_min=0`）：boss 确实被压到 <1% 才全灭，日志里没有 boss 死亡事件，
  不是第十六轮那个「漏判击杀」框架 bug 复发。

### 六、共享层回归（改动只碰圣骑士坦克策略）

| boss | 对照 | 本轮 |
|---|---|---|
| 凯利丝塔萨 `heroic-nexus-keristrasza-disc-n5` | run 494 3/3 | **3/3 零死亡**（123 / 137 / 142 s，对照 run 426 的 117–134 s） |
| 斯卡瓦尔德&达隆 `heroic-uk-skarvald-dalronn-n5` | run 497 3/3 | **3/3 零死亡**（101 / 99 / 108 s） |
| 阿诺玛鲁斯 `heroic-nexus-anomalus-n5` | run 495 / 498 各 2/3（消耗战团灭，待观察项） | **3/3 零死亡**（248 / 248 / 203 s，消耗战团灭未复发） |

三项回归（run 502 / 503 / 504）全部干净，无误拦、无可见回归；阿诺玛鲁斯反而比对照好一档——
但那是 3 场对 6 场，只能说「待观察项这次没复发」，不足以把它划掉。

## 第二十二轮（2026-09-14 夜 ～ 09-15）：萨满补位治疗 + 神圣恳求常驻 + 英勇卡最后冲刺

三项各跑两轮 5 场。**共同结果：每一项的中间量都确定位移，但击杀率都停在 6/10 = 60%**（对照 60 场基线 24/60 = 40%，Fisher p≈0.3 不显著）。

### 一、萨满补位治疗（AN 层，mod-playerbots `deca7f41`）

改前口径（60 场）：坦克阵亡 39 次，其中 **31 次死前 3 秒 boss 一下没碰到**——是守卫(53.5%)+毒疗者(35.7%)磨死的，
同时刻牧师蓝中位 **38**；而元素萨满 18,475 的法力池整场只放 3.5 次治疗（牧师池 20,049）。
改动：治疗没蓝（<20%）或阵亡、且 `party member to heal` 血 ≤65% 时，非治疗职业放治疗波（相关性 55）。

| 指标 | 基线 60 场 | run 505+506 |
|---|---|---|
| 击杀 | 40% | 60% |
| 坦克消耗型阵亡 | 31 | **1** |
| 牧师首次蓝<500 | 中位 231s（51/60 场） | 中位 **249s**（5/10 场） |
| 失败场 boss 最低血 | 20% | **4%** |

### 二、神圣恳求常驻（共享层，仅圣骑士坦克）

用户给的 WLK 防骑续蓝闭环逐条核对（`character_talent` 为准）：精神协调 **1/2**（缺 33776）、圣光守护 **2/2**、
庇护祝福 ✓（回蓝 proc 57319 每场约 48 次）、祈福 **0/5**（5 点在躲闪 Deflection）；
大雕文三件套（神圣恳求 63223 / 复仇圣印 56416 / 正义防御 54929）**全中**，见 [GLYPHS.md](../../fixtures/normal5-v1/GLYPHS.md)。

**但比天赋更致命的是执行层**：恳求挂在共享层 `HighManaTrigger`（蓝<65% 才亮）+ `ACTION_HIGH`(20)，
实测坦克 3733 个 tick 里推入 381 次、**只执行 9 次（2.4%）**，被 `tank assist`(50)/`drop target`(99)/
`set facing`/`hand of reckoning`(27) 抢光；恳求不在身上，圣光守护每场 123 次近战 proc 全刷在空气上。
先只抬相关性（run 507）无效——门槛还挡着，两场 0 次施放；改成 `DivinePleaUptimeTrigger`「战斗中且恳求不在身上」后生效。

| 指标 | run 505+506 | run 508+509 | run 513+514 |
|---|---|---|---|
| 坦克**活着时**法力地板中位 | 3224（最低 1064） | **4636（最低 3916）** | 4512（最低 3537） |
| 200 秒后奉献 / 神圣之盾 | 2.9 / 0.8 | — | **3.7 / 1.3** |

**天赋不用再改了**：地板从占蓝池 50% 抬到 70–72%，坦克全程不见底，精神协调/祈福要解决的问题已不存在。
稳态形态是「一场只放 0.2 次恳求」——上身后被圣光守护无限续，连跨场都不掉，所以**「放了几次」不是好口径，看法力地板**。

### 三、英勇卡最后冲刺（AN 层 + Multiplier）

判别输赢的是 **180 秒时 boss 的血**（击杀场中位 24% / 失败场 41%）；英勇在击杀场中位落 213s、失败场落 143s。
改动：`AnubarakHeroismMultiplier` 在第三次出土前把英勇/嗜血归零，`AnubarakHeroismTrigger` 在之后放（相关性 56）。

踩了三个坑，都记下来：
1. **夹具**：疲惫(57723) 10 分钟 > 一场周期 5.3 分钟，下一场施法直接 `SPELL_FAILED_TARGET_AURASTATE(111)`。
   已在 mod-raidtest `4beddea` 的 `RestoreRoster` 里清掉。**这也修正了第二十轮「13 场没放英勇」的归因**。
2. **`find target` 看不到潜地中的 boss**（`UNIT_FLAG_NOT_SELECTABLE` 不进目标表），Multiplier fail-open，run 511 英勇又落在 184/195s → 改 `FindNearestCreature`。
3. **「可选中且血 ≤25%」≠「第三次出土后」**：run 512/1 实测 boss 191s 已可选中且 23%，第三次潜地 217s 才开始 → 改成数出土次数的状态机。

结果：英勇 **10/10 场放出、中位 259s**，全部在第三次出土后（对照 46/60 场、中位 136s）。
但击杀率仍 6/10，且**击杀耗时 254s→280s、180 秒时 boss 血 34%→38%**——把加速全押到最后 40 秒，前半段更慢，净收益约等于零。

## 第二十三轮（2026-09-15）：两项证伪——圣佑收紧、近战绕背

### 一、圣佑收紧（破甲 ≥5 且读条只剩 1.5 秒再开）→ run 515 **0/5**，已回退

改前量化（run 508/509/513/514 共 20 场）：**51 次 boss 打到坦克 ≥8k 的命中里只有 1 次（2%）落在圣佑 12 秒窗口内**；
圣佑 1.2 次/场（2 分钟冷却理论 2–3 次）；24 次圣佑里 11 次之后 5 秒 boss 根本没打到坦克（读条被血线/潜地打断）。
践踏对坦克按时间分档：0–59s 中位 10.4k → **240–299s 中位 15.5k、最大 20.1k**。

收紧后：触发器 4.7→2 次/场、圣佑 1.3→0.6 次/场、≥8k 覆盖率 4%→**0%**、坦克 **5/5 全死**、击杀 0/5。
教训：践踏读条 3.2 秒，但 tick 粒度加排队让「剩 1.5 秒再开」经常错过，**白给一次圣佑远好过该开时没开**；
破甲阈值也不能抬——守卫要到第一次潜地（约 50 秒）才刷，抬到 5 只会让中段也开不出来。
已回退到破甲 ≥3，只保留「boss ≤25% 时即使破甲不足也能开」这一处放宽。

### 二、近战绕背 → 无头寸，注册但不接入

**先前那份「盗贼 35% 时间在锥内、中位夹角 50°」的量化是错的**：混入了潜地期（boss 不可选中、践踏锥不存在）。
只统计 boss 浮出期（脚本 `evidence/melee_arc_stats.py`）：

| | 改前 513+514 | 改后 516 |
|---|---|---|
| 盗贼锥内占比 | 19% | 20% |
| 盗贼中位夹角 | 116° | 110° |
| 坦克锥内 | 92% | 88% |

盗贼本来就站背后。两次 32k 一击秒杀（夹角 50°/44°）属于那 19% 里的偶发，只能靠读条期的 `dodge pound` 兜。

### 三、关于 DBM 式预警（用户提问）

三档：(a) 读 boss 施法条——已在用，等价真人看读条；(b) 按遭遇战脚本 + 观察事件推算的计时器模型——等价于 DBM，正当但需新建；
(c) 直接读 boss AI 的 `EventMap`——比 DBM 还多，属于作弊，不做（上游 `UldBossHelper.h:325`、`NaxxMultipliers.cpp:151` 也把这行注释掉了）。
本 boss 用不上 (b)：近战站位已经是对的。

### 四、共享层回归（run 518/519/520）

| boss | 对照 run 502–504 | 本次 |
|---|---|---|
| 凯利丝塔萨 | 3/3（123/137/142s） | **3/3 零死亡**（133/132/116s） |
| 斯卡瓦尔德&达隆 | 3/3（101/99/108s） | 2 击杀零死亡（98/91s）+ **1 场前置清怪没拉起来** |
| 阿诺玛鲁斯 | 3/3（248/248/203s） | 2 击杀零死亡（252/216s）+ 1 场超时 |

击杀耗时都在或快于对照，无可见回归。但两场没跑成要记账：斯卡瓦尔德那场是 `BeginPullForAll: could not initiate
attack on Tunneling Ghoul`，**与 run 496 同签名**（当时归因「战斗中必在战斗引擎」并修成「有可见 attackers 才强制」），
说明那个修法只压低了频率、没根除。

### 五、这三轮的总结论

坦克资源、治疗接力、英勇时机三条都修好了，中间量都确定位移，**但击杀率一直停在 60%**。
说明瓶颈已经不是任何单一技能时机，而是这套装备/职业组合的**总输出与治疗总量 vs boss 血量 + 固定 127 秒潜地时间**之间余量本来就薄：
打得顺 250 秒杀掉，稍有波折拖到 300 秒，然后在治疗见底那刻整队崩。再往前推需要的是总量级的改变，不是第四个时机。

## 第二十四轮（2026-09-15）：DPS 排查 → 共享层目标接管 → **击杀率 40% → 80%（p=0.036）**

前三轮都停在 60%，于是改查「输出总量差 28%」这个缺口（见第二十三轮结论）。

### 一、DPS 排查：不是没机会出手，是出手时放不出该放的技能

| | DPS | 对 boss | 对小怪 | 移动时间 | 5 秒空窗 |
|---|---|---|---|---|---|
| 盗贼（刺杀） | **997** | 121k | 150k | 25% | 4% |
| 法师（火焰） | 1229 | 134k | 197k | 6% | 17% |
| 萨满（元素） | 1687 | 161k | 265k | 6% | 15% |
| 坦克 | 798 | 56k | 148k | 14% | 1% |

盗贼 997 是最扎眼的（这档装备该有 1800+），但**空窗只有 4%**，说明一直在打，只是每次出手价值太低：
毁伤 21.2 次/场（该 60–80）、毒伤 **3.3 次/场**（该 20+）、**能量堆满 100 的采样占 24%**。

tick 级（1820 个 tick）：毁伤被推入 **303 次只有 17% 轮得到**，抢先 OK 的是
`dps assist` 53 + `reach melee` 43 + `set facing` 32 + 躲穿刺 31 + `drop target` 21 ——
**目标管理链合计 46%**。相关性表：`drop target` 99 / `dps assist` 50 / `set facing` 37 / `reach melee` 21 / **`mutilate` 13**。

**两个被证伪的中间推断（记下来别再走）**：
- 「42% 的 tick 花在切目标 = 42% 的输出损失」**不成立**：自动攻击不占 tick，盗贼 5 秒空窗只有 4%。
- 自制的「切换成本」脚本**口径错误**：换目标的空档中位 0.1–0.2 秒，比同目标出手间隔还短——群体技能同刻命中多目标被误判成切换。该脚本（`target_switch_cost.py`）作废。

### 二、面向判据（`IsFacingValue`）：与核心不一致，改对了但收益不在目标指标上

`HasInArc(M_PI_2)` = **±45°**，而核心自身判面向用 `HasInArc(M_PI)` = ±90°（`Unit.cpp` 的招架/格挡/命中方向、施法的 `isInFront`）。
中间 45° 灰带里 bot 能正常攻击施法，却每次目标微动就点亮 `not facing target` → `set facing`（相关性 37，压在主输出之上，
且执行后 `SetNextCheckDelay(ReactDelay=100ms)`，盗贼一场 198 次 ≈ 额外 20 秒停顿）。

改成 170° 全角（±85°，留 5° 余量）后（run 522）：`set facing` 执行 **−39%**、施法被 `NOT_INFRONT` 打回 **0 次**，
但**毁伤没涨**（6.3→5.6 每百 tick）——省下的 tick 被目标管理链吃掉了。**正确但收益不在目标指标上**，保留。

### 三、目标接管：丢掉尸体的同一个 tick 内直接打下一个 → 今天第一个统计显著的改动

`DropTargetAction` 改继承 `AttackAction`，丢完目标直接 `Attack(下一个)`。真人是死一个立刻 tab 下一个；
原来要连走 `drop target → dps assist → reach melee → set facing` 四个 tick（每 tick 0.69 秒 ≈ 2.8 秒不出手），
一场死约 20 只小怪 ≈ **56 秒空转**。

| | 基线 60 场 | 前几项改动 20 场 | **目标接管 523+524（10 场）** |
|---|---|---|---|
| 击杀 | 24/60 = 40% | 12/20 = 60% | **8/10 = 80%** |
| Fisher 双尾 p | — | ≈0.3 | **0.036** |
| 击杀耗时中位 | 264s | 267s | **253s** |
| 全队 DPS | 4494 | 4349 | **4876（+9%）** |
| 三职业 `dps assist` 执行 | — | — | **−31~−42%**（机制直接效果） |

两轮分别 4/5、4/5，没有前几项那种 5/5→1/5 的大起大落。
**单职业 DPS 归因不稳**：合并后收益主要在盗贼(+4%)与坦克(+6%)，萨满 +0.4%、法师 −3%；
全队 +9% 里含「战斗更短 → 低效时段占比更小」的相互作用，不能简单拆到人头。

### 四、回归（run 525/526/527）：9/9 零死亡，且普遍更快

| boss | 对照 run 518–520 | 本次 |
|---|---|---|
| 凯利丝塔萨 | 3/3（133/132/116s） | **3/3**（121/125/115s） |
| 斯卡瓦尔德&达隆 | 2 击杀 + 1 场前置清怪失败 | **3/3**（108/90/92s） |
| 阿诺玛鲁斯 | 2 击杀 + 1 超时 | **3/3**（188/230/171s，对照 248/216s） |

阿诺玛鲁斯快 30–60 秒；run 518 那次偶发的前置清怪失败未复现。

## 下一步

~~去修共享层治疗缺陷~~ 已修并验证：不是本 boss 的瓶颈（第四轮）。~~补 prepare 结果码日志~~ 已补：**FAILED = 没蓝**。

~~牧师药水 + 盾退出 almost full~~（第五轮）、~~法师药水 + 群攻归零~~（第六轮）、~~践踏锥角/半径 + 走廊怪移除~~（第七轮）已做；
站位三实验已回退。当前基线 = run 469 二进制 + 场景（有效 4 场 2 击杀）。

按影响：
1. ~~法师蓝量~~ 已修：炸弹铺小怪被真正压住后法师全程有蓝、DPS 1341（第八轮）。
2. ~~西沿掉落~~ 共享层同层守卫上线后 run 471 掉落 0 人次（第八轮）。阵型移动禁用实验不再需要。
3. ~~践踏躲避~~ run 472 证明"所有非坦克只看距离"有害（治疗跑位），run 473 改为按角色分档。剩余缝隙：治疗在 10–15 码被 32k 秒杀（473/5）；
   候选：治疗也接入保距（战斗中、TACTICAL，run 466 的失败原因已修）而不是靠躲；查 187 秒后 30k+ 践踏的成因（debuff 叠加？）。
4. 夹具：给 roster 配背包或清理拾取物，否则约每 20 场一次 `fixture_invalid`。
5. 剩下的团灭都是 200–260 秒第三次潜地前 boss 没死：DPS 检查。法师 1341、盗贼 ~1050、萨满 ~2000、坦克 ~800。
6. ~~查 30k+ 践踏成因~~ 已查（守卫破甲）；~~坦克圣佑 + 治疗预盾~~ 已做，坦克践踏均值 16.9k→9.2k、最大 31.6k→11.4k（第十轮）。
7. 共享层三处已提交（`4511b131`），AOE 两项已跑（第二十轮，待回归后提交）。末段墙 = 牧师 215–260 s OOM + 守卫/毒疗者打非坦克。
   候选（按预期收益）：(a) 坦克对"正在打非坦克的小怪"主动嘲讽（现在只嘲讽当前目标，守卫开在非坦克身上 52/110 只、2–10 秒才转）；
   (b) 英勇开怪即放；(c) 牧师痛苦压制接践踏（圣佑冷却中）；(d) 保距候选半径 24。
8. 守卫优先（破甲来源、也是缩短潜地的召唤物）仍是最有潜力的一项，需要单独设计与量化。
2. 西沿掉落：先在 AN 层禁掉 `combat formation move`（Multiplier，有先例），单变量跑 5 场看掉落人次。
3. 坦克被践踏 29.5k 秒杀（469/3）：查该次命中的 debuff/暴击。

本副本这边不要再动目标选择/仇恨（已有四条反证），也不要再在践踏上花时间（不是瓶颈）。
本副本这边不要再动目标选择/仇恨（已有四条反证），也不要再在践踏上花时间（不是瓶颈）。
