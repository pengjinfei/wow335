# 英雄艾卓-尼鲁布 / 阿努巴拉克（29120，map 601）

## 接手摘要

- 更新：2026-09-12 深夜第三轮（run449–run456）。累计 **22 场 4 击杀（约 18%）**，
  击杀耗时稳定在 **249–264 秒**。
- **本轮最重要的产出不是击杀率，是定位了一个共享层缺陷**：
  [bot 移动时放弃一切读条法术](../../DEFECT-CAST-WHILE-MOVING.md)。
  牧师有读条的治疗 **92% 被移动打掉**，2.5 秒的强效治疗 110 次尝试 0 次成功，
  一半以上 GCD 在甩魔杖。这解释了「坦克被爆发打死 → 全队崩」这条主线。
- 唯一下一步：**去修那个共享层缺陷**（单开一轮，见该文档的修复方向与回归口径）。
  本副本这边不要再改目标选择/仇恨，本轮又新增了一条反证。
- 阻塞：无。

## ⚠️ 本轮我判断错两次，先写在前面

1. **把践踏当成瓶颈**。做了闪避才发现它两场只触发一次——`ANPOUND` 逐 tick 轨迹显示
   非坦克与锥轴的最小夹角是 25° / 89° / 108°，**根本不在 ±12° 的锥里**。
   践踏不是瓶颈。
2. **把潜地空转的收益估成 90 秒**。核对召唤时刻表后是错的：50% 段最后一只毒疗者
   **29 秒**才进场，按实测每只 12–13 秒，理论最快 42 秒清完、47 秒出土，
   对比上限 60 秒，**每段天花板只有 13 秒**，两段合计约 25 秒。

教训写在最后一节。

## ⚠️ 先纠正上一轮记错的结论

上一轮（run430）我把死因记成「戒律牧师开场把 GCD 花在 1 小时团队 buff 上」。**这是错的**，
那只是次要现象，且只出现在团灭之后的场次（团灭抹掉 buff、框架开怪前不补，bot 只能在战斗中补）。

真实死因是**场景坐标错误**，下面有完整证据链。

## 根因：远程被关在 boss 房门外，治疗因此选不到坦克

阿努巴拉克房间有三扇门 192396/192397/192398（`gameobject_template.type = 0` 实体门、
`Data0 = 1` 初始开启、尺寸 3.4–3.75），位于 **y ≈ 252.2–256.2**；实例脚本把它们登记为
`DOOR_TYPE_ROOM`，`boss_anubarak.cpp` 的 `EVENT_CLOSE_DOORS` 在开怪 **5 秒后**关闭。

boss 在 y = 248.3，即门的**南侧**。而我把准备点 (551, 280) 与开怪点 (551, 260) 都放在了
门的**北侧**。于是每场都是：坦克和盗贼进门内打 boss，治疗/法师/萨满留在门外 —— 门一关，
**三个远程与坦克之间没有视线**。

治疗侧的连锁反应（`PartyMemberToHeal.cpp:135` 的 `IsWithinLOSInMap`）：
坦克被过滤掉 → 队伍里唯一「可治疗」的只剩牧师自己 → 所有治疗动作退化成对自己施放 →
自己满血 → 全部 `IMPOSSIBLE`。

**逐 tick 轨迹实证**（run443，`LogInGroupOnly = 0`）：

```
target is immuned to spell - target name: Raidtebnfivc, spellid: 48066, bot name: Raidtebnfivc
Raidtebnfivc A:power word: shield - IMPOSSIBLE
Raidtebnfivc A:power word: shield on party - IMPOSSIBLE
```

（48066 是真言术：盾，撞的是牧师自己身上的虚弱灵魂。冷却类失败同样全是自己：
强效自愈 10060 ×35、愈合祈祷 48113 ×12、苦修 53007 ×7。）

采样证据（run443 a1）：牧师 `in_combat=true`、`hp=16500/16500`，
**法力一路从 15786 回到 16283**（= 完全没施法），从 10.7 秒到 102.5 秒空转 **92 秒**；
同期坦克从满血被磨到 0、**55.2 秒阵亡，全程一次治疗都没吃到**。
按角色分：坦克与盗贼（近战）全程活跃，治疗/法师/萨满三个远程在 20–80 秒几乎零施法。
萨满图腾落点（带 `dst=`）显示它整场停在 y ≈ 278–280，牧师位置采样固定在 (551.00, 280.00, 224.33)。

## ⚠️ 产生这个坐标的方法错误：`raidtest los` 的 z 用法

`raidtest los <map> <x1> <y1> <z1> <x2> <y2> <z2>` 里：

- `vmap_floor_from/to` 是从 **z+2 向下搜 50 码**找地面 —— 传偏高的 z 只是搜索起点，没问题；
- 但 **`los` 是从 `(x1,y1,z1+2)` 到 `(x2,y2,z2+2)` 量的**。

我勘测时传了 z1 = 240 而地面在 ≈ 223，等于**在离地 18 码处量视线**，墙沿、坑沿全越过去了，
整批 `los=true` 都是假的。按它选的第二版坐标 (551, 224) 用实测地面 221.65 复量是 `los=false`，
5/5 场 `pull_rejected`（日志：`los=false distance=12.88 leader_pos=(551,224,221.65)`）。

**正确做法是两步**：第一遍随便传个高 z 拿 `vmap_floor_from`，
第二遍**用拿到的地面高度当 z1** 重新量 los，只信第二遍。

另外，`raidtest los` 是**静态 vmap 探针，看不到战斗中才关上的门**。
boss 带 `DOOR_TYPE_ROOM` 门时，准备点与开怪点必须都在 boss 那一侧。

## 可复现基线

| 项 | 值 |
|---|---|
| 场景 | `heroic-an-anubarak-n5`（完整遭遇战，无前置怪） |
| map / boss | 601 / 29120（英雄 entry 31610） |
| 难度 / 人数 | heroic / 5，normal5-v1，`BotCheats` 空 |
| 盗贼专精 | 刺杀 |
| **准备点** | **(527, 248, 224.35)** — 距 boss 24.0 码（> 22 码仇恨半径），门内侧，以 224.35 为 z1 实测 `los=true` |
| **开怪点** | **(543, 248, 223.44)** — 距 boss 16.0 码，地面高度实测 `los=true`，同在门内侧 |
| TimeoutSeconds | 480 |
| run | **445** |

内圈地面 y 240–251 约为 x 527–575，向南收窄；x ≤ 523 没有地面。
**门内侧距 boss 最远就是 24 码**（东西两翼 y = 248/251），没有更远的合法点。

## 尝试记录（run 445）

| 场 | 结果 | 用时 | 死亡 | boss 最低 | 盾 | 苦修 | 愈合祈祷 | 牧师总施法 |
|---|---|---|---|---|---|---|---|---|
| 1 | 作废 | 157.7 秒 | 0 | 42% | 19 | 4 | 35 | 249 |
| **2** | **击杀** | **249.9 秒** | **0** | **0%** | 35 | 6 | 46 | 399 |
| 3 | 团灭 | 229.4 秒 | 5 | 20% | 33 | 10 | 41 | 400 |
| 4 | 团灭 | 127.6 秒 | 5 | 54% | 10 | 2 | 6 | 124 |
| 5 | 超时 | 480.0 秒 | 4 | 17% | 25 | 7 | 40 | 323 |

对照修正前（run443 a1，治疗空转那场）：牧师**全场只有 29 次施法**。

a1 的 `boss lost combat state (stuck/reset)` 值得注意：`boss_anubarak.cpp` 的
`SummonedCreatureEvade` 会在**任何一只召唤怪 evade 时直接 `EnterEvadeMode()`**，整场重置。
建场景时就记过这个易碎点，a1 应当就是它。

## 第三轮（run449–run456）：践踏机制被查清，两条改动被证伪并回退

### 基线表

| run | 改动 | 击杀 | 各场 boss 最低血量 | 均时长 |
|---|---|---|---|---|
| 449 | 穿刺闪避（上一轮结论） | 1/5 | 0 / 46 / 21 / 73 / 18% | 337s |
| 450 | 践踏「沿半径后撤 25 码」 | 0/3 | 72 / 87 / 37% | 313s |
| 451 | 践踏「横向侧移出锥」 | 0/5 | 6 / 73 / 24 / 10 / 21% | 295s |
| 452 | 同 451（诊断构建） | 2/2 | 0 / 0 | 258s |
| 453 | 同 451（复跑） | 1/5 | 46 / 19 / 19 / 14 / **0** | 211s |
| 454 | + DPS 集火大召唤物（无坦克接怪） | **0/5** | 65 / 48 / 45 / 43 / 71% | 213s |
| 455 | + 两段（坦克先接 + DPS 集火已接住的） | **0/5** | 13 / 21 / 14 / 24 / 20% | 244s |
| 456 | 回退 454/455，单场诊断 | 1/1 | **0** | 258s |

451 / 452 / 453 是同一份代码，击杀率 0/5、2/2、1/5 —— **这个副本的单次 5 场样本噪声极大，
不要用一个 5 场 run 判断一条改动的好坏。**

### ✅ 践踏的真实机制（上一轮和本轮前半段都搞错了）

`DoCast(target, SPELL_POUND)`，英雄版 **59433**：

- `EffectImplicitTargetA = 24` → `SpellInfo.cpp:237` 是 **`TARGET_UNIT_CONE_ENEMY_24`**
  （CONE / DIR_FRONT），**不是圆形区域**
- `Spell.cpp:1251` 对该目标类型写死 `cone_degrees = 24`，`Position::HasInArc` 取半角
  → **±12°**；`EffectRadiusIndex` 给出锥长 **15 码**
- boss 在 `DoCast` **之前**先 `SELF_ROOT` + `DisableRotate(3300ms)`，读条期间锥向锁死
- `spell_azjol_nerub_pound` 对每个命中者补 59432，**满额不分摊**（BasePoints 47,124）
- `IsWithinBoundaryRadius` 不是漏洞：玩家 `UNIT_FIELD_BOUNDINGRADIUS` ≈ 0.4，
  取 `max(…, MIN_MELEE_REACH = 2.0)`，只有站在 boss 原点 2 码内才无视角度

实测单次伤害：坦克 15,551–19,973（扛得住），布甲 27,502 / 32,603 / 35,953 / 37,154 /
40,664（约 2.5 倍血量上限，必死）。

### ❌ 践踏「沿半径后撤」——方向错误，已改（run450）

原实现 `Move(bot->GetAngle(boss), distance + 2)` 是朝 boss 跑、穿到背后 2 码。
我先按「圆形 AoE」把它改成朝反方向撤到 25 码 —— **正好是沿锥轴直线逃跑，全程待在锥里**，
要跑满 15 码才出得去。三场 7 次重击、单次最高 39,254，比不动还差；第 1 场 22.3 秒
一发同时打中三人（23,663 / 37,540 / 39,254），81 秒团灭。

### ⚠️ 践踏「横向侧移」——几何正确，但实战几乎不触发（保留）

改成垂直于 boss→自己连线横移：夹角直接加 `atan(L/d)`，在 d 码处只要 `d·tan(12°) ≈ 0.21d`
就出锥，取 `max(6, 0.45d)`，且只有真在锥内（`HasInArc` + 15 码内）的非坦克才动。

**但逐 tick 轨迹证明它是个近乎空操作**：run452 两场（都击杀）133 个读条 tick 里，
`ANPOUND exec` 只有 **1 次**（`moved=1`，成功）。

| bot | 坦克 | ticks | 离 boss 距离 | 与锥轴最小夹角 | 在锥内的 tick |
|---|---|---|---|---|---|
| Raidteanfivc | 是 | 30 | 8.2–12.0 | **0.0°** | 30 |
| Raidtebnfivc | 否 | 32 | 12.2–20.7 | 25.0° | 0 |
| Raidtecnfivc | 否 | 31 | 4.9–12.3 | 108.0° | 0 |
| Raidtednfivc | 否 | 22 | 12.2–20.6 | 89.2° | 0 |
| Raidteenfivc | 否 | 17 | 5.8–17.9 | 0.0° | 1 |

boss 永远面向坦克，±12° 太窄，其余人天然站在锥外。**践踏不是这个 boss 的瓶颈。**
这条改动保留，因为它是对真实机制的几何修正（旧实现 52 次零成功），且无害；
但**不要指望它提高击杀率**。

### ❌ 潜地期集火大召唤物——两种形状都失败，已全部回退（**别重走**）

机制本身是真的：`boss_anubarak.cpp:267` `SummonedCreatureDies` 只统计
守卫 **29216** / 毒疗者 **29217**，`_remainingLargeSummonsBeforeEmerge` 归零就把
`EVENT_EMERGE` 从 60 秒改成 5 秒（开头 `if (!me->HasUnitFlag(UNIT_FLAG_NOT_SELECTABLE)) return;`
—— 出土后再杀**不计数**）。英雄三段分别要杀 2 / 4 / 4 只。

22 场潜地停滞实测：75% 段 20–45 秒（**杀掉了**），50% 段 51–66 秒、25% 段 38–63 秒
（**都吃满 60 秒**）。一场约 145 秒 boss 不可攻击。

**run454（只让 DPS 集火）**：0/5，五场全部活不过第二段潜地（最好才 43%），
首段停滞中位数从 ~31 秒涨到 ~49 秒。承伤归属（每场均值）：

| 来源 | run453（无集火） | run454（DPS 集火） |
|---|---|---|
| Anub'arak | 123,640 | 100,988 |
| **守卫 29216** | 92,497 | **111,070（跃居第一）** |
| 毒疗者 29217 | 78,415 | 48,339 |

**run455（两段：坦克先接 + DPS 只打已被坦克接住的）**：0/5，但危害消除
（boss 最低血量均值从 54.4% 回到 18.4%，是所有非击杀 run 里最深的）。
**危害没了，收益也没兑现** —— 见上面第 2 条判断错误，天花板本来就只有 25 秒。

两条都已 `git checkout` 回退，只留践踏侧移。

**教训（与上一轮的转火反证叠加）**：这个 boss 的小怪目标选择已经被证伪**三次**
（run446 全队转火、run447 坦克主导、run454 潜地 DPS 集火），第四次（run455 两段）
虽然不再有害但也无收益。**不要再动这个副本的目标选择和仇恨。**

### 📌 召唤物不会在出土时消失

`EVENT_EMERGE` 只做 `RemoveAurasDueToSpell` + 去掉不可选中标记，**不清召唤物**；
`summons.DespawnAll()` 只在 `EnterEvadeMode` 里。实测每场生成 6 只守卫 + 2–4 只毒疗者，
180 秒后仍有约 4 只在持续输出。这是后期承伤越滚越大的结构性原因。

### 🔴 真正的瓶颈：共享层治疗缺陷

完整证据链见 [DEFECT-CAST-WHILE-MOVING.md](../../DEFECT-CAST-WHILE-MOVING.md)。摘要：

- `PlayerbotAI.cpp:3946`：bot 移动时，有读条的法术被直接取消返回失败，
  `bot->StopMoving()` 被注释掉了
- 单场 258 秒里，有读条的治疗 **11 次成功 / 101 次 FAILED**；强效治疗 **110 次尝试 0 次成功**
- 只有 37% 的 GCD 用在治疗上，魔杖甩了 23 次
- 4/5 场团灭是**坦克先倒**，治疗死在坦克之后 —— 治疗阵亡是结果不是起因

### 一并记下但未修的脚本缺陷

`boss_anubarak.cpp:297` 的 `if (!UpdateVictim()) return;` 排在 `events.Update(diff)`
之前：仇恨持有者死光后 `EVENT_EMERGE` 永不触发，boss 永久卡在水下直到脱战重置。
run451 seq5 就是这么废掉的（boss 21% 血、只死 1 人，被判
`boss lost combat state (stuck/reset)`）。不是击杀阻塞项，但会污染样本。

## 下一步

**去修 [共享层治疗缺陷](../../DEFECT-CAST-WHILE-MOVING.md)**，单开一轮上下文。
本副本这边不要再动目标选择/仇恨（已有四条反证），也不要再在践踏上花时间（不是瓶颈）。
