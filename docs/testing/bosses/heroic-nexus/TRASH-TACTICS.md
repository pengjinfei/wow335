# 英雄魔枢：清怪阶段的三个战术假设

更新：2026-09-11（第二轮，见末尾「假设 3 重测」）。目的是**别重走**。三条里：

- **假设 1（打断治疗）与假设 2（优先击杀治疗）是真的被实测推翻**，且都做到了
  「机制确实生效」再判优劣，不是没跑起来。
- **假设 3（控制治疗）的测量无效**——变形术那一半是我的实现有缺陷（读条自我顶掉），
  妖术那一半反而完全正常。**「控制无效」没有被证实**，需要用纯瞬发版重测。

## 背景：为什么会去动清怪

泰蕾斯特拉与奥莫洛克的完整链路里，各有一组 4 只等级 80 精英的前置怪。
boss 段本身没问题（分别 2/2、6/6），但**清怪阶段减员会作废整场 attempt**：
泰蕾斯特拉 12 场里 3 场、奥莫洛克 8 场里 2 场卡在这里。

这两组不是能 A 掉的普通怪，带治疗和法系（实测施法次数，run354/357/367/368 合计）：

| 小怪 | type | 关键技能 |
|---|---|---|
| `Mage Hunter Ascendant` 26727/30460 | 7 人型 | 寒冰箭 ×17、冰锥术 ×3、火球 ×3、奥术/冰冻陷阱 ×5、急速光环，**还会对我们的 bot 放变形术 ×6** |
| `Mage Hunter Initiate` 26728/30478 | 7 人型 | **恢复(25058) ×46**、法力燃烧 ×30、法力吸取 ×21 |
| `Steward` 26729/30485 | 7 人型 | 奥术洪流 ×19、法术破除 ×22 |
| `Crystalline Tender` 28231/30525 | 7 人型 | **宁静(57054) ×22**（引导群疗）、坚韧皮肤 ×11 |
| `Crystalline Keeper` 26782/30526 | **4 元素** | 水晶打击 ×41（**这组的主要伤害源**，run343 承伤 288,315） |

牧师法力吃紧不只是「bot 不喝水」那个缺陷，还有小怪 51 次法力燃烧/吸取在抽。

## 假设 1：打断治疗 —— 机制本来就有，但这两个治疗打不断

**不是「上游没给盗贼和萨满」**（这是本轮一度写错的结论，`grep` 加了 `head` 被截断）。
实际上法师、圣骑、盗贼、萨满、战士、DK、牧师、德鲁伊**都有** `"<spell> on enemy healer"`
触发器，且挂在常驻策略里：`DpsRogueStrategy.cpp:176`、`GenericShamanStrategy.cpp:119`、
`GenericMageStrategy.cpp:126`、`GenericPaladinStrategy.cpp:21`。
`EnemyHealerTargetValue` 遍历 `attackers`、跳过当前目标、专挑正在读/引导**有益法术**的那个，
**对生物同样有效**。

用 `interrupt_watch` 只读采样（mod-raidtest `9c16bb7`）直接量：

- bot 侧全部正常：`picks` 命中治疗、`known=true`、`cd=false`、`in_attackers=true`、
  `interruptable=true`、`dist=2–9` 码对 `spell_range=28.5`。
- 而且**真的打了**：run370 三场里有 **9 次**脚踢/风剪/法术反制直接打在两只 Tender 身上，
  其中 **6 次在宁静的引导窗口内**。
- **`cast_cancel` 仍然是 0。**

原因在核心 `SpellEffects.cpp:3768-3772`：`EffectInterruptCast` 对引导类要求
`PreventionType == SPELL_PREVENTION_TYPE_SILENCE` **且** `ChannelInterruptFlags & INTERRUPT`。
宁静(57054) 的这两个标志位在客户端 DBC 里（`acore_world.spell_dbc` 只有 4492 行覆盖数据，
不含它），SQL 查不到，但行为已经把结论钉死。
另一边 `Mage Hunter Initiate` 的恢复(25058) 是**瞬发**，定义上无法打断。

免疫也排除了：`CreatureImmunitiesId` 查下来 Tender/Keeper 是 0，两只 Mage Hunter 是
**-12 = 只免疫魅惑**，没有任何一只免疫打断。

**结论：这条路走不通，且不是 bot 的问题。别再试打断这两组的治疗。**

## 假设 2：优先击杀治疗 —— 机制生效，结果更差

在 `src/Ai/Dungeon/Nex/` 加了「转火治疗小怪」的规则（trigger + action + 压住
`dps assist` 的乘子），**已回退，未提交**。

| | 规则前 | 规则后 |
|---|---|---|
| 泰蕾斯特拉 | 12 场 8 击杀；清怪 27.8 / 30.3 / 32.5 / 33.1 / 33.3 / 43.5 秒 | run372 5 场 **2 击杀 / 3 场清怪减员**；清怪 **42.0 / 40.9** 秒 |
| 奥莫洛克 | 8 场 6 击杀；清怪均值 45.4 秒 | run371 5 场 **3 击杀**；清怪均值 **52.3** 秒 |

机制确实生效（run372 每场两只 Initiate 先死、run371 每场两只 Tender 先死，规则前是混着死）。

**当初的推断错在因果方向**：观察到「奶妈先死的那几场清怪快」就当成了因果，实际是
**打得顺 → 奶妈碰巧先死**，不是先杀奶妈 → 打得顺。

**失败机制很具体**：规则后 abort 的场次每场死 **2–3 人**，规则前只死 1 人。
规则把 DPS 锁到一只**坦克没在抓的**怪身上（乘子还压住了 `dps assist` 不让它们回到坦克目标），
那只怪就转头打 DPS。**「让 DPS 去点非坦克目标」在 4 只精英的场子里，代价是丢掉坦克保护。**
另外奥莫洛克那组的伤害源是 Keeper（288k）而不是 Tender，先杀奶妈等于让伤害源多活十几秒。

## 假设 3：控制治疗（法师变形术 + 萨满妖术）—— **测量无效，结论不成立**

> **2026-09-11 更正**：这一节最初写的是「机制生效但中性到更差」。复查施法间隔后发现
> **变形术那一半的测量是无效的——实现有缺陷**，不是战术的问题。妖术那一半则完全正常。
> 原始的 4/5 与 1/5 数字保留在下面，但**不得当作「控制无效」的证据**。

怪物类型决定了能用什么（`creature_template.type`）：人型的四种都能羊/妖术/闷棍/致盲，
**Crystalline Keeper 是元素，只能致盲**。角色侧：法师会变形术全 4 阶、盗贼会潜行/闷棍/
致盲/凿击、**萨满会妖术 51514**（妖术在 playerbots 里没有注册过任何动作，但
`CastSpell` 是按名字在运行时查技能的，副本层可以自建动作，不必改萨满的职业文件）。

实现（**已回退，未提交**）：副本层自带目标取值的 CC 动作（绕开共享 `CcTargetValue`
那条「AoE 团里的怪不 CC」——那条对普通小怪是**正确默认**，羊一只正在被 AoE 的怪等于倒忙），
外加一个按动作名压制 AoE 的乘子。

| | 规则前 | 加控制后 |
|---|---|---|
| 奥莫洛克 | 8 场 6 击杀；清怪均值 45.4 秒 | run373 4 击杀 / 1 abort；清怪 51.0 / 55.9 / 55.9 / 60.9 秒 |
| 泰蕾斯特拉 | 12 场 8 击杀；5 场共 9 死 | run374 1 击杀 / 4 abort；5 场共 9 死 |

### 妖术（瞬发）：按预期工作

run373 五场各 **恰好 1 次**（14.5–18.5 秒，目标固定 guid 10），之后「已被控住就跳过」的
判据正确跳过它，没有任何重复施放。**1 次 GCD 换一只治疗出局，成本可忽略。**

### 变形术（1.4 秒读条）：实现缺陷导致的空转，数据作废

- 78 次施放，**间隔恒定 1394–1413 毫秒**（= 急速后的读条时间），且反复打同一只 guid。
  119 个间隔里 90 个 < 2.5 秒。
- `cast_cancel` 只有 12 条、**全部 `by_self=false`**，`cast_ms=1091/1316/1418`（半截被顶掉）。
- 即**羊从未成功上身**，法师陷在「读条 → 被下一次下发顶掉 → 重读」的循环里空转。

根因是**我写的动作缺少保护**：用的是裸 `Action`，`isUseful()` 只判了职业，绕过了上游
`CastSpellAction` 那条 `CanCastSpell`（它会在已有读条时返回 false）。引擎每 tick 重新下发，
`Spell::prepare` 就把上一次读条打断。**与「羊持续时间只有多久」无关——变形术 4 阶是 50 秒。**

### 因此正确的读法

- **「控制治疗净负面」这个结论不成立**，需要用修好的实现重测。
- 值得优先测的是**纯瞬发方案（只用妖术）**：不可能出现读条自我取消，实测成本是每场 1 次 GCD。
- AoE 压制不完整这条**仍然成立**（见下面的优化项），只是它不是本轮变形术空转的原因。

## 假设 3 第三轮（run391/392）：加上变形术，拿到了 E 失效的决定性证据

变形术是唯一**确实带 `AURA_INTERRUPT_FLAG_TAKE_DAMAGE`** 的候选（从客户端 `Spell.dbc` 直接
解出：变形术 4 阶 12826 的 `AuraInterruptFlags = 0x80002`，妖术 51514 是 `0x80000`
——只有 `CHANGE_MAP` 一位）。所以它是唯一能真正触发 E 的控制。

### 决定性的一次：羊上身 2 毫秒就被自家奉献打掉

run392 / attempt 3 是十场里唯一一次变形术真正落地（`miss=0`）：

| 时刻 | 事件 |
|---|---|
| 14.443s | 圣骑 796 施放**奉献(48819)**，`dst=(518.35,102.99,-16.12)` —— 地面 AoE 已经铺下 |
| 14.637s | 法师 799 的**变形术(12826)** 落在 guid 98 上，`miss=0` |
| **14.639s** | guid 98 被 796 打掉 **51 点** —— 奉献的一跳 |
| 之后 | `cc_watch` **一次都没采到这个羊**（1 Hz 采样，下一格在 15.0s） |

**羊的存活时间 < 2 毫秒。**

### 所以 E 的设计本身不成立，必须成对做

`CrowdControlProtectionMultiplier` 压的是「**要不要发起一次新的 AoE 施法**」。但是：

- **已经铺在地上的持续性 AoE（奉献、暴风雪）不会因为 E 而停**，它们每跳都照常打；
- 于是只要控制落进一块**已经生效的 AoE 区域**，它在下一跳（最坏 2 毫秒）就没了，
  E 根本没有任何介入的机会。

**因此要更正本文档上一版的一个判断。** 上游 `CcTargetValue` 里那条
「不要 CC 已经在 AoE 团里的怪」，我此前写的是「对普通小怪是正确默认，但守卫组 4 只叠在
一起，这条会把治疗全部排除掉」，并在副本层自建取值绕开了它。
**这个判断是错的——那条排除是承重的。** 正确的形状是**一对**：

1. **控制侧**：CC 目标必须落在当前生效的 AoE 落点之外（= 上游那条排除的本意）；
2. **输出侧**：新的 AoE 不要打到被控住的目标（= E 做的事）。

只做第 2 条（本轮），控制会被存量 AoE 秒破；只做第 1 条（上游现状），在 4 只叠在一起的
包里就等于没有可控目标。两条都做才有意义，而这需要 AoE 落点与半径进入选目标的判据里——
正是下面「遗留优化项 1」描述的那个跨副本能力。

### 另一个实测限制：变形术几乎放不出来

run391 + run392 共 10 场，变形术**只成功施放 1 次**。原因分两层：

- **选不出目标**：判据排除了坦克的目标、自己的当前目标、以及已被控住的怪。
  守卫组里符合条件的治疗只有两只 `Mage Hunter Initiate`，妖术先占掉一只之后，
  另一只通常正是坦克或法师自己的目标 —— 于是没有可选目标。
  `Spell` 对象在 10 场里只被构造了 20 次（`Auras.log` 的
  `GetSpellForDifficultyFromSpell: ... 12826` 计数），即触发器本身极少活跃。
- **少数有目标时也常常发不出去**：`Playerbots.log`（本轮临时把
  `AiPlayerbot.LogInGroupOnly` 置 0 才能看到）里**没有任何 `CanCastSpell Check Failed`**，
  说明 `CanCastSpell` 通过了；但 `Spell::prepare: spell id 12826` 只出现过 1 次。
  丢失发生在 `PlayerbotAI::CastSpell` 内部，它在 `prepare` 之前只有三个不打日志的早退：
  飞行状态、起身延迟、以及 **`bot->isMoving() && spell->GetCastTime()`**。
  对一个 1.7 秒读条、且在不断走位的法师，最后一条是最可能的，但**未证实**。

### 妖术早掉的根因仍未定位

`Auras.log` 给到了移除模式 = `AURA_REMOVE_BY_DEFAULT`，且发生在**暴风雪(42938) 对 4 个
目标结算伤害的中途**，卸掉的正是 Hex 的两个效果（aura type 56 `TRANSFORM` +
60 `MOD_PACIFY_SILENCE`）。但妖术没有 `TAKE_DAMAGE` 标志位，而且在 run390/attempt2 里
它扛住了 5.2s、7.7s 两次伤害才在 8.1–9.1s 之间消失——**「任何伤害都打破」解释不了**。
已排除：递减抗性（`max_ms=30000`）、死亡、采样中断、小怪驱散（Spellbreaker/Arcane Torrent
的施放时刻逐场比对过，没有一次落在消失窗口内）、`acore_world.spell_dbc` 覆盖（无该行）、
`AuraInterruptFlags` 路径（唯一置位的 `CHANGE_MAP` 在战斗中不可能触发）。
**下一步只能是在 `Unit::_UnapplyAura` 临时打印调用方**。

### 三轮合计的清怪战绩：控制是中性的

| | 基线（无控制规则） | run390 妖术 | run391 妖术+羊 | run392 妖术+羊 |
|---|---|---|---|---|
| 到达 boss 的场次 | 9/12 | **5/5** | 3/5（2 场 group lost，共 3 死） | **5/5** |
| 清怪时长均值 | 33.4 秒 | 33.7 秒 | 36.6 秒 | 35.2 秒 |

三轮合计 **13/15** 对基线 **9/12**，时长 33.4 → 35.0 秒。**都在噪声范围内**：
以现在这个「只做输出侧」的半套实现，控制既没帮上忙也没帮倒忙。

## 遗留的两个优化项（都未实现，且都是跨副本的通用能力）

1. **AoE 会打破自家控制，框架层没有任何结构支撑。** 这不是核心不支持，是 mod-playerbots
   缺两样东西：(a) AoE 动作**没有公共基类或标记**——它们由 `"light aoe"` / `"medium aoe"`
   这类触发器驱动、各职业各写各的（法师暴风雪/烈焰风暴/冰锥术/冲击波/龙息、盗贼刀扇/
   剑刃乱舞、萨满闪电链/火焰新星/岩浆图腾/雷暴、圣骑奉献/神圣风暴/正义之锤、牧师神圣新星/
   心灵灼烧），所以没法 `dynamic_cast` 一把拦住，只能按动作名列表压，压不全（本轮就漏了
   刀扇和圣骑的 AoE）；(b) AoE 的**目标/落点选择里没有「被控住的单位」这个概念**，
   共享 `CcTargetValue` 只做了反向的事（不去 CC 已经在 AoE 团里的怪）。
   正确的形状应该是上游加一个 AoE 动作基类/标记 + 一对共享判据：**「AoE 半径内有被控单位
   就让路」**（输出侧）**与「不要把 CC 放进已经生效的 AoE 落点」**（控制侧）。
   run392 证明只做输出侧那一半是不够的——已经铺在地上的奉献/暴风雪不会因此停下，
   羊落进去 2 毫秒就没了。这是**跨副本、跨职业的通用能力**，也确实是机器人"智能度"的体现，
   但改动面大，不该塞进某个副本的策略里。
   本轮已实现的 `CrowdControlProtectionMultiplier`（mod-playerbots `0cccd22c`）是输出侧
   那一半的可用原型，判据与核心 `Unit::HasBreakableByDamageCrowdControlAura()` 同源；
   但要注意**那个判据本身会漏判**：妖术实测会被打断而该函数报 false。
2. **控制类动作缺少「已在读条」保护。** 本轮自建的 CC 动作因为绕过 `CastSpellAction`
   的 `CanCastSpell` 而自我顶掉读条（见上）。任何新的读条类动作都要么继承
   `CastSpellAction` 系列，要么自己判 `bot->IsNonMeleeSpellCasted(...)`。

## 当前结论

**基线（不加任何清怪战术规则）是实测最好的已验证配置**，回退后已复验：
run375 奥莫洛克 4 场 3 击杀，击杀 104–127 秒零死亡，与 6/8 基线一致。

清怪阶段减员这个瓶颈**仍然存在**。假设 1、2 是真的堵死了；**假设 3 只是测坏了，没有被证伪**。
按优先级排的后续方向（**都未验证**）：

- **只用妖术的纯瞬发版控制**（假设 3 的正确重测，成本实测每场 1 次 GCD）；
- 让坦克先抓稳全部 4 只再放 DPS（针对假设 2 暴露的「非坦克目标」失败模式）；
- 治疗侧：小怪 51 次法力燃烧/吸取对戒律牧的影响没单独量过；
- 装备档位本身——这两组是 4 只等级 80 精英对 ilvl 187 五人，可能就是这个档位的真实难度，
  而不是策略问题。

## 假设 3 重测（run390，2026-09-11）：实现修好了，但妖术自己撑不住

### 先建了一个能单独量清怪段的测试床

新场景 `heroic-nexus-telestra-trash`（mod-raidtest `f0dad96`）：前置 = 泰蕾斯特拉那
**4 只站桩守卫**，`BossEntry` 指向被冰冻牢笼锁着的凯利丝塔萨，而且**拉怪点与准备点
取同一坐标**（清怪点本身）。于是队伍全程不移动到任何 boss 附近，清完后那次拉怪以
`pull_rejected(distance=239.5 los=false)` 干净收场，每场 ≈ 清怪 + 短恢复。

**这是与上一版 `heroic-nexus-ormorok-trash` 的唯一实质差别，那版是错的**：它把拉怪点
放在凯利丝塔萨房间，队伍被拖向奥莫洛克平台，run388/389 每一场**奥莫洛克本人都造成
12–43k 伤害**，两个 A/B 组全部作废。该场景已删除。

### 实现缺陷确实修掉了

副本层动作改为继承 `CastSpellAction`（mod-playerbots `0cccd22c`），`isPossible()` 因此
走上游 `CanCastSpell`，已有读条时返回 false。run390 五场：

- **每场恰好 1 次妖术**，`miss=0`，无任何重复施放 —— run374 那个「每 1.4 秒顶掉自己
  读条、78 次零成功」的循环不再出现。
- 顺带纠正一条旧记录：**妖术不是瞬发**，实测读条 1082–1407 ms。正是因为有读条，
  才必须要这条保护。

### 但妖术在这组怪里只撑 1–4 秒

`cc_watch` 采样（1 Hz，State 事件不经 `ShouldKeep` 过滤，不会被丢）：

| attempt | 施放 | 最后一次采到 | 下一秒 | 采到时剩余 | 怪 |
|---|---|---|---|---|---|
| 1 | 10.00s | 11.05s | 消失 | 28.95s | 活着 |
| 2 | 5.11s | 8.12s | 消失 | 26.99s | 活着（91.8% 血） |
| 3 | 16.04s | 17.34s | 消失 | 28.70s | 活着 |
| 4 | 8.44s | 9.50s | 消失 | 28.92s | 活着 |
| 5 | 8.68s | **一次都没采到** | — | — | 活着 |

满额时长 `max_ms=30000`，所以**不是递减抗性**。采样器在同一场后续仍在工作
（还采到了冰霜新星、肾击），所以**不是采样停了**。怪在 29–34 秒才死，所以**不是死亡**。

**也不是被小怪驱散**：Steward(26729) 的 `Spellbreaker(47780)` / `Arcane Torrent(47779)`
施放时刻全部拿出来比对过，**五场里没有任何一次落在妖术消失的那个 1 秒窗口内**
（例如 attempt 2 妖术 8.12s 还在，最近的 Spellbreaker 在 7.02s；attempt 4 妖术
9.50s 还在，最近的在 5.74s 与 15.96s）。

**根因未定位。** 已排除：递减抗性、死亡、采样中断、小怪驱散、伤害打破
（妖术的 `AuraInterruptFlags` 不含 `AURA_INTERRUPT_FLAG_TAKE_DAMAGE`，
`cc_watch` 直接读到 `breakable=false`，而核心里判「伤害能否打破控制」的官方函数
`Unit::HasBreakableByDamageCrowdControlAura()`（`Unit.cpp:949-970`）用的正是这个标志位）。
定位这条需要给 51514 的 `AuraRemoveMode` 加一行临时日志。

### 另一条待查：被妖术控住的小怪仍在施法

attempt 2 里 `cc_watch` 在 8.118s 还显示妖术在身，而 **8.566s 这只 26728 施放了
`Drain Mana(17682)`**。妖术带 `MOD_PACIFY_SILENCE`，本不该能施法。
怀疑 SmartAI 的 `SMART_ACTION_CAST` 绕过了沉默/缴械判定，**未定位**。
若成立，则「控住治疗」在这组怪里连治疗都停不下来。

### 所以 E（AoE 给控制让路）**这一轮仍然没有被验证**

`CrowdControlProtectionMultiplier` 只保护「受伤即掉」的控制，而本轮场上出现的每一个
控制光环（妖术、肾击、Crystal Freeze）`breakable` 全是 `false`
—— **它没有任何东西可保护，全程返回 1.0，没压制过任何一次 AoE**。
要验证 E 必须用一个**确实会被伤害打破**的控制，首选**法师变形术**
（`MOD_CONFUSE + TRANSFORM`，带 `TAKE_DAMAGE`）；现在自建动作已继承 `CastSpellAction`，
run374 那个读条自我取消的结构性缺陷不会再出现。

### 清怪战绩：与基线持平，减员为零

| | 基线（无任何控制规则） | run390（妖术 + E） |
|---|---|---|
| 清怪时长 | 27.8 / 30.3 / 32.5 / 33.1 / 33.3 / 43.5 秒 | 34.0 / 33.9 / 35.0 / 35.6 / 29.9 秒（均值 33.7） |
| 清怪阶段减员 | 12 场里 3 场 | **5 场里 0 场，零死亡** |

时长基本没变（均值 33.4 → 33.7）。0/5 减员比历史 3/12 好，但**样本太小，不构成结论**
（Fisher 精确检验 p ≈ 0.5）。代价侧是明确的：每场 1 次 GCD，不重复施放。

## 复现所需

- 只读采样 `interrupt_watch` 已提交（mod-raidtest `9c16bb7`），跑任何带前置怪的场景即产出。
- 证据：本地 MySQL `raidtest_events`，run370（采样）、371/372（假设 2）、373/374（假设 3）、
  375（回退复验）。
- 假设 2 的实现已回退。**假设 3 的实现已提交**：mod-playerbots `0cccd22c`
  （`CrowdControlProtectionMultiplier` + 魔枢副本层 `trash healer hex`），
  mod-raidtest `f0dad96`（`cc_watch` 采样 + `heroic-nexus-telestra-trash` 场景）。
- 第二轮证据：run390（纯清怪场景 5 场）；作废的 run388/389 见上文说明。
