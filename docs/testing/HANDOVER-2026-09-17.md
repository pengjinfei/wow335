# 交接：英雄古达克一轮（2026-09-17）

这一轮从「选下一个副本」开始，到**一个 boss 通关、斯拉德兰十刀攻坚后又完成一次合并后复核、两个 boss 阻塞、一个 boss 未覆盖**为止。
**下一个副本开新会话，先读 [START-HERE](../START-HERE.md) 和 [LESSONS](LESSONS.md)，本文件只记这一轮。**

## 一、结果

副本：**英雄古达克（Gundrak，map 604）**，装备档 normal5-v1（ilvl 上限 187），
难度英雄，无 cheat、无难度切换、无换装，口径均为**隔离 boss 战**（不是连续通关一遍副本）。

| # | boss | 场景 | 结果 | Wilson 95% CI | 判定 |
|---|---|---|---|---|---|
| 1 | 毒蛇领主斯拉德兰 29304 | `heroic-gd-sladran-disc-n5` | 合并后 **3/5**；平台阵位 **0/5**（回退） | 0–43%（平台组） | 不稳定，平台阵位否决 |
| 2 | 莫拉比 29305 | `heroic-gd-moorabi-n5` | **0 个有效样本** | — | 场景阻塞，未测 |
| 3 | 德拉克瑞巨像 29307 | **未建** | — | — | 框架阻塞，未测 |
| 4 | 迦尔达拉 29306 | `heroic-gd-galdarah-disc-n5` | **10/10 零死亡**，64.2–111.9 秒 | **72–100%** | **正常规则通关** |
| 5 | 凶残的艾克 29932（英雄限定） | **未建** | — | — | **完全未覆盖，连勘测都没做** |

**副本级：5 个 boss 里只有 1 个确定按当前口径打通。** 斯拉德兰合并后 3/5 尚不能宣称稳定；随后平台阵位 0/5 已回退；两个是我们自己的阻塞（测不了），一个没碰。

### 斯拉德兰平台阵位复测（run 630–631，已回退）

NGA 的「坦克把 boss 拉到平台下，远程站平台」被实现为一个独立的 playerbot 阵位动作：坦克点
`(1775,653,124.48)`、远程/治疗点 `(1775,675,129.22)`；静态 `raidtest los` 实测二者 22 码且
LOS=true。毒新星避让与 Snake Wrap 处理均保持更高优先级。编译通过后，在完全相同的 Heroic /
normal5-v1 / 无 cheat / `heroic-gd-sladran-disc-n5` 口径下获得五个有效样本：

| run / seq | 结果 | 用时 | 最低 HP |
|---|---:|---:|---:|
| 630 / 1 | wipe | 88.413s | 12% |
| 631 / 1 | wipe | 109.822s | 7% |
| 631 / 2 | wipe | 90.113s | 15% |
| 631 / 3 | wipe | 72.472s | 35% |
| 631 / 4 | wipe | 95.857s | 21% |

run 631 / 5 因 5 个专用测试账号再次撞 `AccountInstancesPerHour` 而传送前 abort，**不计样本**。
平台组的平均 Grip / Snake Wrap / 蛇对队伍伤害是 **66.4 / 7.0 / 82.5k**；合并后基线 run 628–629
分别是 **54.2 / 4.4 / 61.3k**。因此该阵位没有减压，反而提高蛇压力；源码和二进制都已回退到
合并后基线。下一步若继续斯拉德兰，先补位置/决策日志，再考虑把毒性新星自保作为另一个单变量。

⚠ **迦尔达拉那 10/10 带一层口径折扣**：夹具移除了竞技场四组载具犀牛，但
`FixtureDespawnSpawns` **删不掉载具乘客**，骑手仍在场且 run 580 里 2/5 场参战。
10/10 = run 580 五场（改动前二进制）+ run 626 五场（四项共享层改动落地后复测，无回退）。

## 二、这一轮改了什么

### mod-playerbots（分支 `codex/gd-sladran-aoe`，**有 6 个未推送提交**）

斯拉德兰共**十刀**，只有两刀留下了东西：

| 刀 | 改动 | 结果 |
|---|---|---|
| 第一刀 | 删掉 `SladranMultiplier` 里「DPS 全程禁 AoE」 | **有效**：0/5 → 2/10。上游自己写着 "Will re-test in heroic"，英雄从没测过 |
| 第七刀 | 防骑奉献相关性 `ACTION_HIGH+7` → 52（`39ac1a7f`） | **有效**：0/5 → 3/19（CI 6–38%，Fisher p=1.000）。共享层回归 9/9 零死亡 |
| 第二刀 | 删掉「有包裹时禁 `dps assist`」 | 已试 **已回退**：包裹被打死 3.9 → 0.8 只/场 |
| 第三刀 | 按优先级选包裹（`8ebd5cd4`） | **未证实有效，保留** |
| 第四刀 | 打通保护链（`PartyMemberToProtect` 的 `return nullptr;` + 骑士圣术名 TBC 残留） | 未测出效果，**保留**（修的是真缺陷） |
| 第五刀 | DPS 目标固定回 boss | 未证实有效，**保留** |
| 第六刀 | 法师镜像触发器名 `high threat` → `medium threat` | 机制生效未跨击杀线，**保留** |
| 第八刀 | 神圣之锤高相关性节点 | 已试 **已回退**：和奉献抢同一个 GCD，把奉献挤晚 11% |
| 第九刀 | 开场英勇/嗜血 rush | 已试 **已回退**：存活 −24%，0/5。新增的共享 `CombatOpeningTrigger` **保留但未在任何策略注册** |
| 第十刀 | 红蛇优先（小怪优先级） | 已试 **整刀回退**（`bcd130eb`，`git diff` 对基线为空） |

**当前二进制 = 上游同步后重新配置并全量构建的 `worldserver`（2026-09-17）。**
2026-09-17 随后已将 mod-playerbots `origin/master` 合入源码分支（`f0e08d97`）：采用上游
Slad'ran 的包裹分工/收拢/坦克驻留实现，并保留本地「DPS 回 boss、包裹优先」触发器；已重新构建并以
run 628–629 验证：同一隔离档有效 **3/5**（65.764s kill、94.721s wipe 14%、67.831s kill、
99.283s wipe 15%、80.565s kill）。run 627 是帐号实例频率上限导致的传送前 abort，不计样本。
这个结果验证的是合并组合，不是任一单点改动的因果。

### mod-raidtest（分支 `codex/dtk-scenarios`，**已推送，无未提交改动**）

`556c118` 等：五个古达克场景模板（`conf/mod-raidtest-scenario-heroic-gd-*.conf.dist`）。
五个模板与 `env/dist/etc/modules/` 的运行配置**已逐个 diff 核对一致**（2026-09-17 收尾时把
`galdarah-n5` 运行配置里已证伪的旧注释同步掉了；设置本来就相同）。

### core（azerothcore-wotlk）

本轮**一行未改**，仍在 `codex/an-formation-despawn-crash` / `fa702e6a5`。

## 三、运行状态（接手先核对，别当作自动成立）

- 二进制 = `var/build/obj/src/server/apps/worldserver`，**已是第十刀回退后重新编译的版本**。
- worldserver 以 `scripts/restart_world.sh r77a` 启动（脚本在**仓库根的 `scripts/`**，
  不在 `azerothcore-wotlk/scripts/`），日志 `/tmp/wow335-worldserver-r77a.log`，收尾时 state=IDLE。
- `AiPlayerbot.LogInGroupOnly = 1`。
- **源码同步后的未推送状态**：core `codex/an-formation-despawn-crash` @ `c747f55ca`
  有 70 个提交待推 `mine`（其中包含 `origin/Playerbot` 的 69 个提交和 merge）；
  mod-playerbots `codex/gd-sladran-aoe` @ `f0e08d97` 有 31 个提交待推 `mine`
  （包含 `origin/master` 的 24 个提交和 merge）；mod-raidtest 已同步。管理库的 docs 提交待推。
- **主干回灌（随后完成）**：core 的 `main` 已快进至 `c747f55ca` 并推送 `mine/main`；
  mod-playerbots 的 `main` 已快进至 `f0e08d97` 并推送 `mine/main`。这两个 GitHub fork 的默认分支
  已统一为 `main`，旧 `Playerbot` / `master` 保留。mod-raidtest 的本地 `main` 已快进至 `556c118`；
  其远端尚无 `main`，待明确确认目标后创建并推送。
- `env/dist/etc/modules/` 下五个古达克场景运行配置**只存在于磁盘**（gitignored），
  换机器要从 `.conf.dist` 手工拷一遍；`raidtest_accounts` 里五个 scenario_key 各 5 个槽位都在。

## 四、下一步的三个候选（按代价排序）

**接手分支（2026-09-17）**：从统一后的各模块 `main` 创建本地
`codex/gd-takeover`（mod-playerbots 与 mod-raidtest）。创建时无源码、配置或二进制改动；
core 保持 `main`，因为古达克目前没有 core 改动需求。

1. **莫拉比（最便宜，不用编译）**：boss 在队伍 39 码外 3–10 秒自己进战斗，5/5 场作废。
   只有 1/5 能用法师闪现解释。**先加只读探针记录是谁把 boss 拉进战斗**，再改坐标。一次重启。
2. **凶残的艾克（未覆盖，先勘测）**：英雄限定 boss（29932，81 级），这一轮压根没碰。
   建场景走 [准备点四关](LESSONS.md)，注意 `raidtest los` 的两遍法要**逐点**做。
3. **斯拉德兰（先复测，不要立刻动治疗侧）**：合并后的 3/5 与历史 3/19 差异很大，先用同场景再跑
   一组独立 5 场；如需归因，临时打开决策日志量化包裹分工/收拢/坦克驻留。不要把本轮结果误写成某一行代码
   已被单独证实，也不要在未经对照前动治疗侧。
4. **德拉克瑞巨像（新功能，口径要先确认）**：boss 开场 `NON_ATTACKABLE`，
   要先杀 5 只**召唤**出来的 Living Mojo，而 `PrerequisiteSpawns` 只接 spawn guid。
   需要 mod-raidtest 支持「按 entry 声明/移除召唤物」。

## 五、这一轮值得记住的两件事

**（一）第十刀否掉的是一整类改动，比它本身的成败更值钱。**

参照打法说「绿蛇优先」（缠绕是硬控）；我量完经济性认为该「红蛇优先」
（单只红蛇打出 3,521、绿蛇 907，血量同为 6,517；每 1 点输出换回的「少挨伤害」红蛇 0.54、绿蛇 0.14）。
**两种主张错在同一个前提上**——都假设「我方输出打在哪种怪身上」由目标选择决定。

|  | 绿:红 **数量**比 | 绿:红 **输出**比 |
|---|---|---|
| 基线 19 场 | 2.77 | **2.73** |
| 加优先级节点 5 场 | 2.85 | **2.62** |
| 再修一次 5 场 | 2.87 | **2.70** |

输出比紧贴数量比，优先级节点毫无影响——分配是 **AoE** 打出来的。
**判据已进 LESSONS**：改任何「先打谁」之前，先量「各类目标承受的我方输出比」
对「各类目标在场只数比」。贴合 ⇒ 目标选择不是杠杆，这一刀不用做。这个比值**免费**。

**（二）我在这一轮犯了两个口径错误，都记下来了。**

- **跨场景对比**：第十刀第一次冒烟跑在 `heroic-gd-sladran-n5`、基线在 `heroic-gd-sladran-disc-n5`，
  据此得出的「输出速率 ↓65%」是无效数字，已撤回（`8445f7d`）。
  跑 A/B 前**先核对 `scenario_key`**。
- **「boss 召的小怪一只都没死」是错的**：`CombatEventBus::ShouldKeep` 只对
  `IsMember(source)` 保留 Death 事件，召唤物的死亡从来不记录。
  改用「累计 bot 伤害 ÷ 模板血量」反推，实际打死了 16–42%。

另外，**LOS 探针的 z 用法这一轮是第三次踩**：批量网格给了统一 z1=129.3，
而下沉走廊地面在 124.5/123.4，「两条走廊对 boss 可见」全是假的。
**两遍法必须逐点做**（第二遍每个点用它自己的 `vmap_floor_from`）。
