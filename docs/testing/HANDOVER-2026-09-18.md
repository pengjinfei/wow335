# 交接：英雄古达克 / 巨像隔离基线（2026-09-18）

## 已完成

- 德拉克瑞巨像（29307）新增场景 `heroic-gd-colossus-n5`，并在 Heroic / normal5-v1 /
  `BotCheats=""` 下完成隔离 boss 战基线：run651–652 合计 **5/5 击杀、0 死亡、64.374–68.137 秒**
  （平均 66.373 秒；Wilson 95% CI 57–100%）。
- 每场开战前夹具均在 0ms 移除四只常驻 Living Mojo（127076–127079）和两只编队
  Drakkari Golem（127080–127081）；boss 自身 Reset 时召出的五只临时 Mojo 未受影响。
- `EngageTrigger=summon` 只会选择 `GetSummonerGUID()==bossGuid` 的临时 Mojo。坦克对其执行
  原生 `AttackAction` 后，由 boss 脚本完成 Mojo 合并、全队进战和约 3.5 秒后的解冻；框架不注入
  boss 仇恨或战斗引用。
- 空载 worldserver 的 20 个 tick 只有数十毫秒，已改为最多等待 6 秒真实时间确认原生开战；run647–648
  是这个时序问题的 aborted 诊断，不计 boss 样本。
- 莫拉比的北侧准备点 `(1772,875,124.44)` 已排除清怪阶段的 proximity aggro，但 10 个 boss 样本仍是
  8 kill / 1 aborted / 1 timeout。`CanNotReachTarget` 的现象尚未归因；若继续，只在复现样本中重新接入
  只读 `movement.chase` 分支日志，不能把 run646 的成功当成归因证据。

## 代码与配置

- `azerothcore-wotlk/modules/mod-raidtest`：`codex/gd-takeover`。
  - 通用场景键：`EngageTrigger=summon`、`SummonTriggerEntry`。
  - 召唤物开战的真实时间确认、临时 Mojo 只读状态采样、前置清怪 boss proximity/威胁只读探针。
  - 新增 `conf/mod-raidtest-scenario-heroic-gd-colossus-n5.conf.dist`；运行时副本在
    `azerothcore-wotlk/env/dist/etc/modules/`（gitignored），其有效键已与 `.conf.dist` 核对一致。
- core `main` 与 mod-playerbots `codex/gd-takeover` 没有待提交源码改动。core 目录中的
  `*.log`、`raidtest-rosters/`、`raidtest-scenes/` 是运行生成物，保持不跟踪。

## 验证证据

- 已完成增量构建：`MTHREADS=4 cmake --build azerothcore-wotlk/var/build/obj --target worldserver -j4`。
- 已在本地 auth + world + MySQL 实际运行 run651–652。run652 的四个 attempt ID 为
  1788427798–1788427801；每个都有六条 0ms `fixture_despawn` 事件、均为 kill / 0 deaths。
- 运行结束后 worldserver 已正常停止，测试账号未删除；仅重置账号 52–56 的副本限额。

## 口径与下一步

巨像结论是**隔离 boss 战基线通过**，不是连续清完整副本：房间路怪与 boss 编队附属物由夹具移除，故不能与
run649–650 的混合样本合算。若继续巨像，应另建“完整房间”口径，不能调装备、难度、cheat 或伪造 boss 仇恨。

古达克下一项推荐是新建凶残的艾克勘测/场景；继续莫拉比或斯拉德兰前，先遵循
[START-HERE](../START-HERE.md) 中的只读取证条件。
