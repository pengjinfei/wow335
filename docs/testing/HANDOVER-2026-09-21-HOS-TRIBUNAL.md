# 交接：英雄岩石大厅 / Tribunal 首次完成（2026-09-21）

## 当前证据

- 诊断运行环境已就绪（2026-09-21）：隔离 `worldserver` 使用项目 `env/dist` 的编译时配置根，已受控替换 r32 并确认 ready/IDLE；日志确认加载 `heroic-hos-tribunal-event-n5`。二进制仅含三处 tank-assist telemetry。排除 lifecycle 分母的 run752/attempt `1788427975` execution smoke 已终态：前置 141.567 秒、真实 gossip 0/1 163.285/187.472 秒，436.028 秒动态 wipe、5 deaths、boss_hp_min=100%。两类 `tribunal-tank-assist*` telemetry 均为 0 行，表明这场未覆盖 run751 的空目标接管条件，不可用于候选漏纳/调度竞争/LOS 三分或策略结论。隔离 server 已受控停止，r32 已确认 ready/IDLE。后验源码检查显示三处 telemetry 用 `LOG_DEBUG("playerbots", ...)`，而本次默认日志未输出该级别；0 行是不可见日志级别，不是条件未触发。已仅将隔离 telemetry 的两个实际日志点由 `LOG_DEBUG` 提升至 `LOG_INFO`（头文件没有日志点）；`git diff --check` 和隔离 `worldserver -j4` 重建均通过。已受控替换 r32，隔离 INFO telemetry server 已 ready/IDLE 并加载 Tribunal scenario；已启动唯一一次 INFO execution smoke run753/attempt `1788427976`，仍不改行为或 lifecycle 分母；30 秒仍 prerequisites/hp_min=100%，且日志已验证 telemetry 可见：`attackers=3`/`active=true` 后 action 选 27970/27971、`los=true result=true`。有界 60 秒后前置 160.191 秒完成、真实 gossip 0/1 166.826/191.008 秒；telemetry 累计 247 trigger/12 action 行，含 4 条 `current=0`。随后按行号与 phase 关联，动态 gossip 后 2855–2857 为 `attackers=3,current=0,active=true` 两 tick，紧接 action `target=27983,los=true,result=true`，之后 current=27983。这证明既有 tank-assist 在空当前目标时能调度、选中且 LOS 成功，故不支持恒定 trigger/action 竞争或 LOS 失败解释 run751 的 23 秒缺口；空分支的 `tank_target=0` 是刻意不读该 value 的诊断口径，不能当候选遗漏。run753 终态为 440.359 秒动态 wipe、5 deaths、boss_hp_min=100%，继续排除 lifecycle 分母；共 597 trigger/77 action 行与 7 次 positive-attacker empty-current active 记录。空当前目标 handoff 能立即选中并 Attack 成功，完成诊断目标：run751 的 23.082 秒首伤→接战缺口不是恒定 `TankAssist` 候选/调度/LOS 管道断路；但一次成功 handoff 不解释 run751 偶发长缺口。隔离 server 已停止；r32 已确认 ready/IDLE。原 module worktree 的 11 个未提交外来文件保持未触碰，诊断改动没有回灌。只读 DB 关联复核 run751：204/205/206 于 401.045/401.065/402.572 秒死亡；821 402.654 秒已 victim=0/select=206，403.660 秒起 engine=1/select=0，吻合长缺口起点。为使诊断可对齐 DB `rel_ms`，隔离 worktree 新增纯观测锚点：CombatEventBus 记录实际 `_attemptStart` 的 `app_ms`，tank-assist 日志记录同一 `steady_clock - GetApplicationStartTime()` 的 `getMSTime()`；差值即 attempt `rel_ms`（毫秒截断）。隔离 `worldserver -j4`/diff-check 通过；run754/attempt `1788427977` 已实际输出 anchor `app_ms=44958`，所以 instrumentation 执行已验收；但它 300.006 秒前置 timeout 终态（2 roster deaths、hp_min=100%、无 gossip/动态），明确排除 lifecycle 且无可比 handoff。诊断 server 已停、r32 已恢复 ready/IDLE。run754 对 run753 的只读前置对照无策略变量：同一 `126746` Lightning Construct (low 8) 在 run753 102.124 秒死亡、全员存活且 160.191 秒完成前置；run754 该目标接敌先降至 2/5、两 roster 死亡、重试降为 1/3 且持续 LOS reject。该“相同 binary”前提经审计已推翻：run753 用先前诊断 source；run754–756 的隔离 source 改指向 clean `mod-raidtest` HEAD，漏掉 active r32 module 工作树809行未提交的 script-spawn Tribunal orchestration（Brann延迟出现、preclear boss absence、follower hold/auto-engage guard）。故r754–756三种前置异常不能归为随机暴露，也不能与run753/r32作策略/telemetry对照。第二场隔离 smoke run755/attempt `1788427978` 也实测输出 anchor `app_ms=33042`，但 204.147 秒为 `pull failed (boss not engaged)` 前置终态（1 roster death、hp_min=100%、无 gossip/动态），明确排除 lifecycle、仍无 handoff 可量。连续两场 time-aligned diagnostics 均被既有前置路径截断（run754=300.006 秒 clearing timeout；run755=末只 low-10 LOS/pull failed）；不得重复同一 smoke 碰运气或归因 telemetry/Tribunal。诊断 server 已停、r32 已恢复 ready/IDLE。调度层只读排除了 minimal-mode 饥饿：`tank assist` relevance=50，`Engine::DoNextAction(minimal=true)` 会跳过 <100；但 `PlayerbotAI::AllowActive()` 对 dungeon/raid 与 in-combat 无条件 true，map-599 的821不会因随机 activity throttling 进入 minimal tick。故 run751 23秒 gap 不能归因该 low-relevance skip。更关键：run751 403.660–440.736 秒每秒均 `in_combat=true, engine=1`；`DoNextAction()` 的 noncombat→combat 同步唯一看 `attackers` 非空，未切换可推知这些检查的 `AttackersValue` 为空。run753 直接记录 attackerCount=3 后即时 handoff。静态追踪收敛到明确 gate：member 的 ThreatenedByMeList 仅 member→attacker 小于 sightDistance 时加入；`RemoveNonThreating()` 随后以 `IsValidTarget(unit, bot)` 过滤，后者强制 **tank→attacker `IsWithinLOSInMap`**。进一步由 core threat-manager/event 排除前门：AddThreat 会将 creature reference 写到 victim 的 ThreatenedByMeList；221于425.530秒伤822、822于424.957秒先伤221，故已有combat/threat relation及组内tap；821→822约29码、221→822约18码，均低于sightDistance。余下合理空化 gate 收敛为**821基准的 CanSeeOrDetect/IsWithinLOSInMap**（均在IsValidTarget）；DB无LOS/visibility，不能判时变原因或放宽LOS。关键修正：LOS在AttackersValue而非仅AttackAction，可解释engine不切换。为只观测该残余gate，隔离 `AttackersValue.cpp` 的既有拒绝分支新增 map-599/in-combat tank `tribunal-attackers-reject` INFO（同app_ms、entry、CanSeeOrDetect、LOS）；仅 target 已入集合却被IsValidTarget拒绝时输出，不变返回/筛选。隔离 diff-check 与 worldserver -j4 已过；全树codestyle仍只见既有core告警。这项精确观测已作为新变量受控启动一次、明确排除 lifecycle 的 diagnostic：run756/attempt `1788427979`，正常规则/无行为改动，anchor `app_ms=33493` 已实测输出。run756 未进动态：64.585秒 `prerequisite_invalid: boss missing or engaged before clearing completed`（0 roster death、hp_min=100%；Brann low-98已有victim/threat），明确排除 lifecycle；rejection telemetry 无run751-shaped输出、无策略结论。三次新诊断均在不同前置异常截断，按约定停止 diagnostics/不重试。诊断server已停、r32已恢复ready/IDLE；仅隔离telemetry保留。只读diff确认 clean diagnostic harness 漏 active r32 的 script-spawn preclear guards，正可解释run756 Brann过早engagement；三场不再用于任何bot行为判断。provenance核实 r32 binary 含active harness独有 `script_spawned` reset 与 `could not hold followers before script boss appearance` literals，clean diagnostic binary不含后者，确认r32用active module。为同harness且playerbots telemetry仍隔离，已仅将isolated core source的mod-raidtest symlink改回active r32 module worktree（不改其16项外来变更）；mod-playerbots仍指isolated telemetry worktree。首次常规worldserver -j4未重编译已替换symlink后的raidtest objects（active source时间早于旧objects，binary仍缺follower-hold literal），故仅在隔离build artifact上 `--clean-first -j4` 强制重建；600秒上限仍编译、被工具终止，isolated diagnostic binary已按clean-first移除，未可执行。r32未受影响且ready/IDLE。cycle35续建由19%至55%后到窗口上限；cycle36继续隔离`-j4`并100%成功链接worldserver（仅既有`PartyMemberValue.h` missing-override warnings）。provenance验收：raidtest symlink=active r32 module、playerbots symlink=isolated telemetry；binary同时有active harness `script_spawned` reset、`could not hold followers before script boss appearance`和三条isolated `tribunal-*-` telemetry literals；AttemptRunner/AttackersValue objects及binary均为本次重建、两relevant diff-check通过。随后受控replacement/startup gate：原r32 binary先保存至`/tmp/wow335-worldserver-r32-before-active-harness-diag-cycle37`，替换诊断binary并由restart脚本停旧server；diagnostic server未留存进程、启动log为空，立即复制backup并重启r32。r32再次ready/IDLE。未替换r32下诊断binary `--dry-run` 约12秒、playerbots config加载中 SIGSEGV(11)/rc=139（末尾`Playerbots: ListSpellsAct...`）；`--help` rc=0，非Mach-O/配置路径无法启动。无crash report堆栈。startup gate失败，绝不执行Tribunal observation run。cycle38只读比较发现isolated playerbots虽同HEAD却只有3处base telemetry、漏active r32既有8个HoS/ReachTarget paths（51 additions/3 deletions），违反保持r32行为基线，可能影响运行一致性但无证据为config-load SIGSEGV根因。已仅复制这8个既有r32文件至isolated（原11 paths未触）；isolated现12 paths=active11项行为基线+三处observation telemetry，diff-check过。cycle39以`-j4`重编译修正，仅重编译8个HoS/ReachTarget关联objects并成功链接；新binary仍有active follower-hold和`tribunal-attackers-reject` literals。受控替换启动gate通过：PID87469、playerbots config14.127秒、`ListSpellsAction caches initialized`、world ready，且加载`heroic-hos-tribunal-event-n5`；FIFO=IDLE。此对照表明补齐r32 playerbots行为基线后SIGSEGV不再出现；单次修复不宣称精确因果，但binary已满足startup gate（不外推为Tribunal策略结论）。当前是verified isolated observation binary、正常规则。cycle40已启动唯一active-harness observation-only diagnostic：run757/attempt`1788427980`（`--attempts 1`，排除lifecycle分母）。有界60秒仍prerequisites、elapsed=64.383秒/hp_min=100%/无终态；tank-assist INFO可见（attacker=1/current=27971），亦见leader对Lightning Construct两条`could not initiate attack ... no LOS/invalid target`。cycle41再有界60秒：run757/attempt`1788427980`仍active，前置179.606秒/recovery3.006秒，真实script gossip0/1=182.612/206.821秒，observing elapsed=223.223秒/hp_min=100%。`HoldFollowerAttackTagged`实际held4 followers，验证本次用active r32 script-spawn harness；仍不作lifecycle。cycle42再有界60秒仍observing（elapsed=383.308秒/hp_min=100%），但有效active-harness动态样本再覆盖空-current handoff：app_ms=553608 `attackers=3,current=0,active=true`，同毫秒action=27983/los=true/result=true，521ms后current=27983。与run753一致、未复现run751长gap，只再排除恒定tank-assist/action/LOS管道故障，不能解释偶发gap。cycle43有界终态：run757/attempt`1788427980`=479.629秒动态wipe/5 deaths/boss_hp_min=100%，仍排除lifecycle；完整log归档`/tmp/wow335-worldserver-hos-tribunal-active-harness-diag-cycle39-run757-terminal.log`，原r32已恢复ready/IDLE。active-harness telemetry=766 trigger/98 action/55 rejection、10次positive-attacker empty-current active；55个已收集且real-threat target reject全为`visible=true,los=false`（非visibility），含27972及后期27984/27985。首次直接实测tank-relative LOS可使AttackersValue删除候选；但active r32 raidtest无此前clean-harness CombatEventBus app_ms anchor，不能精确对齐run rel_ms/证明是run751实例。与即时handoff并存，仅证明LOS过滤是已观测间歇候选空化机制，不能放宽LOS。cycle44归档行序关联：preclear 27972 LOS rejects(app_ms274530–281786)全在`prerequisites_complete`前；gossip1后有动态rejections。关键app_ms=506435同一world tick先三条27983 `visible=true,los=false` reject，后同tick`attackers=2,current=0,active=true`；action仍从别的eligible target立即handoff。active harness无attempt-start app_ms，不能转精确rel_ms/称为run751空-Attackers engine gap；但直接证明LOS过滤与tank空current可同tick共存。不能绕开/放宽LOS。cycle1静态追踪修正“不同来源”假设：TankAssistAction固定`tank target`；TankTargetValue smart strategy经TargetValue::FindTarget**直接读GuidVector `attackers`**。故app_ms=506435选27984不是绕开AttackersValue，而是27983 LOS剔除、27984留在同一vector，严格证明该tick`current=0`≠空Attackers。TankAssistTrigger仅以另一`attacker count`正值+空current激活，不能代替engine-sync attackers空化证据。run751 engine=1的403.660–440.736推断仍限其独立每秒state。cycle2缓存时序：AttackersValue=`ObjectGuidListCalculatedValue(...,1*1000)`；CalculatedValue::Get最多每1000ms重算，其余同一cached vector。DoNextAction engine-sync直接读它；AttackerCountValue也读它、仅alive/distance计数。故run757 app_ms=506435 `attacker count=2`与action选27984同一非空snapshot，排除trigger另候选集。cache至多约1秒陈旧，不能单独解释run751 403.660–440.736约37秒engine=1；只能是每1000ms刷新持续算空。run751秒级state不能精确给每次刷新但相容。cycle3 hasRealThreat/LOS：RemoveNonThreating仅map不同、hasRealThreat或IsValidTarget删候选；hasRealThreat只是即时in-world/alive/非poly/非friendly、无缓存。run757 rejection日志守卫要求hasRealThreat=true，故55条不是此gate。run751 gap内821/822互伤至少排除822当时dead/out-world/friendly/poly，不能排其它候选。IsValidTarget=IsPossibleTarget&&IsWithinLOSInMap；core每次用双方hit-sphere/碰撞高调用Map::isInLineOfSight、无LOS cache。若37秒为LOS，须位置/几何/phase持续射线失败，不是旧值滞留。cycle4 DB position/spawn：run751 821在403.660/412.687/423.692/427.705/438.735/440.736/441.740均固定(929.03,361.04,203.78)；近战组近乎固定，远端822/824小幅移动。spawn原点相对821：210=41.122、215=42.720、221=40.997码，Z差约2.38；它们攻击远端而821无目标。确证gap内tank未用移动改LOS几何，但仅有敌spawn、无每秒敌位/LOS射线，不能由距离断LOS。run757 reject亦无双方坐标。cycle5仅在`/tmp/mod-playerbots-tribunal-diag`既有reject INFO扩纯观测字段：bot/unit XYZ+双方phase mask；守卫仍map599/in-combat tank/同map/hasRealThreat/已有target被拒绝，不改筛选/返回/策略，active r32未触。可直接分几何与phase、不再从spawn距离推断；isolated diff-check过。cycle6`-j4`增量重建，只编isolated AttackersValue.cpp并链接成功。provenance：links仍active r32 raidtest+isolated playerbots；binary有active follower-hold及扩展reject bot_pos/unit_pos/phase literal，fresh object/binary时间戳+两diff-check过。r32 IDLE，未替换/启动/run。cycle7 startup gate通过：r32先保存`/tmp/wow335-worldserver-r32-before-geometry-phase-diag-cycle7`，旧PID9884正常退出，isolated PID19220 RelWithDebInfo ready；playerbots config13.902秒/ListSpellsAction caches initialized，Tribunal scenario加载/FIFO=IDLE。当前verified geometry/phase observation binary、无行为改动。cycle8启动唯一geometry/phase observation：run758/attempt`1788427981`（`--attempts 1`，排除lifecycle）。有界60秒仍prerequisites/elapsed59.343秒/hp_min100%/无终态；tank-assist INFO可见、尚无rejection geometry可分析。cycle9有界60秒run758仍active且进动态：gossip1=211.298秒，observing elapsed232.814/hp_min100%。具有效geometry/phase采集条件，仍排除lifecycle/不预判。cycle10再有界60秒仍observing(elapsed393.362/hp_min100%)。本窗tank-assist稳定27983 current/tank target，多次los=true/result=true；末result=false同时current=target=27983且attacker=1，符合AttackAction已攻击同target拒绝，非LOS/候选结论。cycle11终态：run758/attempt`1788427981`=479.307秒动态wipe/5 deaths/boss_hp_min100%，排除lifecycle；log归档`/tmp/wow335-worldserver-hos-tribunal-geometry-phase-diag-cycle7-run758-terminal.log`，r32 backup恢复ready/IDLE。40 reject=38×27983(app_ms395753–571923)+2×27984，**全visible=true/los=false/phase=1:1**。27983三维距离82.54→32.22仍LOS false(27984=61.12)：排phase mismatch且非仅超距；map碰撞/几何射线可在同phase/32码持续挡tank候选。仅run758机制证据，无run751同tick坐标，不能称同实例/放宽LOS。诊断预算用完。cycle12静态：generic ReachTargetAction只跟current target；run751 Attackers空→current空，故不触发。`possible targets no los`100码收集可见有效但不需map LOS敌，可发现被AttackersValue删的27983/4/5；直接generic reach缺group安全。唯一候选为map599 tank LOS-reacquire：仅in-combat+Attackers空+current空、27983/4/5正攻击存活同组成员，从possible取最近；并要求敌距存活治疗≤heal range，才PathGenerator/ReachCombatTo至正常combat距离（不Attack/不改优先级/不放宽LOS）。这使run751 tank向治疗侧敌人回接，避run489远点追怪。cycle13注册/安全通过：ApplyInstanceStrategies(599)同时向combat/noncombat加wotlk-hos，覆盖run751 engine=1；HoS contexts/InitTriggers有直接模式。node用ACTION_RAID+5=65，低于Gaze emergency+10=100。ReachCombatTo已有same-map/CanMove/PathGenerator normal|incomplete|shortcut早退；新action仍显式拒stay，并trigger/action双层tank/combat/empty attackers+current/活同组victim/活healer范围/三entry，防possible no-los带未参战敌。cycle14实施唯一变量于active r32 HoS集合(7 paths)：TribunalLosReacquireTrigger/Action双层同一strict finder，仅tank/in-combat/not-stay/Attackers空/current空、27983/4/5攻击活同组且敌距活治疗≤heal range，从possible no-los选最近，ReachCombatTo；不Attack/不set target/不改优先级或LOS。contexts注册、strategy ACTION_RAID+5。MTHREADS=4 worldserver成功（HoS contexts/strategy/actions/triggers重编链接），diff-check过。cycle15 r34 los-reacquire受控restart：旧r32 PID40704正常退出，新PID63841 ready，playerbots config11.889秒/ListSpellsAction caches initialized，Tribunal scenario加载/FIFO IDLE。当前唯一行为变量r34；原r32 rollback仍`/tmp/wow335-worldserver-r32-before-geometry-phase-diag-cycle7`。cycle16启动唯一r34 execution smoke：run759/attempt`1788427982`（--attempts1，排除lifecycle）。有界60秒仍prerequisites/elapsed59.354/hp_min100%/无终态；仅既有4/5 engaged后party approach retry，尚不满足动态LOS-reacquire验收、不能读变量效果。cycle17有界60秒run759仍active且进动态，observing elapsed224.206/hp_min100%；具r34 execution验收条件但无逐trigger日志，不猜变量触发。cycle19终态：r34 run759/attempt`1788427982`=525.633秒kill/0 deaths，但预声明execution smoke、排除lifecycle，不改r32 1/5；log归档`/tmp/wow335-worldserver-hos-tribunal-r34-los-reacquire-cycle15-run759-terminal.log`，r32恢复ready/IDLE。DB强窗：391.766 821 combat/engine1/current0/victim0/moving=false→392.767 moving=true→393.767 combat engine/melee214；214/215=392.621生成，215=393.626伤821，tank(948.78,377.88)→(956.38,377.44)。新action唯一no-current ReachCombatTo HoS node且静态不Attack/target/LOS bypass；但DB无214 392–393 victim/action dispatch、default follow亦可动，故仅与触发一致，不能验strict finder；0死kill不归因。cycle20 active r34 action加唯一INFO dispatch telemetry：仅finder成功、既有ReachCombatTo后记录app_ms/target entry/victim low/最近活治疗距/调用前双方XYZ/moved返回，不影响finder/动作/返回/LOS。MTHREADS=4只重编HoSActions并链接成功，diff-check过。cycle21 literal/provenance后restart：旧r32 PID85103正常退出，新r34 telemetry PID96282 ready，ListSpellsAction caches initialized/Tribunal scenario/FIFO IDLE。当前r34+dispatch INFO，r32 rollback仍`/tmp/wow335-worldserver-r32-before-geometry-phase-diag-cycle7`。cycle21启动唯一排除lifecycle dispatch smoke run760/attempt`1788427983` (--attempts1)；10秒=ATTEMPT_RUNNING/prerequisites/9.338秒/hp_min100%，仅正常4/5 pull approach retry，不读效果。cycle22有界60秒后run760仍active：observing 169.566秒/hp_min100%；recovery_complete=27.185、真实gossip0=167.978，HoldFollowerAttackTagged held4，验active harness。刚动态，无dispatch INFO/效果结论，仍排除lifecycle。cycle23有界60秒后run760 observing=325.823秒/hp_min100%，gossip1=192.193。完整log `tribunal-los-reacquire`=0行，至此strict finder/action未dispatch（非日志级别不可见：binary literal+INFO已验）；无行非变量成败。本场排除lifecycle。cycle24终态：run760/attempt`1788427983`=470.622秒动态wipe/5 deaths/boss_hp_min100%，observer+orchestrator+IDLE确认；排除lifecycle，不改r32 1/5。log归档`/tmp/wow335-worldserver-hos-tribunal-r34-dispatch-telemetry-cycle21-run760-terminal.log`；全场dispatch INFO=0，strict finder/action未发生，run759移动不可归因r34、run760 wipe亦不可评价。rollback后r32 PID20931 ready/IDLE。r34候选只保留source待新证据受控验证，未验收/不得lifecycle。

- `heroic-hos-tribunal-event-n5` 的 r32/run745/seq1 已在正常规则下完成真实 Brann 事件：Heroic、normal5-v1、5 人、`BotCheats=""`、无 fixture、`PrerequisiteTimeoutSeconds=300`。
- 六个前置于 148.515 秒完成；真实 gossip phase 0/1 在 150.241 / 174.447 秒；Brann 存活；`EventCompletionBossState=2` 到达 DONE。
- runner 结果：**491.468 秒 kill、零玩家死亡**。event-bus 汇总的 `death=6` 是登记事件数；runner 明记 `deaths=0`，不得误读。
- 原始日志：`/tmp/wow335-worldserver-hos-tribunal-r32-restore.log`；阶段、完成条件及结果已同步写入 Tribunal README 和 BOSS-LEDGER，因此日志不是唯一证据。

## 判定与边界

r32 的同配置复验现为 **1/5 DONE**：run745 的零死完成，run746、run748、run750 与 run751 的有效动态全灭。run748 前置于 146.608 秒完成、gossip 0/1 于 146.609/170.909 秒，终态为 463.049 秒、5 roster deaths；它是有效 lifecycle 失败。该组推翻“当前必然在动态波次团灭”的暂时性描述，但 n=3 仍不能声明稳定通关、Tribunal 机制验收或英雄岩石大厅完成。run747 是前置减员后的 300 秒 clearing timeout、未进 gossip，单列且不入 1/3 分母。此前 run733–735 的 Gaze 修复后动态 wipe（0/3）仍是不同阶段背景，不与这组率混算。

## 运行与工作树

- worldserver r32 在运行，FIFO relay 单一读端正常；`AiPlayerbot.LogInGroupOnly=1`、`AutoEquipUpgradeLoot=0`、`BotCheats=""` 已核对。
- 代码工作树有既有未提交改动：mod-playerbots 的 HoS/GD 文件、mod-raidtest 的 HoS/GD/Eck 框架与场景文件；本轮未改源码、未编译、未提交或推送。
- 管理库本轮更新：`docs/testing/BOSS-LEDGER.md`、`docs/testing/bosses/heroic-hos/README.md`、`docs/testing/bosses/heroic-hos-tribunal/README.md`、本交接。

## 当前下一步

cycle 6 已复核运行中的 r32 worldserver：FIFO `raidtest status --json` 为 `idle=true`，无 active attempt；四库状态亦已复核，未触碰 core 的既有生成物或 module 的既有改动。r34 LOS-reacquire 的 execution smoke（run760）全场 dispatch=0，不能作为策略变量或 lifecycle 证据；r32 的 1/5 样本也尚未给出新的单一归因。cycle 8 还排除 Kaddrak：它每 1.5 秒随机直施的 Glare 在 r32 kill 与四场 wipe 均有 56.7–68.6k 总伤害，没有可由正常规则 bot 决策改变的入口；无专用 HoS node 不是策略缺陷证据。

cycle 9 只读形成唯一候选：Marnak 每30秒的 Dark Matter 28237 在5秒后向随机玩家的当时坐标移动，3–4.5秒后自爆；五场均有5.1–14.2k伤害，且 run746 有同次对两人非零命中。它有正常规则 bot 移动入口但尚无HoS node。下一步只能先做隔离、排除 lifecycle 的触发/移动/非零命中验收，不能把它与r34合入同一binary或直接跑生命周期。

故不以重复 lifecycle run 替代证据。保持 r32、同一场景/300 秒预算和 IDLE；任何候选须先经不计 lifecycle 的 execution smoke，且不得叠加策略或让 mod-raidtest 代打。

### 运行中状态（cycle 1）

- 已清 `account_instance_times` 与 map 599 instance（不改变规则），以正确 FIFO `/tmp/ac_world_fifo` 启动 run746/seq1；首次写到旧 `/tmp/wow335-cli.fifo` 没有送达，已通过 `raidtest status` 明确纠正，不能把该无效写入当运行。
- 启动后 2.470 秒状态确认：`ATTEMPT_RUNNING`、stage=`prerequisites`、`runId=746`、attempt `1788427969`、`hp_min=100%`。cycle 2 在 98.871 秒只读复核仍为同一运行中 attempt；当前碰到第 4 只 Lightning Construct (27972) 的反复 `pull_rejected`（目标 `valid=true/alive=true`，但 leader 距 68.9–131.2 码且 `los=false`）。cycle 3 作了一次有界 60 秒终态等待后，`finished_at` 仍为 NULL；日志已推进到第 6 只 Lightning Construct，177.023 秒仍在 prerequisites，leader 曾到 19.97 码但仍 `los=false`，故不是单纯距离不足。cycle 4 随后的只读状态已证明该接近没有永久阻断：第六只于 200.359 秒清完、gossip 0/1 于 200.360/224.548 秒到达；414.777 秒仍在 observing。cycle 5 终态 DB 确认 **522.820 秒 wipe、5 roster deaths**（五名 `death_names` 全列），无 DONE；它是有效动态失败，前置 LOS 抖动只影响时长，不能作为剔除理由。
- 作为不叠加变量的第三个同基线 sample，run747/seq1 已启动；2.435 秒 `raidtest status` 明确为 `ATTEMPT_RUNNING`、stage=`prerequisites`、attempt `1788427970`、`hp_min=100%`。cycle 6 作一次有界 60 秒终态等待后 `finished_at` 仍为 NULL；因此当时 `aborted/0` 只是运行中占位。cycle 7 终态为 **300.000 秒 `prerequisite_failed: clearing timeout`、1 death (`Qzozpvcnfive`)**，未进 gossip；不入 lifecycle 分母。末段第六只 27972 仍活、四名存活者反复 `los=false`。cycle 8 只读时间线校正：盗贼 818 的最后可见承伤早在 84.307/88.324 秒、来自 spawn 126746 的 27972（5,193/9,786）；末段 59038 是 spawn 126749 在自己 143.852 秒死亡前 1.208 秒触发的 `Just Died` Electrical Overload，不能是盗贼死因也不可打断。61528 的候选 interrupt 虽没有对应 spell action，仍不与死者窗口重合。**不得据此改 interrupt 策略**。
- 归因后已恢复同基线 lifecycle 复验：run748/seq1 在 2.500 秒由 `raidtest status` 确认为 `ATTEMPT_RUNNING`、stage=`prerequisites`、attempt `1788427971`、`hp_min=100%`。cycle 9 及 cycle 11 各作一次有界 60 秒终态等待后 `finished_at` 仍为 NULL，因此当时 `aborted/0` 只是运行中占位。cycle 12 终态为 **463.049 秒 wipe、5 roster deaths**；前置 146.608 秒完成，gossip 0/1 于 146.609/170.909 秒，故计为有效 lifecycle 失败。补充只读分层：动态玩家承伤 Protector 27983 = 279,191（最大；Stormcaller 67,192、Custodian 70,193、Gaze 10,082）；首个可见阵亡状态是 818 @ 433.460 秒，随后 820/817/816 @ 442.492/448.511/450.513，属级联形态。cycle 13 的 818 首死窗口 `[421.460,435.960]` 量到 Custodian 13,592 + Protector 4,494 + Stormcaller 1,397 + Kaddrak 609；818 从 10,689 至 4,361（431.450 秒）后死亡。817 已在 423.957/427.516/430.384 三次各治疗 818 2,849，但 430.981–432.642 的后续施法都落在同时低血的坦克 816；817 本身满血、法力约 2.5–4.5k/20k。这只证明一个有限的目标竞争窗口，不能从单场推定治疗策略缺陷或直接改优先级。cycle 14 的 run745 末 15 秒对照表明相同低血取舍在完成场亦存在：817 仅 154–715/16,708 法力仍先治 819、后治坦克 816，818 始终至少 16,089/16,644，且全团可计承伤仅 11,351。两窗压力不等，因此不选治疗变量。cycle 15 按 gossip 对齐 250.551–265.051 秒显示：run745 的 Protector 230/231 已于 425.050/426.013 秒死亡，下一组三只至 437.953 秒才生成；run748 的一个 Protector 于 421.790 秒死亡，但 227/228 至 428.897/431.561 秒才死，且坦克 816 始终选 Stormcaller 232/233、在 818 死后 431.450 秒才选 Protector 228。run748 该窗承伤为 Custodian 26,426、Protector 12,856、Stormcaller 7,412、Kaddrak 3,519、Gaze 1,396，而 run745 相应为 9,173/0/3,748/2,505/0。Protector 生存/选中次序是可检验 target-priority 假说，非治疗结论。cycle 16 的 run746 首死前亦复现：Protector 236–238 于 464.554 秒生成，816 467.871–471.882 秒短暂选它们、472.882–478.890 秒却回选 Stormcaller 243，237/238 仍活；818 于 478.890 秒首死。该窗 Protector roster 伤害 40,904，三只到 475.021/485.848/496.921 秒才死。两个有效 wipe 现支持一个 Protector target-priority 设计调查。cycle 17 已定位入口：无 RTI 时 `TankTargetValue::Calculate()` 调 `FindTankTargetSmartStrategy`，只比较未获仇恨、近战、威胁/距离；`tank assist` 与死后即时接管均读 `tank target`。最小变量可限定为该选择器在 map 599 对存活 27983 的排序优先（RTI 不覆盖、raidtest 不选目标、其他地图不变）。预先验收：任何存在可攻击 Protector 的连续动态窗口，tank-state `entry=27983` 快照率须 100% 至该批清空，同时量 spawn→death 延迟及 roster 承伤；不把单次 DONE 当策略判定。cycle 18 已确认最小接入不需新 HoS trigger/action（既有 HoS diff 仅为 Gaze 离点）：在 `TankTargetValue.cpp` fallback comparator 加局部 `map==599 && entry==27983` predicate 并优先比较即可；`CheckAttacker` 已获得 `FindTarget` 的有效 attacker，RTI 早退发生在 comparator 之前。cycle 19 已读 C++/build 规范并只实施此 comparator：`TankTargetValue.cpp` 增 map 599/entry 27983 early preference，RTI、raidtest、DPS selector、其他地图均未改。`cmake --build var/build/obj --target worldserver -j4` 成功并重新编译该 cpp；r33 已以 FIFO relay 受控重启（PID 25327、启动后 status=IDLE）。codestyle 全树扫描因既有 core 文件空行/qualifier/tab 问题失败，本文件未出现在报告中，且改前 diff-check 与编译均通过。run749/attempt `1788427972` 已在 4.398 秒确认 `ATTEMPT_RUNNING/prerequisites`，仅作执行 smoke，验收 27983 tank-state 快照率、spawn→death 延迟和 roster 承伤，不入 completion 分母；cycle 20 另作一次有界 60 秒终态等待后 `finished_at` 仍为 NULL，`aborted/0` 仍为运行中占位。cycle 21 的第二次有界 60 秒等待仍未终态，但已确认六只前置 150.944 秒完成、真实 gossip 0/1 于 162.657/186.900 秒到达；smoke 已进入动态观察。cycle 22 终态是 **477.207 秒 wipe、5 deaths**，只作执行 smoke、不入 completion 分母。新 comparator 未过执行门槛：Protector 存活窗口 166 tank-state 快照仅 127 选 27983（76.5%，目标 100%），33 spawn/28 death、平均存活 13.497 秒（最大 20.497）；窗口 roster 承伤 Protector 259,932、Custodian 63,752、Stormcaller 52,237、Kaddrak 41,001、Gaze 17,995。313.888/314.888 秒仍选 Stormcaller 27984，而 Protector 同时各造成 2,543/5,127 伤害；说明 comparator 仅重选时触发，无法抢占仍活当前目标。不得开正式样本或从本场 wipe 得出成效。cycle 23 排除 `tank target` value cache：其 checkInterval=1、每次 Get 重算；`TankAssistTrigger` 只有 current/tank target 不同且当前目标有仇恨才以 relevance 50 排 `tank assist`。没有逐 tick action log，故不能把 snapshot 归因为 gate、动作竞争或候选集缺失；r33 `Playerbots.log` 却已有多条 Protector reach-leash 拒绝（anchor 距 39.1–60.3、leash 38.5），所以存活/施压不等于安全可追，原 100% 生命周期快照验收过宽。不得以更高 preemption 强迫坦克穿过正常组队 leash。cycle 24 已回退此次 comparator（`TankTargetValue.cpp` diff 为零），`cmake --build var/build/obj --target worldserver -j4` 成功重编译/链接后以 FIFO relay 受控重启 `r32-restored`（PID 48353）。world ready 后 `raidtest status=IDLE` 已确认；未启动新 attempt。run749 仅作为不计分的执行失败证据。cycle 25 已量化并拒绝距离/LOS 假说：r32 的每只 27983 spawn 关联此前最近坦克位置到 `(943.09,401.38)`，run745 DONE 均距/平均存活 42.3 码/12.781 秒，run746/run748 wipe 是 40.5/13.945、42.7/14.971；合并桶 <30、30–40、>=40 的死亡率/寿命也不单调（22/27/14.188、30/33/13.592、39/39/13.832）。它既不区分完成与失败，也无安全可执行门槛，故不改空间策略。cycle 26 已在 r32-restored、无代码/策略变更下排入一个 `heroic-hos-tribunal-event-n5 --attempts 1 --force-recreate`。runner 依既有 normal5-v1 blueprint 重建五人 roster（GUID 821–825；未改 cheat/装备/难度/boss），身份重建为结果边界。30 秒有界状态为 run750/seq1/attempt `1788427973`，`ATTEMPT_RUNNING/prerequisites`、21.300 秒、hp_min=100%，5/5 在首只 Raging Construct 正常战斗。cycle 27 一次有界 60 秒等待后，前置于 178.922 秒完成，recovery 1.631 秒，真实 scripted-event gossip 0 于 180.553 秒触发；185.592 秒状态为 `ATTEMPT_RUNNING/observing`、hp_min=100%。已进动态 lifecycle。cycle 28 再等 60 秒后真实 gossip 1 于 204.746 秒到达，343.933 秒仍 `ATTEMPT_RUNNING/observing`、hp_min=100%。cycle 29 的第三次有界 60 秒等待后为 496.311 秒，仍 `ATTEMPT_RUNNING/observing`、hp_min=100%，日志确认 5 bot online/in-combat、0 dead；DB `aborted,duration=0,deaths=0` 仍为运行中占位，不是失败。cycle 30 已终态：run750/seq1/attempt `1788427973` 是 **499.753 秒 wipe、5 roster deaths、boss_hp_min=100%**；all-5-dead observer、runner/event-bus wipe 及回到 IDLE 相互确认。它在前置和真实 gossip 0/1 后结束，故入 lifecycle 分母；虽 roster GUID 821–825 为重建身份边界，normal5-v1/heroic/无 cheat/无 fixture/300 秒预算未变。r32 现为 **1/4 DONE**，仍不稳定。cycle 31 分层 run750：动态首死是坦克821@476.863 秒，之后825/823/治疗822/824/825于481.609/483.814/491.297/493.664/499.745级联；此前12秒坦克吃 Protector 10,362、Stormcaller 10,057、Custodian 4,308、Kaddrak 373（25,100），另有 Kaddrak 对822/824/823 1,719/1,076/573、Gaze 对824 1,556。治疗822在465.767–471.173多次施坦克、472.520–475.994转824；仅是单场目标竞争，且多敌压力，不能重开已被 run748 对照拒绝的治疗归因或改敌方优先级。cycle 32 已比较：按失败场 gossip1 后首死年龄253.342/262.403/272.117秒，取run745 gossip1后263秒的12秒对照，run745 roster总承伤13,155（Custodian7,950、Stormcaller3,748、Kaddrak1,457）；run746 61,473（Protector36,550最大）；run748 48,776（Custodian24,211最大、Protector12,856）；run750 30,191（Protector10,362、Stormcaller10,057、Custodian4,475、Kaddrak3,741）。三失败均高于成功窗，但主导敌人不一致，run750约为run746一半仍灭；只能判定高/多源压力不稳，不能归因某敌方、治疗或设计新 bot policy。cycle 33 已按此启动 r32 无变量 `heroic-hos-tribunal-event-n5 --attempts 1`，未带 `--force-recreate`，保留GUID821–825；8秒确认 run751/seq1/attempt `1788427974` 为 `ATTEMPT_RUNNING/prerequisites`、7.501秒、hp_min=100%，五人正常首拉。cycle 34 一次有界60秒后仍 `ATTEMPT_RUNNING/prerequisites`、163.404秒、hp_min=100%；第六只27972时五人均alive、距0–2.36、LOS=true，属正常推进而非前置减员/LOS blocker，cycle 35 再等60秒：前置167.023秒完成、recovery5.203秒，真实gossip0/1于172.226/196.410秒；315.962秒为`ATTEMPT_RUNNING/observing`、hp_min=100%。DB aborted/0仍运行占位。cycle 36 再等60秒后470.565秒仍`ATTEMPT_RUNNING/observing`、hp_min=100%，但stats为4 in-combat、1 dead；尚非全灭或runner终态，DB仍aborted/0占位，不能计wipe。cycle 37 终态：run751/seq1/attempt `1788427974` 是 **473.432秒动态wipe、5 roster deaths、boss_hp_min=100%**；all-5-dead observer、runner/event-bus、DB和IDLE相互确认。前置和真实gossip0/1已到达，未force-recreate且保留GUID821–825，故同roster有效lifecycle失败、入分母；r32现为**1/5 DONE**仍不稳。cycle38分层run751：首死824@439.560秒，后续治疗822@457.249、坦克821@470.272、823@470.619、825@473.430；此前12秒824吃Custodian13,387（6击），另Gaze/Kaddrak对治疗822为2,792/1,131、对坦克821为954，治疗822在428.827–438.181多次施824。它与run748首死窗Custodian13,592相似，却仍仅两场；run746首死是Protector、run750是混合坦克承压，故不能归因Custodian或治疗。cycle39对照否定Custodian优先级：run745 Custodian打819/坦克816=6,530/1,420，坦克先选Stormcaller至433.861、434.864才转Custodian；run748打818/816/817=13,592/7,881/2,738，坦克432.452（首死后）才转Custodian；run751的13,387全打824，但坦克821在427.705–438.735连续victim=0/select=0、engine多为1/无威胁、位置(929,361)距boss40.1，而824死于(950,393)。它是tank/group脱离下远端承压，不能证明强制Custodian或外推Protector comparator。cycle40追溯：821在400.652仍打Protector206，402.654变victim=0（select仍206）、403.660 engine转1/select=0，469.787才再选Custodian236，约67.1秒未重接战。动态段没有reach-leash/action拒绝日志，不能归因具体动作；402–439秒821固定(929,361)、823/825约(927/923,363/360)，治疗822/首死824约(949/953,383/389)，两团距30–38码。可确认Protector接战结束后坦克未重获目标且团体分裂，不能归因移动/leash/选择器或强制追击。cycle41确认206于402.572死，同批204/205于401.045/401.065死；821失victim是三Protector全清后。398.652–402.654两团已固定（821/823/825约929/928/923,361–363；822/824约949/953,383–389），全员moving=false且无移动跃迁/reach-leash/action日志；所以是既有双点站位，不能把206死亡或脱团移动当根因，只有清空后坦克未重接战为事实。cycle42量出有效敌人接管缺口：三Protector清后Stormcaller210/211于412.421 spawn，210于418.658首打824=1,726；Custodian221于423.424 spawn、427.535首打824=5,787。821至441.740才选Protector228（spawn436.418），即Stormcaller spawn→接战29.319秒、首伤→接战23.082秒；无动态reach/action拒绝日志。不是单纯空波：新敌存活伤队时坦克无目标；但未证明从821位置安全可攻击，不能追击/改优先级。cycle43静态检查否定TankAssist的current为空/非战斗安装缺失：`attacker count>0`后current=null即active，action relevance=50，combat及non-combat都有tank assist。AttackersValue收100码内全队threat；run751 821(929.03,361.04)距27985/221(964.30,381.94)约41码，221 427.535已打824，821 427.705–440.736仍combat=true、engine=1、victim/select=0。若221进入AttackersValue，既有路径应尝试；TankTarget无reach过滤而Attack才验LOS。没有tick候选/trigger/action证据，不能分候选漏纳、未入队、relevance竞争；不改策略也不新正式样本。cycle44/45已实现并收紧无行为诊断：GenericTriggers记map599/in-combat tank的attacker-count/current/active，只在原有current-target路径读tank-target，避免空target诊断读取污染缓存；TankAssistAction仅在实际选中时记target/LOS/Attack结果，不改返回。cycle45 MTHREADS=4 rebuild成功、三文件diff-check通过；全树codestyle仍只报既有core空行/tab/qualifier。无live session可归属，且mod-playerbots仍有8个非本cycle未提交行为变更（HoS Gaze移动、Tribunal reach-leash例外）；共同重启破坏单变量baseline，仍未重启/smoke。cycle46复核仍IDLE且无live session可确定归属；外来集合稳定为ReachTargetActions.cpp及HoS ActionContext/Actions/Strategy/TriggerContext/Triggers共8文件（本诊断仅另3文件），含两项行为策略。cycle47未触碰原worktree，已建HEAD 2dba88eb detached module worktree `/tmp/mod-playerbots-tribunal-diag`，仅应用本诊断三文件patch；status恰3文件、外来8文件patch为空，诊断已隔离。CMake modules路径固定为source root下，现build不能直链该worktree，不能交换原目录。下一步在独立source/build树挂接该module，编译/重启诊断binary并作一次不计lifecycle smoke。
