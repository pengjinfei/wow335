# Boss 验证台账

更新：2026-09-10。每行结果限定版本、装备、难度和辅助配置；未填不代表支持。

2026-09-10 UK 另两个 boss 的 normal5-v1 基线（**普通装打英雄本的第一批结论**）：新建
`heroic-uk-keleseth-n5` 与 `heroic-uk-skarvald-dalronn-n5`，与各自英雄场景逐行相同、只换
RosterFile 到 `normal5-v1`，不动 boss、难度与 cheat（`BotCheats = ""`）。
**凯雷塞斯 run322：4 击杀 / 0 团灭，四场零死亡**（boss 段 79.8–90.0 秒，对照英雄档
run273/274 的 99.3/116.1 秒总时长）；**斯卡瓦尔德&达隆 run323：5 击杀 / 5 尝试 = 100%，
五场零死亡**（前置 34–47 秒、双 boss 段 59.5–64.8 秒，对照英雄档 run286–288 的 86.1/80.6/90.1
秒总时长）。结论：**UK 三个 boss 里只有因格瓦尔（67%）受装备档位影响，另两个在普通档下
不是瓶颈，不需要为它们改 bot 策略。** 详见 [凯雷塞斯记录](bosses/heroic-uk-keleseth/README.md)
与 [斯卡瓦德&达尔隆记录](bosses/heroic-uk-skarvald-dalronn/README.md)。

2026-09-10 新缺陷（mod-playerbots，尚未修）：**bot 在战斗外低于 `LowMana` 阈值仍不喝水。**
run322/attempt1 是唯一非击杀，记为 `prerequisite_failed: natural recovery timeout`——boss 从未
参战、零死亡，纯粹卡在框架的恢复门禁上。清完 4 只英雄小怪后戒律牧只剩 **1,989/16,708 = 11.9%**
法力（`AiPlayerbot.LowMana = 15`，已在阈值以下），包里有 20 个蜜风茶 33445 与 20 份食物 33443，
`UseFoodStrategy` 在无 food cheat 时正是 `low mana → drink`；但回蓝曲线 46→151 秒是一条
恒定 ~78/s 的直线，**全程没有喝水加速段**，151 秒才到 61%，120 秒门禁判负。attempt 2–5 只是
清怪耗蓝较少才踩过门禁，同条件会随机复现。分层：喝水行为属 mod-playerbots 缺陷（优先修）；
框架 120 秒自然恢复预算按英雄档法力池设定，在喝水修好前普通档可能需要更长预算，**不得用框架
补资源绕过**。

2026-09-10 Ingvar P2 团灭根因（**推翻同日早些时候的「治疗空转」结论**）：真凶是
**Woe Strike（59735）**——因格瓦尔英雄 P2 每 15–20 秒给坦克挂一次（每场 4–7 次），
挂着期间**治疗每治疗坦克一次就有一份暗影伤害直接打回治疗身上**（触发 59736）。
这解释了治疗站在 24.68 码外仍持续挨 boss 伤害、**八场团灭治疗全部阵亡且五场是第一个死**。
反射伤害是干净判据：击杀 15 场为 1,432–19,175，团灭 9 场为 **21,311–60,098**（阈值约 2 万，
唯一越界的 run324/seq3 为 36,378 且死了 3 人）。Woe Strike 的 `Dispel = 2` 是**诅咒**
（已用 980/172/2818 校准枚举；牧师的驱散魔法无效），能解的是**法师的解除诅咒 475**，
法师也确实在用（全部普通档 run 合计 74 次），但**每场 4–7 次上身只解掉 0–4 次**：
3 秒内被解的窗口 proc 0–7 次，一直没解的窗口 proc 3–24 次。**法师漏解的原因尚未定位**，
是下一步唯一要量的一环。

2026-09-10 同日已证伪、勿重走的两条（详见 Ingvar 记录）：(1) **`HealerAutoSaveManaMultiplier`
掐掉快速治疗**——非坦克分支确实少了坦克才有的 `health >= lowHealth` 门槛，日志也抓到过
`Multiplier save mana made action flash heal on party useless`，但补上门槛重编后（run325）
快速治疗次数纹丝不动（仍 0–1），改动已回退，二进制与源码回到 `67ac953c`；引擎日志显示
快速治疗的真实拒绝原因是 GCD（49 次）、`UNIT_STATE_LOST_CONTROL`（26 次）与自身苦修引导
（15 次），`save mana` 仅 5 次。(2) **治疗空转**——第一版统计把治疗阵亡后的时间算成了空档；
按存活期重算，GCD 利用率击杀 60.3%、团灭 **62.7%**（团灭反而更高），最长真实空档 9.4 秒
与 Dreadful Roar 的 8 秒恐惧同量级。真正的差别是治疗活多久（团灭 30.0s vs 击杀 50.5s）。
另外仇恨也已排除：boss 仇恨采样 133 个样本中 132 个当前目标是坦克。


本轮未提交差异审阅确认：Ingvar 的历史 1/3 不代表后续策略修复后的固定版本稳定率；“多斧同时重叠”尚未证实。6538 已自然验证 P2 前锥应急主链路；6540–6543 已确认 P1 effect-0 前锥模型与单斧生命周期。6544 复验 P1 的正确职责边界，6546 为新增自然击杀；精确结算站位又发现非坦克可在 Boss 中心重叠，常态后方阵型尚待处理，详见 Boss 记录末尾。

2026-09-10 普通档 Ingvar P2 走位优化：**20% → 67%**（run 317/318/321 合计 10 击杀 / 5 团灭，15 样本，4 场零死亡；修复前 run316 为 1/5）。不动装备、难度或 cheat，只修 mod-playerbots 缺陷（`codex/heroic-uk-ingvar` 67ac953c）。共同根因是 2.0 码 boundary 旁路——`IsWithinBoundaryRadius` 走 `Position::IsInDist`，纯 3D 中心距不加体积，中心距 < 2.0 时锥形的角度判定被完全绕过。两条把成员送进去的路径：(1) **Killing Spree 与暗影猛击读条重叠**，盗贼本已稳在后弧 3.50 码，51690 把它瞬移到 1.99–2.00 并锁住约 3 秒，三次团灭的读条起点/位移偏移为 99023/+1.26s、68383/+0.42s、95636/+1.22s，effect 0 打出 25,102–39,232 而盗贼血上限仅 14,644，`front_60` 恒为 false；改为读条窗口内禁止此类位移后，窗口内命中由 5 次降为 0 次而技能总使用次数 8→7。(2) **散开只校验落点不校验路径**，成员完全重叠时 `crowd->GetAngle(bot)` 为 `atan2(0,0)`=0，方向退化成正东，加上 `route=direct` 导致治疗直线穿过 boss 贴到 1.38 码（起点/boss/终点三点共线），随后被磨死并成为该场唯一团灭起点；改为重叠时以「背离 boss」为基准方向 + 点到线段距离校验后，治疗最小距离回到 10.30 以上。**已回退的负面结果**：用震荡图腾(8143)+防护恐惧结界(6346)应对 Dreadful Roar 的 8 秒恐惧，两个技能 bot 本来就会且从未使用过，但震荡图腾占大地图腾栏位——run319 与常规循环的石肤图腾来回顶替，萨满铺 28–37 次、输出从约 150,000 掉到 0–5,493、一场 300 秒超时；run320 降优先级并关掉竞争图腾后仍为 0 击杀/2 团灭，全队总伤 395k–447k 对照 483k–496k，栏位机会成本抵不过收益。~~剩余靶子与站位无关：Dreadful Roar 的全队 6.6k–10.8k 伤害~~ **（本句已作废，见本文件顶部同日的 Woe Strike 条目：真凶是 Woe Strike 反射伤害；且『全场零群体治疗』有误，run318/seq4 牧师放了 2 次治疗祷言）**run318/seq4 全队以 84/74/77/50/94% 血进 Dreadful Roar、法师被 10,766 打死这组数字本身仍成立。详见 [Ingvar 记录](bosses/heroic-uk-ingvar/README.md)。

2026-09-10 普通五人本毕业档位（normal5-v1）：新建与 heroic5-disc-v1 并存的装备档位（**未覆盖英雄档，其证据全部保留**），除装等外阵容完全一致，boss 仍为英雄难度。ilvl 上限 187（普通本掉落上限；英雄本才 200），85 件装备在本地世界库/Spell.dbc 全部可核验来源（drop 40、quest 34、chest 27、crafted 26、vendor 4），宝石降为 ilvl 70 优秀档。场景 `heroic-uk-ingvar-n5`，run 316 六次 attempt（末次撞副本创建限流）：**1 击杀 / 4 团灭 = 20%**，对照英雄档同场景 8/8。四次团灭中三次把 P2 打到 **4–9% 血**，是窄边界而非碾压；瓶颈在 P2 后段承伤与治疗续航（坦克耐力 −21.7%、潜行者命中 −61.1%），伤害侧五人均有实质输出。详见 [normal5-v1 装备档位](fixtures/normal5-v1/README.md) 与 [Ingvar 记录](bosses/heroic-uk-ingvar/README.md)。

2026-09-10 框架修复（mod-raidtest）：`CombatTrigger::BeginAssistForAll` 的 `alreadyAssisting` 判据过严，要求跟随者 `IsInCombat() && BOT_STATE_COMBAT`，而坦克建立仇恨后跟随者常已锁定 boss 但尚未出手，导致 run 312/314/315 共 9 次 attempt 里 **8 次**被误报 `pull failed (not all followers entered combat)`（装备夹具同时全部 `valid=true`，排除装备原因）。判据改为只看 `current target == boss && victim == boss`；boss 是否参战已由上一步 `AwaitTankAggro` 确认，跟随者是否输出由采样证据体现。修复后 `could not begin assist` 归零，7 次 `already assisting` 全部可用伤害事件核验为实际参战。

2026-09-09 Ingvar 三骑手前置根因：官方 `heroic-uk-ingvar` 首次跑通一次完整链路（1788426626，185.3 秒、1 死，三骑手 48.3 秒清完），但五次冷启动仅 **1/5**，四次失败全在骑手阶段。零位移导航探针（run294）证明 `PrerequisiteX/Y/Z = 252,-350,185.8` 在网格上**没有任何投影**（`type=17`、终点 `polyRef=0`、`find_path=0x00000000`、`component=disconnected`），成员到达即坠落——run296 的 1.41 秒 `victim=736 killer=736` 单次 **26,324 自伤**打死坦克，正是 run183 症状的确切机制。改从已实测可走的准备点集结（run295）则因骑手高 5 码而无视线，180 秒超时、0 死亡。故这是 **map 574 上层平台缺 mmap 覆盖**的资产阻塞，与跨房间自主行进同源，纯配置无解；隔离形态 `heroic-uk-ingvar-disc` 仍为 9/9 击杀。另修掉本轮自引入的告警刷屏（180 秒 73,185 条 → 0）。

2026-09-09 斯卡瓦尔德&达隆完整链路打通：**10 只前置怪 + 双 boss 三次零死亡击杀**（1788426621–6623，86.1/80.6/90.1 秒，`prerequisites_complete` 35.2 秒）。两条根因：准备点 `(75,0,117)` 距最近可走多边形 **8.3 码**（零位移导航探针 `type=10`、`actual_end=81.50,-5.13,118.90`），bot 被传送进几何体内部，对小怪（19.26 码）与 boss（约 51 码）的 `los` 全为 false，两种拉怪都被拒——已改为该实测投影；房间为 L 形，实测「小怪房内 on-mesh 点」可清掉 4 只后第 5 只在 48.13 码外无视线，故不存在能看到全部目标的单一坐标，编排层新增 `ApproachPrerequisiteTarget`（拉怪被拒时下达一次普通接近移动并重试，沿用整队全或无路线预检，上限仍为 `PrerequisiteTimeoutSeconds`）。场景注释「首领房小怪」已订正：那 10 只在 x∈[23.9,64.4] 的西侧房间，首领房 45 码内无小怪。同一二进制回归：凯雷塞斯 1788426624 击杀 107.6s/0 死，因格瓦尔 1788426625 击杀 117.0s/0 死。

2026-09-09 英雄 UK 三 boss 回归（1788426608–6615，固定源码/二进制、同一 roster、每样本一次冷启动）：凯雷塞斯 run273/274 **两次零死亡击杀**（99.3/116.1 秒，含 4 只前置怪清理）；斯卡瓦尔德&达隆 run279/280 **两次零死亡击杀**（51.5/49.0 秒，对照 run127 的 48.6 秒）；因格瓦尔 run265–272 **8/8 击杀**。三者均无回归；凯雷塞斯与斯卡瓦尔德样本的 playerbots 日志中 `Ingvar diagnostic` 条目为 0，证明整本注册的新增触发器不会在其它 boss 误触发。同时定位到既有缺口：`heroic-uk-skarvald-dalronn` 的前置清怪链自 `c460d05`（2026-09-07 17:50，与 `PreparationX/Y/Z` 同一提交）加入起从未启动——准备点 `(75,0,117)` 对小怪区无视线（按实测距离重排后最近目标 19.26 码，`los` 仍为 false），对 boss 亦无可用视线；run127 的击杀早于该提交。三个配置探针（1788426610–6613）已全部还原，运行配置与模板逐行一致。详见 [UK 机制审计](bosses/heroic-uk/MECHANICS-AUDIT.md)。

2026-09-09 Ingvar 第三轮：**八次独立冷启动 8/8 击杀**（1788426600–6607，93.9–112.3 秒，4 场零死亡）。此前「坦克只能硬吃猛击」的前提是错的：root 窗口从结算前 3.005 秒开始、Boss 被定身且 `DisableRotate`，坦克 `can_move` 全程为 true，2 秒全队昏迷属于结算的一部分。而 P2 的 `59709` 是**暗影**伤害（P1 的 `59706` 是物理，护甲减到 7,137–13,515），坦克实收 22,891–25,787、血上限仅 28,884、每 9–11 秒一次；此前偶尔存活是 `66233` Ardent Defender（60 秒一次的免疫致死打击）撞上了，不是抗住了。根因是 `IngvarDodgeSmashAction::isUseful` 用 `!behind` 判据（43 次评估里 32 次被判「在背后=安全」跳过），而 2.0 码 boundary 旁路不看角度；且它执行时落点正好停在 2.0 码阈值上。改为后弧 3.5 码、`MOVEMENT_FORCED`、判据换成两个真实选中条件后，56 次猛击中 effect-0 命中仅 **1 次**，坦克承伤 40,217–58,957（原 89,765–144,266）、治疗输出 68,207–120,094（原 111,593–209,394），8 场中 6 场在第二把斧投出前结束。第二轮列为第一优先的「治疗对坦克优先级」随之不再是瓶颈。仍未验收：三骑手房间准备条件下的完整链路与跨房间自主行进。详见 [Ingvar 记录](bosses/heroic-uk-ingvar/README.md)。

2026-09-09 Ingvar 站位改动第二轮：核心 `WorldObjectSpellConeTargetCheck` 的判定是 `IsWithinBoundaryRadius(target) || isInFront(target, coneAngle)`，`Unit::IsWithinBoundaryRadius` 对玩家为 **2.0 码且绕过角度判断**。因此「绕背」不是近战对 Smash/Dark Smash effect 0 的答案，「离开 2.0 码」才是；这也解释了此前全部坦克 effect-0 记录都是 `front_60=false`。`Unit::GetMeleeRange` 为 5.0 码，故近战安全带是中心距 (2.0, 5.0)。近战后弧落点由 7.0 改为 3.5 码（7.0 在射程外，必被 `reach melee` 拉回，往返是越界来源），贴身清理阈值由 1.5 改为 3.0 码。五次冷启动（1788426595–6599）中近战盗贼 effect-0 命中 **0 次**（改动前 2/5），清理触发覆盖 1.41–2.97 码、落点恒为 3.50 码。两轮合计 11 场 **7/11 击杀**，仍不宣称击杀率提升。当前唯一制造首个减员的原因已量化为治疗不给坦克治疗：坦克血上限 28,884、`59709` effect 0 为 26,249–33,750，两场失败中坦克以 66%–71% 进入结算被 22,891/25,787 一击打死，而治疗分别有 57% 与 **98%** 法力、其中一场致死前 6 秒对坦克零次治疗；全场 76 次治疗只有 4 次是 5k–7k 大治疗。另有一项未证实的规则（斧在 7 码内不起手新非瞬发法术）须单独 A/B 或删除。详见 [Ingvar 记录](bosses/heroic-uk-ingvar/README.md)。

2026-09-09 Ingvar 站位改动第一轮：法术数据已固定全部阈值——`59706`/`59709` 的 effect 0 是 **10 码前锥**（36,999–43,000 / 26,249–33,750），斧的常驻光环 `42750` 每秒触发 `42751`、半径 **5 码**；`59709` 的 effect 1 昏迷（**2,000ms**）与 effect 2 伤害半径均为 200 码，Dreadful Roar 为 60 码，二者无站位解法。已实施远程/治疗外移到 13 码脱离前锥半径、非坦克远程互散 8 码，并新增全员每秒资源采样。五次独立冷启动加一次同进程样本为 **4/6 击杀**，与改动前正式 2/3 在该样本量下无法区分，**不宣称击杀率提升**；可确认的是五场冷启动中 effect-0 再未命中治疗或远程 DPS（全部落在坦克或近战盗贼），且失败窗口从第二把斧的 102–110 秒前移到首把斧与首次 `59709` 的 65–75 秒。新发现两个未修复缺陷：斧的落点成员 3–4 秒不执行规避（日志无成功也无拒绝记录），近战盗贼仍会吃 10 码前锥（2/5 场）。详见 [Ingvar 记录](bosses/heroic-uk-ingvar/README.md)。

2026-09-08 Ingvar NavigationOnly 复核：原骑手点不是 MMap 地面端点；实测上层端点与下层在同一 4,562-poly 连通分量。run166 证实 1,024 Detour 节点池耗尽，run167 证实 148-poly 输出缓冲截断；节点池提高至 4,096、playerbots 路径容量提高至 512 后，run168 预检取得完整 `type=1` 地面路径。leader 同地图重试现强制 worldport，run173–175 用新二进制通过五人同一 `Map*` 门禁；run175 证明首段起点位于斯卡瓦尔德/达隆近战范围。该跨房间路线已转入完整副本自主通关待办；当前 Ingvar 验收使用平台准备夹具且不把传送计为行进证据。详见 [Ingvar NavigationOnly 验证记录](bosses/heroic-uk-ingvar/README.md#2026-09-08-导航阶段死亡归因首个-boss-遭遇非复活或-mmap-问题)。

| 场景 | 最近证据 | 当前判定 | 下一步 |
|---|---|---|---|
| heroic-uk-keleseth，五人 heroic5-v1 | run91/a1、run92/a1、run92/a2 连续三场零死亡击杀（再拉怪门槛修复）；run86/a1 完整链路零死亡击杀；框架3b9203b+未提交修复 | 前置/重置/保护实测；连续稳定与冰墓未验收 | 连续多场与冰墓机制验收 |
| heroic-uk-keleseth-n5，五人 normal5-v1 | run322：4 击杀 / 0 团灭，四场零死亡（boss 段 79.8–90.0s）；1 次 abort 是框架恢复门禁，非策略失败 | **普通装不构成瓶颈**；冰墓机制仍未验收 | 不需为它改策略；喝水缺陷修好后复跑一轮确认 abort 消失 |
| heroic-uk-skarvald-dalronn，五人 heroic5-disc-v1 | run127/a1 重启后同实例零死亡击杀（48.555s）；历史 run94/a1、run96/a1、run96/a2 共三场击杀 | 双 boss 隔离战正常规则可通关；队长目标实例传送已实测；房间小怪清理未纳入 | 房间清怪前置调优、更多场次 |
| heroic-uk-skarvald-dalronn-n5，五人 normal5-v1 | run323：**5/5 击杀，五场零死亡**（前置 34–47s、双 boss 段 59.5–64.8s） | **普通装不构成瓶颈**，完整链路含 10 只前置怪 | 不需为它改策略 |
| heroic-uk-ingvar，五人 heroic5-disc-v1 | 6514 验证首把斧 0 码成员可借侧向回退撤至 8–12 码；6520/6528/6530 击杀，6521–526 为诊断样本；6526 记录 `59709` resolved targets；6532 覆盖 5 次 P2 的 history 探针反例；6534、6536 为新紧急移动分支后的自然团灭；6538 为新二进制自然击杀；6540–6543 为 P1/斧诊断；6544 复验 P1 职责边界；6546 为新增自然击杀；6548–6549 验证精确结算站位；6557、6559 动作归因/隔离样本；6561–6568 斧净空、普通绕背与 `reach melee` 复验；6569–6573 安全环及当前版本 cold start；6574 P1 只读诊断；6575 不修正共同准备点对照；6576 角色分离 fixture 首样本；6577–6579 新基线 cold start；6578 P2 首死归因；6580 暗影斧只读动作闭环；6581 CanMove 控制状态样本；6582 aura/时序样本 | 角色分离 fixture 已连续 5 次以 5/5 位置门禁通过；新基线正式 cold-start 仍仅为 **2/3 击杀，稳定性未验收**（6577 kill/0 death，6578 wipe/5 deaths，6579 kill/1 death）。6580–6582 是单场诊断团灭，不混入稳定率：两把斧的 20/1/7 码、HP、最近治疗与生命周期均已持久化。6580 的第二把斧前治疗已在 P2 的后方 `effect_mask=6` 伤害中死亡，随后萨满 0 码面对有效的 8 码候选却因 `CanMove=false` 未提交，收到斧实体 5,514 点伤害死亡。6581 确认有效候选的 `CanMove=false` 可同时为 `lost_control=true/rooted=true`；6582 将其具体映射为英雄暗影猛击的 `stun:59709`，排除旧移动等待。6582 中 `59709` 于 108.595s 施放、暗影斧于 108.608s 出现；斧首次进入危险半径时角色已处于正常控制窗口，不能用优先级或强制移动绕过。三场首次 P1 `59706` 均只命中坦克，排除旧共同前方出生点作为本组团灭的直接解释；三场均有 P2 记录。旧共同准备点结果不得混计。P2 前锥紧急动作已与普通绕背分离为 `MOVEMENT_FORCED`，且不依赖当前攻击目标；6538 已验证主链路。P1 恢复坦克朝向、非坦克后方模型，6544 的 P1 effect-0 仅命中坦克。6563 验证 7 码斧候选净空（20 次成功提交最低 7.88 码）；`Follow` 试验 6566 与单参数 `MoveChase` 试验 6568 均出现中心重叠，均已回退；6567 证明 `reach melee` 成功端点为 3.75–4.88 码，非该场 1 码重叠的直接来源。6571/6572 验证安全环触发→forced 7 码后方移动。 | 保留 fixture、斧的 7 码净空、`set behind` 隔离、`reach melee` 观察和 1.5 码提前安全环（`ACTION_MOVE + 10`）。2026-09-09 已完成该只读对照（6577–6582）：P2 首把斧在六场中全部落在首次 `59709` 之后恰好 +1.99~2.00 秒（必定在控制窗口内）且从未致死；第二把斧（102.0–108.6 秒）在出现的四场中每场至少致死一人，与是否撞控制窗口无关（+0.01s 两场、+6.17/6.46s 两场）。因此排除移动优先级与强制移动方向。三场团灭停在 Boss 7%/10%/13%；P1 六场均为 247,626 点/36.7–42.8 秒（约 5,800–6,700 raid DPS），P2 同为 247,626 点却只有 2,551–5,389 raid DPS，差距来源已实测为 `59709` 自读条起始把全队（含 reach 校正后 28.83 码的治疗）置为 `can_move=false`，而 P1 的 `59706` 窗口全员 `can_move=true`。6582 第二把斧同时命中相距约 3.9 码中心距的法师（55% 血）与萨满（45% 血），两跳各约 4.7k–5.1k 致死；中心距约 7.2 码与 8.3 码的成员未受斧伤，满血两跳不致死。下一步按序为：P2 非坦克互散 8 码、补治疗法力/目标只读字段、归因治疗在 101–107 秒先死、查清近战 effect-0 接触路径、统计 P2 不可行动/规避/输出占比。不得绕过控制、调装备、难度、cheat 或泛化 P1 躲避；跨进程以 attempt ID 聚合 |
| naxx-loatheb，十人 fixture-v1 | run77/a1,a2、run78/a1；3 次零死亡击杀；框架 8f06a10 | 当前配置编排回归通过；无辅助、同阶段装备及完整机制覆盖未验收 | cheat 审计后建立正常规则对照 |
| naxx-patchwerk | 已有场景配置和历史测试；未做 8f06a10 固定角色回归 | 待本版本验证 | 完成基线口径核验后复测 |
| 其他 WLK boss | 仅源码覆盖初查 | 未验收 | 按 WORKFLOW 新建逐 boss 记录 |

Loatheb 证据：[终局与登录竞态](../investigations/roster-fixture/GROUP-LOGIN-RACE.md)、[角色指纹](../investigations/roster-fixture/FINGERPRINTS.json)。历史 run 不按记忆追补“通过”。

新 boss 的记录放 `docs/testing/bosses/<scenario>/README.md`，复制 [模板](BOSS-TEMPLATE.md)。状态采用：待审计 / 待运行 / 框架阻断 / 策略失败 / 当前配置击杀 / 正常规则机制验收通过。完整记录应同时保留失败尝试。

当前优先五人线：[凯雷塞斯王子记录](bosses/heroic-uk-keleseth/README.md)。用户已将 Patchwerk 复测后移。
