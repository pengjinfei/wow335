# 从阿努巴拉克那一轮攻坚里带走的东西（2026-09-13 ～ 09-15）

这份文档给**下一个 boss** 用。阿努巴拉克的逐轮细节在 [boss 记录](bosses/heroic-an-anubarak/README.md)，
这里只留可迁移的：通用方法、引擎层已修/未修的系统性缺陷、口径陷阱清单、以及待办。

结果口径：阿努巴拉克从 40%（24/60 场）提到 **80%（8/10 场，Fisher 双尾 p=0.036）**；
三个旧 boss 回归 9/9 零死亡且普遍更快。**全部收益里，真正统计显著的只有一项**（目标接管），
其余四项只动了中间量——这本身就是最重要的一条经验。

---

## 一、通用方法（下一个 boss 直接照搬）

### 1. 先量化「改前口径」，再动手

每一项改动都必须先有一个**能复跑的脚本**把改前的数字打出来，并明确"改动要让哪个数字动"。
本轮最贵的教训是反过来的：凭"看起来像瓶颈"动手，跑两轮（约 45 分钟）才发现目标指标压根没动。

### 2. 新动作上线**先验执行率**，再开正式两轮

playerbots 是「每个 tick 只执行一个动作」的引擎，新动作最常见的失效方式不是逻辑错，是**排不上队**。
本轮三次白跑整轮就是这么来的：

| 事故 | 现象 |
|---|---|
| 坦克接小怪挂 26.5 | 触发器亮起的 15 个 tick 里执行 **0** 次（被 `tank assist`(50)/`tank face`(30) 抢光） |
| 神圣恳求挂 `ACTION_HIGH`(20) | 推入 381 次只执行 9 次（2.4%） |
| 英勇闸门用 `find target` | 潜地中的 boss 带 `UNIT_FLAG_NOT_SELECTABLE`、不进目标表，判据 fail-open，英勇照样早放 |

**做法**：改完先跑 1–2 场，`grep "A:<动作名> - OK"` 看计数；为 0 就先调相关性/判据，别开整轮。

已知相关性台阶（盗贼视角实测）：
`pull action` 105 / `loot roll` 100 / `drop target` 99 / `ACTION_EMERGENCY` 90 / **AN 层躲踏 60、保距 58** /
`dps assist`·`tank assist` 50 / `kick` 42 / `set facing`·`set behind` 37 / `tank face` 30 /
`fan of knives` 28 / `garrote` 27 / `hunger for blood`·`ambush` 26 / `envenom` 24 / `reach melee` 21 /
**`mutilate` 13** / `melee` 5。

`USELESS` **不吃 tick**（冷却中/buff 已在身上会直接跳过），所以给保命类动作抬高相关性是安全的。

### 3. tick 级统计法（定位"动作排不上队"的唯一办法）

按 bot 切 `--- AI Tick ---` 分段，统计：
- 该动作被 `PUSH` 的 tick 数 vs 出现 `A:<名> - OK` 的 tick 数；
- 没轮到它的那些 tick 里，**最后一个 `OK` 的动作是谁**（= 抢占者）。

本轮靠它定位到：盗贼主输出毁伤 303 次推入只有 **17% 轮得到**，抢占者里目标管理链占 46%。

### 4. 两轮五场才下结论，早停规则必须**事先声明**

5 场的击杀率差异几乎全在噪声里（本轮出现过同一改动 5/5 与 1/5 相邻两轮）。
序贯早停可以用（省时间），但规则要**事先写下**；本轮我犯过一次：看到中途两场团灭就想引用早停，
而第 3 场其实是击杀——**看着中途数据找理由停**就是滥用。

### 5. 共享层改动必须回归三个旧 boss

`heroic-nexus-keristrasza-disc-n5`、`heroic-uk-skarvald-dalronn-n5`、`heroic-nexus-anomalus-n5` 各 3 场。
回归不只是"没退化"，也可能是**正收益的证据**：目标接管那一轮回归 9/9 且阿诺玛鲁斯快了 30–60 秒，
反过来佐证了收益面是全副本而不是本 boss 调参。

---

## 二、引擎/共享层的系统性缺陷（对每个 boss 都成立）

### 已修（下一个 boss 直接受益，不必重查）

| 缺陷 | 表现 | 修法 |
|---|---|---|
| **目标一死要走四个 tick 才恢复输出** | `drop target`→`dps assist`→`reach melee`→`set facing`，实测 tick 0.69 秒 ≈ **2.8 秒不出手**；一场死 20 只小怪 ≈ 56 秒空转 | `DropTargetAction` 继承 `AttackAction`，同 tick 接管下一个目标（`2fa6cab8`）。**击杀率 40%→80%，p=0.036** |
| **面向判据比游戏严一倍** | `IsFacingValue` 用 `HasInArc(M_PI_2)`=±45°，核心自身判面向是 ±90°；灰带里反复触发 `set facing`（一场 198 次，每次还 `SetNextCheckDelay(100ms)`） | 改 ±85°（留 5° 余量）。`set facing` −39%、`NOT_INFRONT` 0 次 |
| **防骑的续蓝闭环断在第一环** | 神圣恳求挂 `HighManaTrigger`（蓝<65% 才亮）+ 相关性 20，推入 381 次只执行 9 次；恳求不在身上，圣光守护 2/2 每场 123 次 proc 全刷在空气上 | 新增「战斗中且恳求不在身上」触发器 + 相关性 52。坦克活着时法力地板占蓝池 50%→72%，全程不见底 |
| **治疗见底后队里还有一整池蓝没人用** | 坦克阵亡 39 次里 31 次是被小怪磨死、同刻牧师蓝中位 38，而元素萨满 18.5k 法力池整场只放 3.5 次治疗 | AN 层「治疗没蓝/阵亡且有人残血 → 非治疗职业补治疗波」。坦克消耗型阵亡 31→1 |
| 夹具：英勇疲惫跨场残留 | 疲惫(57723) 10 分钟 > 一场周期 5.3 分钟，下一场施法直接 `SPELL_FAILED_TARGET_AURASTATE(111)` | 开怪前清 57723/57724（`4beddea`）。**这也修正了"13 场没放英勇"的错误归因** |
| 夹具：打输了还空跑到超时 | 原本只有「全员死亡→wipe」「无人死亡且 boss 脱战→aborted」两条路径，萨满自复活独活时两条都不占 | 新增「有人阵亡 + boss 脱战回满血 + 残存者脱战 → 立即判 wipe」（`22107fd`），一次省约 200 秒 |
| **看不见队友＝队友不存在（治疗链）** | `PartyMemberToHeal::Check` 要求 `IsWithinLOS`，视线一断治疗目标为空 → 不治；而所有"走过去"的动作又都以同一个值为目标 → 连挪一步都不会。实测一段 20 秒：治疗满血、原地不动、法杖平A、法力还在回，18 码外的坦克被磨死 | 新增 `party member to heal no los` 取值只给「走过去」这条链用（触发器 + 接近动作），**施法仍用带视线的取值**（`9134211d`）。无视线占比 22%→3%、治疗频率 0.37→0.48 次/秒、德雷德 3/5→15/16 |
| **看不见队友＝队友不存在（`main tank`）** | `PartyMemberMainTankValue` 用基类 Check，坦克被柱子挡住那一秒 `main tank` 变空，所有 `if (!mainTank) return false;` 整组静默跳过：盗贼嫁祸/消失、法师隐形、牧师渐隐、给坦克的 buff、**以及"追敌不离主坦"那道护栏**（护栏恰好在最需要时关闭） | 覆写 `Check` 只去掉视线、保留同地图与距离判据（`5ef5adcc`）。非坦克位取不到主坦的战斗秒 **20%→0%**（法师 30%/萨满 27%/牧师 15%/盗贼 6%） |
| 框架：boss 开怪即进入不可攻击阶段就跑不起来 | `BeginAssistForAll` 要求每个跟随者都能对 boss 下 assist，而诺沃斯 P1 立刻挂 `NON_ATTACKABLE\|NOT_SELECTABLE`，`IsValidAttackTarget` 必然 false → 整场判 `not all followers entered combat`，一场都跑不起来 | boss 不可选中时放行跟随者、不代它们选目标（mod-raidtest）。阿努巴拉克潜地同族 |
| 框架：巡逻 boss 的开怪被一次性拒绝 | 开怪那一刻 boss 可能正好被柱子挡住或走出施法距离，`pull_rejected ... los=false` 直接作废——德雷德为此丢过两场 | 开怪被拒给 20 秒重试预算、逐 tick 重来，超预算才 abort（mod-raidtest） |

### 未修（**待办**，价值已量化）

> **注（2026-09-16）**：第 0 条（视线过滤）与第 0.5 条（治疗不给自己留手）**都已修**，
> 连同 `main tank` 那条同族缺陷一起搬到了上面的「已修」表；下面的原文保留供追溯。
> **当前真正未修的是第 1 条起。** 施法型取值（驱散/复活/团队 buff）仍按视线过滤是**刻意保留**的，
> 相关的新增功能见第六节待办第 0 条。

**0. 视线被当成"候选过滤条件"，看不见的队友等于不存在**（2026-09-15 因格瓦尔实测，**2026-09-16 已修** mod-playerbots `9134211d`，见本条末尾）

`PartyMemberValue::Check`（PartyMemberValue.cpp:113）和 `PartyMemberToHeal::Check`
（PartyMemberToHeal.cpp:135）都硬性要求 `IsWithinLOS`。后果不是"少治一次"，是**整条链条静默停摆**：
治疗的 `party member to heal`、法师的 `party member to dispel` 一起返回空 → 不治、不解；
而所有"走过去"的动作（`reach party member to heal` 等）又都以这同一个值为目标 → **连自救都做不到**。
实测一次团灭：治疗连续 10 个 tick `no actions executed`、法师同窗口零伤害 14 秒、
坦克 31 秒没收到任何治疗从满血被磨死。

**上游写过修法但够不到**（`RangeTriggers.cpp:193`）：

```cpp
bool PartyMemberToHealOutOfSpellRangeTrigger::IsActive()
{
    Unit* target = AI_VALUE(Unit*, "party member to heal");
    if (!target) return false;                    // ← 视线不通时就在这里返回了
    return ... || !bot->IsWithinLOSInMap(target); // ← 专门为视线写的分支，永远到不了
}
```

修法方向：给这个触发器和 `ReachPartyMemberToHealAction` 喂一个**不按视线过滤**的取值
（施法类动作仍用带视线的那个，免得隔墙施法），并让接近动作在"距离够但看不见"时也 `isUseful`
（现在它 `IsWithinCombatRange` 就返回 false）。**风险**：共享层、影响所有治疗职业，必须回归三个旧 boss。
因格瓦尔那一轮先在 boss 层验证了机制（新增 `ingvar regain los` 侧移绕遮挡，靶子指标从 9–17 秒归零）。

**共享层版本已做（2026-09-16，`codex/shared-heal-los-recovery` 的 `9134211d`）**，就按上面这个方向：
新增取值 `party member to heal no los`（同判据去掉 LOS）→ 触发器改用它 →
`ReachPartyMemberToHealAction` 覆写 `isUseful`/`Execute`，把「距离够但看不见」也算需要移动，
看不见时先靠到 8 码、已在 10 码内还看不见就靠到 2 码。**施法类动作仍用带视线的取值。**

- **坑**：靠近距离不能用 `distance`（治疗距离 40 码）——`ReachCombatTo` 一进门就判「已经够近」返回 false。
  第一版写成 `max(8, distance*0.5)` = 20 码，而治疗本来就站在 17–19 码，6 次推入 6 次 FAILED，一步没走。
- **效果**（英雄达克萨隆要塞 King Dred）：治疗与坦克之间的无视线占比 **22% → 3%**，最长无视线块 27 秒 → 6 秒，
  治疗频率 0.37 → 0.48 次/秒；击杀 3/5 → **15/16（94%，CI 72–99%）**，Fisher p=0.128（改前只有 5 场，不显著）。
- **回归 9/9**：凯利丝塔萨 3/3、斯卡瓦尔德&达隆 3/3 零死亡、阿诺玛鲁斯 3/3 零死亡。
- **同族第二条已做**（2026-09-16，`5ef5adcc`）：`main tank` 取值也不再按视线过滤。
  「谁是主坦」是事实，与看不看得见无关；取不到时 `if (!mainTank) return false;` 会让
  嫁祸/消失/隐形/渐隐、给坦克的 buff、以及"追敌不离主坦"护栏整组静默跳过。
  实测非坦克位取不到主坦的战斗秒 **20% → 0%**（法师 30% / 萨满 27% / 牧师 15% / 盗贼 6%）。
  量它用 mod-raidtest 的只读探针 `mt=`（每秒一采样，`9d36278`）。
- **仍未做**：施法型取值（`party member to dispel` / `to resurrect` / `without aura`）继续按视线过滤。
  **这是对的**——隔墙放不出驱散和 buff。它们缺的是像治疗那样的「走过去」链条，属于新增功能，不是修缺陷。

**0.5 治疗从不给自己留手**（2026-09-15 因格瓦尔实测，**已修** `20e3e2b0`）

`PartyMemberToHeal::Calculate` 的排序是 `probeValue = 血量% + 到自己的距离/10`。自己的距离项恒为 0，
坦克隔 25 码也只加 2.5，于是坦克长期以微弱优势压过自己（实测一刻：坦克 46+2.5=48.5 对自己 49+0=49），
**治疗永远是"第二低"，永远轮不到自己**。而戒律牧唯一的自保动作是 `critical health`(25%) 的痛苦压制。
实测十场：治疗在 50% 血以下停留 94 秒，其中只有 12 秒收到任何治疗（含 HoT 跳动），不是没蓝。
修法：自己是治疗、血量 <`mediumHealth`、且当前目标不危险（>`lowHealth`）时先治自己。
治疗阵亡场次 7/10→2/10（p=0.07），回归 9/9 零死亡。

**1. `PartyMemberToProtect::Calculate()` 第一行是 `return nullptr;`** —— 这是你记得的那条。

`src/Ai/Base/Value/PartyMemberToHeal.cpp:164`，函数整个被上游短路，下面 40 行是够不到的死代码。
`ProtectPartyMemberTrigger::IsActive()` 就是 `return AI_VALUE(Unit*, "party member to protect")`，
于是 **"protect party member" 这条触发器对所有职业永远不亮**，一次性废掉三个职业的保护链：

| 职业 | 被废掉的技能 | 接入点 |
|---|---|---|
| 圣骑士 | **保护之手**（1022，10 秒物理免疫） | `GenericPaladinStrategy.cpp:31`，相关性 `ACTION_EMERGENCY+3` |
| 牧师 | **给队友的痛苦压制**（−40%） | `HealPriestStrategy.cpp:118` |
| 战士 | 援护 intervene | `TankWarriorStrategy.cpp:324` |

实测 15 场：圣骑士三个"手"系技能（1022/6940/1038）**0 次使用**，技能都已学会（`character_spell` 里都在）。
牧师痛苦压制 0.3 次/场，全部来自"自己残血"那条（第 110 行），给队友的一次都没有。

**先看伤害学派再决定值不值得做**：保护之手是**纯物理免疫**。因格瓦尔那一轮查了 `Spell.dbc` 的
`SchoolMask`（字段下标 225，用火球/寒冰箭/惩击/腐蚀术标定）：Dreadful Roar 59734、Woe 反射 59736、
暗影斧 tick 42751、Dark Smash 59709 **全是暗影**，保护之手一个都挡不住，那条待办对该 boss 零头寸。
换 boss 前先读学派，别默认"保护链一定有用"。

**为什么在阿努巴拉克值得做**：两次团灭的起点都是"守卫咬住萨满/法师 10 秒打掉 14–23k"（505/2 法师 210s、505/3 萨满 63s），
保护之手正是这个场景的对症技能（守卫是物理近战）。而且死代码里的阈值也偏晚（坦克 ≤10%、其他 ≤30%），打开后还要调。
**风险**：这是共享层且影响三个职业，必须回归；`PartyMemberToProtect` 被短路可能有上游未记录的原因（先查 blame）。

**2. 盗贼/法师的循环仍远低于理论值**：目标接管之后毁伤仍只有约 23 次/场（理论 60–80）、能量堆满 24%、
毒伤 3.3→约 5 次/场（理论 20+）。抢占者里 `reach melee`(21) 没降。近战的"跑过去"仍占一整个 tick。

**3. 提速没做完**：并行多实例是唯一能改变节奏的一项（现在一轮 5 场 = 22 分钟，且**墙钟几乎全是战斗时间**）。
前置条件：`MapUpdate.Threads` 现在是 1，必须先验证"并发后 tick 间隔（现 0.69 秒）与击杀时间分布不变"，
否则等于换了个更差的被测对象。详见 [WORKFLOW 验证提速](WORKFLOW.md)。

---

## 三、口径陷阱清单（每一条都是本轮真踩过的）

| 陷阱 | 后果 | 正确做法 |
|---|---|---|
| **指标的分母不等于改动的作用域** | 「治疗自保」只在"治疗<65% 且无队友≤45%"这个约 5 秒/场的窗口里生效，用**全场**治疗落点分布去看，改前改后都是 16.3%——效应被稀释到看不见，一度写下"改动根本没生效"。按窗口重算是 12%→34% | 先写清楚改动**只在什么条件下生效**，指标的分母就取那个条件，别取全场 |
| **量触发器头寸时漏掉条件** | 算"萨满补位治疗"的窗口时只算了"没有能用的治疗 + 有人残血"，**没算补位者自己是否还活着且有蓝**，得出 9.9 秒/场、7/19 场；补齐后是 3.3 秒/场、5/19 场，实测 11 场触发 1 次、执行 0 次——团灭是级联的，治疗一倒补位者基本同时就没了 | 量一个触发器的头寸，就把它 `IsActive()` 里的**每一个条件**逐条放进过滤器，缺一个都会高估 |
| **从 tick 形状推机制** | 治疗死前出现成段 `no actions executed`，据此推断"被自己的 3 秒读条锁住"；加了 `casting=` 探针后实测：治疗存活期只有 **10%** 时间在读条，三次阵亡的死前 10 秒里合计只有 5 秒 | 引擎轨迹的"空"有多种成因（读条中 / 无触发 / 全被判 IMPOSSIBLE），**要区分就得加字段**，不能靠形状推；只读探针很便宜，先量再改 |
| **用刚跑完的一轮去给下一轮做取舍** | 按 534+535 那 10 场算出"萨满补位窗口从 10.4 秒/场缩到 3.2 秒/场、没有头寸"，据此砍掉了一个候选；按 19 场重算是 9.9 秒/场，**基本没变**——单轮噪声被直接传导进了决策 | 候选取舍用**累计样本**；或者至少等这一轮的率本身稳下来（CI 收窄）再算下一轮的头寸 |
| **运行中的 attempt 当成结果读** | `raidtest_attempts` 里正在跑的那一场是 `result=aborted, duration_ms=0` 的**占位行**；据此报过一次"3 场限流作废"，数据落定后其实全是击杀 | 读结果前先确认 run 已收尾（`raidtest_runs.finished_at IS NOT NULL`），别按行数判断 |
| **夹具只看"房间里有谁"** | 德雷德连续三次修正基线：0/5（两只巡逻精英晃进来）→ 2/5（还有一只**是机制要用到的迅猛龙的编队队长**，Raptor Call 拽一只就把它带进来）→ 3/5。前两次都得出了"完全打不过"的假结论 | 建夹具要另查两类：`creature.MovementType=2` 的巡逻路径（用 `creature_addon.path_id` → `waypoint_data` 算到战场的最近距离，别只看 spawn 点）、`creature_formations`（groupAI≠0 时拉一只等于拉一组） |
| **新动作的距离参数照抄基类** | 「看不见就走过去」第一版把靠近距离写成 `max(8, distance*0.5)`，而 `distance` 是治疗距离 40 码 → 20 码，治疗本来就站在 17–19 码，`ReachCombatTo` 一进门判「已经够近」返回 false：**6 次推入 6 次 FAILED，一步没走** | 移动类动作的目标距离要按"想让它走到哪"定，不能沿用触发器的判据距离；冒烟只看 `A:<动作> - OK` 计数就能当场发现 |
| **单轮 10 场当作率** | 同一改动 534+535 是 9/10、补测 539 是 1/5，合并 14/19 = 74%；若只写前 10 场就会把 90% 记进台账 | 结果率至少 20 场再写进台账，并附 Wilson 95% CI；10 场的 CI 宽到 31–83%，本来就不该当结论 |
| **冒烟场 n=1 当早停依据** | run533 冒烟主指标 0/6 且团灭，差点据此放弃；按事先声明跑满 5 场，run534 是 5/5 | 冒烟只用来确认"新动作排得上队/新规则会触发"，**不作效果判据**；早停规则针对的是事先声明的场次数 |
| **死后采样** | bot 阵亡后法力/血量记 0，被当成"没蓝了"；解咒者死后 `los` 恒为 false，被当成"没视线" | 统计资源/视线时先剔除死亡时刻之后的采样。因格瓦尔上一轮发表的"无视线秒"就踩了这条，重算后有一场团灭从 2 秒变 0 秒 |
| **转阶段的假空窗** | 因格瓦尔 P1 打死后趴地复活那 15–25 秒谁都打不到，被当成每个人的"输出空窗" | 空窗统计只算"至少有人对 boss 造成伤害"的那些秒（`blackout_stats.py` 已内置），与 AN 的潜地期同理 |
| **把 boss 不可攻击的时段混进位置统计** | 得出"盗贼 35% 站在践踏锥里"，实际只算浮出期是 19%、中位夹角 116°（本来就在背后） | 位置/站位统计必须排除潜地等 boss 不可选中的时段 |
| **按伤害事件流算"切换空档"** | 群体技能同刻命中多目标被当成换目标，算出"换目标比同目标出手还快"的荒谬结果 | 该口径作废（脚本头部已标注）。改用 tick 级"推入 vs 轮得到" |
| **窗口按整秒取** | 死亡记在 90s、致命一击记在 91s，窗口 `[t-10s, t]` 把它漏掉，得出"没挨打却死了" | 死因窗口取 `[t-12s, t+2.5s]` |
| **空的 SQL 表当成"查不了"** | `glyphproperties_dbc` 是空的，就以为无法核对雕文 | DBC 由核心运行时从 `data/world/dbc/*.dbc` 读；解文件 + 运行中核心 `lookup spell id <id>` 即可（见 [GLYPHS.md](fixtures/normal5-v1/GLYPHS.md)） |
| **`IMPOSSIBLE` 当成"没触发"** | 引擎把 Multiplier 归零（relevance≤0）也记成 `IMPOSSIBLE` | 要区分"被归零"与"施法失败"，后者看 `CanCastSpell Check Failed ... result: N`，N 对照 `SharedDefines.h` 的 `SpellCastResult`（67=冷却未好、111=目标光环状态，如疲惫） |
| **`dropped=NNNNN` 当成丢数据** | 以为承伤统计有缺口 | 那是世界上无关实体的事件被 `ShouldKeep` 过滤，自己人的一条没丢 |
| **tick 占比当成输出损失** | "42% 的 tick 在切目标"≠"42% 输出没了"——自动攻击不占 tick | 看输出空窗（5 秒窗口无伤害的比例）+ 主技能实际释放次数 |
| **`find target` 找不到潜地中的 boss** | 判据 fail-open，闸门形同虚设 | 要看不可选中的单位用 `FindNearestCreature(entry, range)` |
| **血线阈值当成阶段判据** | "可选中且 ≤25%"≠"第三次出土后"：实测 boss 191s 已可选中且 23%，第三次潜地 217s 才开始 | 阶段要用状态机数（如数出土次数），别用血量代替 |

---

## 四、这个 boss 特有的结论（**不要**迁移到别的 boss）

- 三次潜地合计 ~127 秒不可压缩，且潜地期输出已有 90% 压在大型召唤物上——「守卫优先」没有头寸。
- 判别输赢的量是**180 秒时 boss 的血**（击杀场中位 24% / 失败场 41%），不是承伤（击杀场与失败场前 240 秒承伤一模一样，391k vs 392k）。
- 毒疗者是远程施法者，它打非坦克的 66–69k **嘲讽接不走**；可寻址的只有贴身近战。
- 「白给一次圣佑远好过该开时没开」：试过收紧（破甲≥5 + 读条剩 1.5 秒再开），run 515 直接 0/5、坦克五场全死。
- 近战站位本来就对（中位夹角 116°），32k 一击秒杀是那 19% 的偶发，只能靠读条期躲，不是默认站位问题。

---

## 五、可复用的证据脚本

都在 `bosses/heroic-an-anubarak/evidence/`，口径写在各自的 docstring 里：

| 脚本 | 用途 | 迁移性 |
|---|---|---|
| `tank_mana_baseline.py` | 坦克续蓝闭环：恳求/庇护祝福 proc/**活着时的法力地板**/末段群体仇恨 | 换 boss 直接可用 |
| `taunt_baseline.py` | 承伤分解（谁打谁）、大型召唤物是否被坦克接走、嘲讽覆盖率 | 改 entry 表即可 |
| `melee_arc_stats.py` | 近战是否站在 boss 正面锥里（用 boss→主坦方向当朝向代理，并用实际命中反验） | 换锥角/半径即可 |
| `submerge_windows.py` | 用"对 boss 伤害的空档"切出 boss 不可攻击的时段 | 任何有潜地/无敌阶段的 boss |
| `healer_lure_stats.py` / `castmove_stats.py` / `prepare_fail_stats.py` | 治疗被引走、施法与移动冲突、施法失败原因 | 通用 |
| ~~`target_switch_cost.py`~~ | **口径作废**，脚本头部已注明原因 | — |

---

## 六、待办（按预期价值）

0. **给施法型取值补「走过去」链条**（**新增功能，不是修缺陷**，2026-09-16 明确放到后面）。
   `party member to dispel` / `to resurrect` / `party member without aura`（团队 buff）仍按视线过滤，
   **这是对的**——隔墙放不出驱散和 buff。缺的是治疗现在有、而它们没有的那一环：
   「目标在候选里但看不见 → 走到看得见为止」。
   - 照搬治疗那条的三步：① 加一个不按视线过滤的兄弟取值；② 给它配一个
     `<xxx> out of spell range` 型触发器（判据里带 `|| !IsWithinLOSInMap(target)`）；
     ③ 配一个接近动作，覆写 `isUseful`/`Execute` 把"距离够但看不见"也算需要移动，
     **靠近距离要单独定（别沿用 40 码的判据距离）**。
   - **动手前先量头寸**：按「该取值为空、同时确实有驱散/复活/buff 需求」的秒数算，
     别按"视线断了多久"算——德雷德那轮的教训是分母要等于作用域。
   - 风险：驱散和 buff 会让远程离开站位跑动，可能引入新的站位问题；必须回归三个旧 boss。
1. ~~**打通 `PartyMemberToProtect`**~~ —— **2026-09-16 已做**（mod-playerbots `715eb7c8`），
   搬到上面的「已修」表。要点见下面第 13 条；**在斯拉德兰上未测出效果**（频率只有 0.29 次/场）。
   下一个单变量是**阈值**（死代码里是坦克 ≤10% / 其他 ≤30%，卡得太晚）。
   **2026-09-16 更新：在英雄斯拉德兰身上第一次量出了明确头寸**（因格瓦尔是零头寸、已证伪）：
   - 实测 17 场：保护之手 1022/5599/10278 **三个等级全学会、0 次释放**；
     牧师给队友的痛苦压制同样 0 次（3 次全来自「自己残血」那条）。
     `PartyMemberToHeal.cpp:200` 至今仍是 `return nullptr;`。
   - **学派对得上**：解 Spell.dbc 得知斯拉德兰的小怪伤害**压倒性是白字物理**
     （缠绕者每场 204 次命中全是白字；毒蛇 105 次里只有 20 次是自然的 Venomous Bite），
     团灭场小怪承伤 105.0k/场 = 全部承伤的 **48%**。
   - **挂谁也对得上**：首死 13 次里非坦克占 10 次，**这 10 个全是施法者**
     （萨满 5 / 法师 3 / 牧师 2），保护之手只禁物理攻击、不影响施法；盗贼一次没先死过。
   - 对照指标与反向指标写在 [斯拉德兰记录](bosses/heroic-gd-sladran/README.md) 末尾。
   - **换 boss 前先读学派**这条仍然成立——它救过因格瓦尔一整轮，也是这次能下判断的依据。
2. **近战的 `reach melee` 仍占整 tick**：目标接管之后它没降，是循环里剩下的最大摩擦。
3. **并行多实例**（提速，先验证 tick 间隔）。
4. ~~用新基线重跑其他未通关 boss：因格瓦尔 65%~~ —— **已做，结论相反**：目标接管那批改动在因格瓦尔
   **一点没动**（6/10 对旧 65%）。"共享层收益全副本通用"不成立；每换一个 boss 都必须重测基线，
   不能拿别的 boss 的收益当预期。魔枢/AN 其余 boss 同理待测。
5. 阿努巴拉克本身可以考虑按 80% 收尾验收。

---

## 七、建新副本场景时的三条硬约束（2026-09-16 古达克一轮补）

### 1. 「准备点」必须同时过四关，而有的 boss 房**根本不存在**这样的点

隔离型场景（无前置怪）队伍**停在准备点开怪**（`AttemptRunner` 里隔离型跳过 `BossPosition`），
所以准备点必须同时满足：① on-mesh；② 距 boss **>22 码**（英雄 82 级 boss 的仇恨半径）；
③ **<30 码**（坦克制裁之手射程）；④ 对 boss **los=true**。

古达克的斯拉德兰和莫拉比**都不存在这样的点**：两人都站在一小块平台上（平台内任何点 ≤16–18 码），
而唯一的进出走廊比平台低 5–6 码、被平台边沿挡住视线。
→ 这类 boss **结构上必须走「前置清怪 → 恢复 → 传送到开怪点」这条路**，
即使清怪本身不是被测内容，也**至少要留一只前置怪**（德雷德 conf 里已有同结论，但那里是"太远"，
这里是"不存在可见点"）。**建场景第一步就该把这四关一起扫一遍，而不是先定坐标再试。**

### 2. `raidtest los` 的 z：**必须逐点用实测地面高度**（第三次踩）

探针从 `z1+2` 量。用统一 z 扫一片高低不平的区域，低处的点等于"站在几码高空看"，
结论会**整个反过来**。正确做法是两遍：第一遍随便给个 z 只读 `vmap_floor_from`，
第二遍把每个点自己的 `vmap_floor_from` 当 z1 重扫。
古达克这一轮第一遍扫出"两条走廊对 boss 全部可见"，第二遍是**全部不可见**。

### 3. `PrerequisiteSpawns` 只接 spawn guid，**接不了召唤物**

德拉克瑞巨像（29307）开场挂 `UNIT_FLAG_NON_ATTACKABLE`，必须先打死它**自己召的** 5 只 Living Mojo
才解锁。这些召唤物没有 `creature.guid`，而 `StartBossPull` 只对 `BossEntry` 下拉怪指令——
**框架当前拉不动这类 boss**。要做需要给 mod-raidtest 新增「按 entry 指定开怪前必须清掉的召唤物」，
属于**新增能力**，动手前确认口径。诺沃斯 P1 的"开怪即不可攻击"只是不可选中、boss 本体仍可被 assist，
与这一类**不是同一个问题**，别把那条修复当成已经覆盖。

### 4. 记一条口径：清怪失败 ≠ boss 打不过

`heroic-gd-sladran-n5`（完整档，九只走廊精英当前置）run 576：**84 秒全清完、boss 全程没进战斗**，
但因为清怪期间死了 1 人被门禁判作废。这说明**「完整档跑不起来」有两种完全不同的原因**——
"编排/坐标错"和"bot 清不动怪"。前者要改 conf，后者属于「整本全清」范畴，
按当前口径（只验证 boss 机制）不该在 boss 轮里解决。**作废原因要读到具体那一条，别一律归到场景没建好。**

### 5. 载具型小怪：`creature` 表的行数不等于怪的只数，夹具也删不干净（2026-09-16 迦尔达拉）

`vehicle_template_accessory` 会给载具自动配乘客，而 `creature` 表里**只有载具那一行**：
古达克竞技场里「四只 Drakkari Rhino」实际是 **10 只**
（29838×3 各带一名 Drakkari Battle Rider 29836；29931×1 带三名 Drakkari Raider 29982）。
建夹具时**必须查 `vehicle_template_accessory`**，否则怪的规模会算少一半以上。

更麻烦的是**删不干净**：`FixtureDespawnSpawns` 走 `Creature::DespawnOrUnsummon()`，
实测**不会**把 accessory 乘客带走——载具消失了，乘客留在原地存活并照常参战（run 580 里 2/5 场）。
乘客是召唤物、**没有 spawn guid**，`FixtureDespawnSpawns` 根本接不了它们。
要清干净需要给 mod-raidtest 加「按 entry 移除」的夹具能力（与巨像那条待办同族，**新增功能**）。
**在那之前，口径必须照实写「移除了载具、乘客仍在」**，别写成「已清空」。

### 6. 「房里还有没清的怪」不是背景条件，是会被躲避动作推进去的（2026-09-16 迦尔达拉）

判别量做出来非常干净：run 578 五场里，**3/3 团灭都有人进过房怪的 21 码仇恨半径
（最近 15.9 / 13.9 / 15.8 码），2/2 击杀都没进（最近 22.9 码）**；
移除房怪后重跑 run 580 是 **5/5 零死亡**。

推进去的不只是"bot 乱跑"——`AvoidWhirlingSlashAction` 这类躲避动作对坦克是
`MoveAway(boss, 2*radius + 2 - dist)`（背离 boss 挪到 12 码），**方向完全由"坦克在 boss 哪一侧"决定，
没有任何边界判据**；boss 再 `MoveChase` 跟上来，于是**坦克-boss 这一对整体平移**，
一次旋刃走 20 码，而旋刃在巨魔形态每 17–19 秒一次，会**反复棘轮**。

**做法**：算准备点时不能只算"开局距离"，要留出**战斗漂移余量**；房间放不下就把那组怪列进场景
（前置或夹具），别指望"离得远就不会被拉"。
（想改躲避动作的方向判据是**共享层**改动，**先量头寸**：躲避动作造成的净位移、
以及位移方向命中未清怪的比例。）

### 7. 乘子归零与施法失败**可以**精确分离——引擎明写了（2026-09-16 斯拉德兰）

LESSONS 第三节那条「`IMPOSSIBLE` 当成'没触发'」说的是 `A:<动作> - IMPOSSIBLE` 分不清成因。
**但 `LogInGroupOnly=0` 的 `Playerbots.log` 里，被乘子归零时引擎会单独写一行**：

```
Raidtednfivc Multiplier slad'ran made action blizzard useless
Raidtednfivc A:blizzard - IMPOSSIBLE
```

而施法失败写的是 `CanCastSpell Check Failed. - target name: X, spellid: N, ..., result: M`。
所以只要数 `Multiplier <名字> made action <动作> useless` 就能**精确**得到「被哪个乘子归零了多少次」。
斯拉德兰那一轮靠它把结论钉死：暴风雪/烈焰风暴/刀扇的 `IMPOSSIBLE` 计数与归零计数**完全相等**
（101=101、101=101、66=66），**一次施法失败都没有** → 乘子是唯一原因。
**别再用"IMPOSSIBLE 数量"当归零证据，直接数 Multiplier 行。**

### 8. `raidtest_events` 的 Death 只覆盖登记实体，**统计不了召唤物**（2026-09-16 斯拉德兰，我踩了）

`CombatEventBus::ShouldKeep` 对 `Death` 只保留 `IsMember(e.source)`——bot / boss / 已登记的前置怪。
boss 召出来的小怪死了**不入库**。我据此写过「boss 召的小怪一只都没死」，**是错的**：
按「bot 对该 guid 的累计伤害 vs 该 entry 的血量」重算，实际打死了 16–42%。

**做法**：统计召唤物的存活/击杀，用伤害累计 + `creature_template` 的
`basehp(exp)` × `HealthModifier` × `Rate.Creature.*.HP` 反推，别查 Death 表。
（Damage 事件是双向都收的，`IsMember(source) || IsMember(target)`，所以 bot → 召唤物的伤害**在库里**。）

### 9. AoE 的头寸要按「命中数」算，不能按单体 DPS 算（2026-09-16 斯拉德兰）

我第一版按单体算：刷怪速率 0.75 只/秒 × 6,517 血 = 4,888 HP/秒，而全队总输出才 4.5–5.5k/秒，
得出「清不完，AoE 没头寸」。**这是错的**——AoE 的产出随命中数线性放大：
一发暴风雪对每个目标约 8k，场上常驻 30–50 只，**只要 5 只以上进圈就追平刷怪速率**。
**算 AoE 的头寸必须带上"同时能命中几个"这个乘数**，否则会把唯一有效的手段算成没用。

### 10. 占比类指标必须配着绝对值一起看——分母塌了也会让占比变好（2026-09-16 斯拉德兰第二刀）

删掉「有包裹时禁 `dps assist`」之后，靶子指标 **boss 输出占比 38.9% → 52.2%**，看着是成功的。
但**绝对值是反的**：每场打到 boss 的伤害 221.3k → 184.4k（−17%），
boss 每秒输出 2,345 → 2,373（**没变**），场次时长 95.1s → 77.3s（**死得更快**），击杀 2/10 → 0/5。
真正发生的事：包裹被打死从 3.9/场 掉到 0.8/场，被困的人出不来 → 队伍更快团灭 → 分母塌了。
**只看占比会把一次净负面的改动读成成功。**

### 11. 「理论上会抖动」这类顾虑要用数据判，别用它否方案（同上）

我动手前判断删掉这段会造成目标抖动（`attack snake wrap` 与 `dps assist` 互相抢目标，
而换目标要四个 tick）。实测**每秒主目标变化率反而从 44% 降到 33%**——顾虑是错的。
这条改动最终该回退，但**理由不是抖动**，而是「DPS 会半路丢下包裹」。
**用错的理由否掉方案，和用错的理由支持方案一样危险**；把顾虑写成一个可测指标再跑。

### 12. 「保护链有没有用」= 学派 × 挂谁 × 谁先死，三个都要对上（2026-09-16 斯拉德兰）

因格瓦尔那轮否掉保护之手，用的是**学派**（致死伤害全是暗影，纯物理免疫挡不住）。
斯拉德兰这轮肯定它，用的是同一套判据反过来跑通：

1. **学派**：小怪伤害压倒性是白字物理（解 Spell.dbc，SchoolMask 字段下标 225），占团灭场承伤 48%；
2. **挂谁**：保护之手禁物理攻击但不禁施法 —— 而首死的 10 个非坦克**全是施法者**，
   盗贼一次没先死过，所以「近战被封住输出」这个副作用不成立；
3. **谁先死**：13 场有死亡的场次里非坦克占 10 次，保护链本来就是用来保护*别人*的
   （因格瓦尔那轮的另一半否定理由是「快死的就是治疗自己，够不着」）。

**三个判据缺一个，结论就可能反过来。** 下次遇到「要不要打通保护链」，按这三条逐条量，
别只看其中一条。

### 13. 一个缺陷后面可能还藏着第二个——**靶子指标为 0 时别急着否方案**（2026-09-16 保护链）

打通 `PartyMemberToProtect` 的 `return nullptr;` 之后，**保护之手仍然 0 次**。
按「冒烟为 0 就中止」的规矩我中止了那一轮——但**没有据此否掉方案**，而是去查为什么。
查出第二个独立缺陷：`CastBlessingOfProtectionProtectAction` 注册的是 TBC 旧名
`"blessing of protection"`，而 WotLK 的 DBC 里 1022/5599/10278 叫 `"Hand of Protection"`；
`SpellIdValue::Calculate()` 是**按名字**查的，名字对不上就永远 `isPossible=false`。

**分辨这两种「0」的办法**：看**同一条链上的别的职业**。
只修第一个缺陷时，牧师的 `pain suppression on party` 立刻从 0.18 跳到 1 次/场——
**说明取值和触发器已经通了**，那 0 只可能卡在圣骑士这一侧。没有这个旁证，
很容易把「还有第二个缺陷」误判成「这条路没用」。

**顺带一条可复用的检查**：playerbots 里所有按名字取法术的动作，
都可能踩 TBC→WotLK 的改名（Hand of Protection / Freedom / Salvation / Sacrifice）。
本代码库里只剩 `"blessing of protection"` 一处旧名，已修。
验的办法是运行中核心 `lookup spell id <id>` 对一遍名字。

### 14. 「上游为什么短路掉这段」要 blame 出来，不要猜（同上）

`PartyMemberToProtect` 那行 `return nullptr;` 的来历是
**2023-07-27 `6ba6bc361 "performance optimize"`**——一次批量性能提交里顺手加的一行，
**没有任何正确性理由**。LESSONS 原来的措辞是「可能有上游未记录的原因（先查 blame）」，
这次查出来的结论是**没有**，而且把那个理由也用数据否掉了：
打开之后 worldserver 的 tick diff 反而更好（mean 3ms → 2ms、p95 9–11ms → 6ms）。
**把上游的顾虑变成一个可测的反向指标，比拿它当不做的理由强。**

### 15. 前一刀可能把后一刀的头寸吃掉（2026-09-17 斯拉德兰第五刀）

第一刀（删掉「DPS 全程禁 AoE」）之后，DPS 的输出主体从单体变成了 AoE。
第五刀（把 DPS 的目标固定到 boss）于是**没有头寸了**——
盗贼的 tick 级 boss 目标占比 32% → 76%，伤害占比只从 32% → 42%（刀扇是 AoE）；
法师目标 62% → 56%，伤害占比 32% → 28%（暴风雪/烈焰风暴是**地面** AoE，完全不看目标）。
聚合结果：boss 输出占比 38% → 39%，击杀 0/7 → 0/9。

**教训**：头寸是在**当时那一版**上量的。前一刀改变了输出结构之后，
**后一刀的头寸必须重量**，不能直接用改动之前算的那份。

### 16. 判据选错要承认并重新事先声明，不能换一个能通过的（同上）

第五刀我把「boss 输出占比 ≥50%」当冒烟判据，冒烟 34.7% 不通过，按声明中止。
随后查执行率（`PUSH 71 → OK 36`）才发现**动作明明在跑**，是**判据错了**——
AoE 打开之后 boss 输出占比里混了溅射伤害，它不再是「目标选择」的代理。
正确处理是**承认判据无效、重新事先声明一个**（换成「打到 boss 的伤害/场 + 击杀率」
加两条反向指标），然后重跑。
**不能看完数据再挑一个能通过的指标**——那和滥用早停是同一类错误。

### 17. 「注册了但永远不亮」有三种形态，都会让整条链静默死掉（2026-09-16/17 斯拉德兰）

一轮里连着撞上三个，形态不同、后果一样——**动作在策略里注册得好好的，但一次也放不出来**：

| 形态 | 实例 | 怎么发现的 |
|---|---|---|
| **取值被短路** | `PartyMemberToProtect::Calculate()` 第一行 `return nullptr;` | 保护之手三个等级全学会、0 次释放 |
| **法术名过期** | 动作注册 `"blessing of protection"`，而 WotLK 的 DBC 里叫 `"Hand of Protection"`；`SpellIdValue` 按名字查 → 查不到 id | 打通取值后**牧师的同链动作立刻从 0.18 跳到 1 次/场，圣骑士仍然 0** |
| **触发器名没注册** | `TriggerNode("high threat", ...)`，而全代码库只有 `creators["medium threat"]` | 镜像 24 场只放 1 次；grep `"high threat"` 只有使用点、没有 creator |

**排查顺序（从便宜到贵）**：
1. `grep` 那个名字在代码库里**有没有 creator**（触发器名/动作名打错是最便宜的一类）；
2. 运行中核心 `lookup spell id <id>` 对一遍**法术名**（TBC→WotLK 改过名的：
   Hand of Protection / Freedom / Salvation / Sacrifice；Blessing of Might/Wisdom/Kings/Sanctuary 没改）；
3. 读取值/`isUseful()` 有没有被短路（`return nullptr;` / `return !bot->GetGroup();` 这种）；
4. 最后才是 tick 级的「排不上队」（那是相关性问题，见第 1.2 条）。

**关键旁证**：靶子指标为 0 时，去看**同一条链上的别的职业**。
只修第一个缺陷时牧师的 `pain suppression on party` 立刻活了，而圣骑士还是 0 ——
这直接指向「还有第二个缺陷，且只卡在圣骑士这一侧」。没有这个旁证很容易误判成「这条路没用」。

### 18. 回归失败先查「改动在那个 boss 上会不会触发」，再谈归因（2026-09-17）

法师镜像那一刀的共享层回归是 8/9，失败的是凯利丝塔萨。
但**镜像在凯利丝塔萨 0/6 场、阿诺玛鲁斯 0/6 场**——它挂在 `medium threat`
（身上 ≥2 个攻击者）上，而那两个 boss 要么是单体、要么小怪很少两只同时咬法师。
**改动在那里是惰性的，机制上就不可能是死因。**

**这比"再跑 3 场看是不是噪声"强得多**：补场次只能刻画噪声，
而「触发次数为 0」直接排除了归因。回归出现失败场次时，
**第一步永远是量改动本身在那个场景触发了几次**。

### 19. 两个都要 GCD 的技能别挂在同一个高相关性节点上（2026-09-17 神圣之锤）

给防骑的神圣之锤在 `medium aoe` 节点上加了一条相关性 51（奉献 52 的下一位），
靶子指标动得很漂亮（2.20 → 5.20 次/场，+136%），**结果却是净负面**：
击杀 2/5 → 0/5、打到 boss 的伤害 −10%、团灭场 boss 残血 5.0% → 15.0%。

机制：**两者抢同一个 GCD**。奉献先被评估，但神圣之锤放掉之后占住 GCD，
下一 tick 奉献冷却好了却卡在公共冷却里 → **奉献被推迟 11%**。
而奉献是不限目标的地面 AoE、神圣之锤只命中 3 个——**用后者换前者是亏的**。
直接证据：坦克接住的小怪伤害反而 −26%，而小怪总承伤 +7.5%。

**做法**：同一个节点里只放一个吃 GCD 的主力；次要的留在填充档。
要同时抬两个，先把价值最高的那个抬到满覆盖再说。

### 20. 冒烟验得了执行率，验不出「挤占」（同上）

这一刀冒烟 2 场时反向指标看着没问题（奉献 13 次/2 场，与改前持平），
5 场正式对照才显出 **−11%**。
**反向指标（尤其是"别的动作有没有被挤掉"）必须放到正式轮里看，2 场的冒烟撑不住。**
冒烟的职责只有一个：确认新动作排得上队。
